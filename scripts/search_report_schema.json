{
  "type": "object",
  "properties": {
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "route": {
            "type": "string",
            "description": "Tên route search được giao (giữ nguyên văn)"
          },
          "claim": {
            "type": "string",
            "description": "Một khẳng định ngắn, rút từ evidence — không từ kiến thức training"
          },
          "source_url": {
            "type": "string",
            "pattern": "^https?://[^/]+/\\S+$",
            "description": "URL đầy đủ (bắt buộc có path) lấy từ kết quả tool"
          },
          "evidence_quote": {
            "type": "string",
            "description": "Trích nguyên văn từ nguồn chứng minh claim"
          },
          "score": {
            "type": "integer",
            "minimum": 0,
            "maximum": 10,
            "description": "Độ tin cậy model tự chấm 0–10 (chỉ là tie-break khi rank)"
          }
        },
        "required": ["route", "claim", "source_url", "evidence_quote", "score"],
        "additionalProperties": false
      }
    },
    "not_found": {
      "type": "boolean",
      "description": "true khi không tìm được evidence cho câu hỏi trong route này"
    },
    "queries_used": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Các truy vấn search đã chạy thật trong phiên"
    }
  },
  "required": ["findings", "not_found", "queries_used"],
  "additionalProperties": false
}
