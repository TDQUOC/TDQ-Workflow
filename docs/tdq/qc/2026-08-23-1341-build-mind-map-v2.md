# QC — 2026-08-23-1341-build-mind-map-v2

Soul: chất lượng > runtime > context cost · luật gốc: skills/tdq-conventions/references/soul.md
Plan: ../plan/2026-08-23-1341-build-mind-map-v2.md · Spec §6: 12 hạng mục · Kết quả: 12/12 PASS

| # | Hạng mục | Lệnh / cách kiểm | Kết quả | Phán quyết |
|---|---|---|---|---|
| Q1 | Sáu mục đánh số | `grep -c '^## [1-6]\.'` | 6 | PASS |
| Q2 | Mỗi mục nhắc đường dẫn file | đếm backtick chứa `/` hoặc `.` theo từng mục | 25 · 6 · 13 · 8 · 7 · 8 | PASS |
| Q3 | Bốn lệnh có chữ ký đầy đủ | `grep -c '^### Lệnh '` | 4 | PASS |
| Q4 | Ít nhất năm luật khuôn | `grep -cE '^\| L[0-9]+ '` | 7 | PASS |
| Q5 | Ký hiệu dẫn ra có thật | `grep -c` trên `tdq_state.py` và `doc_lint.py` | 15 và 4 kết quả | PASS |
| Q6 | File mẫu qua mọi luật khuôn | kiểm tay L1 đến L7, bằng chứng bên dưới | 7/7 đạt | PASS |
| Q7 | File mẫu có nhánh lỗi | `grep -c '!' docs/tdq/mind-map/vi-du-login.md` | 2 | PASS |
| Q8 | Bảng việc bàn giao | đếm dòng bảng dưới mục Bàn giao | 7 dòng | PASS |
| Q9 | Không đụng file ngoài `docs/` | `git status --porcelain \| grep -v '^.. docs/' \| grep -v graphify-out` | 0 dòng | PASS |
| Q10 | Lint sạch | `doc_lint.py` trên spec, plan, thiết kế, file mẫu | 0 vi phạm, exit 0 | PASS |
| Q11 | Trần 320 dòng | `wc -l` | 270 | PASS |
| Q12 | Nêu cửa thoát của gate | `grep -c '^### Cửa thoát'` | 1 | PASS |

## Bằng chứng Q6 — file mẫu đối chiếu từng luật

File kiểm: `docs/tdq/mind-map/vi-du-login.md`, 9 dòng.

| Luật | Điều kiện | Quan sát trên file mẫu | Đạt |
|---|---|---|---|
| L1 | dòng đầu là `# <tên>` | dòng 1 là `# Đăng nhập`, đúng một dòng | có |
| L2 | mỗi bước một dòng, mở `B<số> · ` | 8 dòng bước, cả 8 đúng khuôn | có |
| L3 | số tăng dần, không nhảy, không trùng | 1, 2, 2!, 3, 4, 4!, 5, 6 | có |
| L4 | nhánh lỗi `B<số>!` đặt ngay dưới bước nó chặn | `B2!` dưới `B2`, `B4!` dưới `B4` | có |
| L5 | mỗi dòng đúng một cặp `(file::hàm)` hoặc `(?)` | 7 dòng có cặp, 1 dòng ghi `(?)`, không dòng nào có hai cặp | có |
| L6 | mô tả bằng lời nghiệp vụ, không tên hàm | không mô tả nào chứa tên hàm, tên hàm chỉ nằm trong ngoặc | có |
| L7 | có ít nhất một nhánh lỗi | có hai | có |

## Ghi chú

- Không có defect, không có vòng sửa.
- Dấu `(?)` ở dòng `B6` là CỐ Ý: nó minh hoạ trạng thái thứ ba của `doi-chieu`, và minh hoạ
  đúng thứ mà cổng chặn `qc` ở mục 4 tồn tại để bắt.
- Một memory cũ trong mem0 (`138cb2e9`) ghi kết luận v1 là không thêm phase mới. Kết luận đó
  đã bị v2 lật. Tôi để nguyên và báo user, vì sửa hay xoá memory cần user đồng ý trước.
