Soul: chất lượng > runtime > context cost · luật gốc: skills/tdq-conventions/references/soul.md

## Nguyên văn

User: "mở request check vụ tính time cho plan hiện tại có vẻ timer đó là cho người chứ ko phải
time cho agent code hãy check và chỉnh sửa lại"

Sau 2 vòng hỏi lại để xác định đúng chỗ:
- Vòng 1: tưởng user nói về `tdq_timing.py` (treo tường vs model chạy trong report) — SAI,
  file đó đã tách đúng 2 cột từ trước, không phải vấn đề.
- Vòng 2: đúng chỗ là field `eNm` trong `skills/tdq-plan/references/plan-template.md` — số
  phút Claude tự ước tính khi viết plan cho mỗi task, mô tả sẵn là "thời gian LÀM, không tính
  lúc chờ duyệt" (tức đã định nghĩa là thời gian agent, không phải người). Không có code nào
  đo lại hay đối chiếu con số này — nó chỉ là ước tính tĩnh ghi 1 lần lúc viết plan.

Chốt yêu cầu: **bỏ điểm độ phức tạp `(nN)` cùng thang neo mốc của nó** — không dùng "theo
mốc" (anchor scale 1–10) nữa. ETA mỗi task chỉ còn dựa vào `eNm` = ước tính số phút để
Claude Code THỰC THI xong task đó (thời gian agent, không phải thời gian người chờ).

## Hiểu & kiến thức

Đã đọc:
- `skills/tdq-plan/references/plan-template.md` mục "Điểm độ phức tạp `(nN)` và ước tính phút
  `(eNm)`" — có thang neo mốc 1–10 cho nN, luật chấm cả nN và eNm cùng lúc lúc viết task.
- `skills/tdq-plan/SKILL.md` dòng 30-36 — lặp lại khuôn `(n3 e6m)`, nói rõ "Status line đọc
  điểm này để ước tính ETA theo trọng số" (mô tả định hướng, CHƯA có code implement).
- Grep toàn repo (`scripts/*.py`, `hooks/*.py`): không có chỗ nào parse/đọc `nN` hay `eNm` —
  hai field này thuần tuý là annotation trong text plan, không ảnh hưởng logic runtime, state,
  hay hook nào. Sửa là sửa THUẦN VĂN BẢN hướng dẫn (2 file: plan-template.md, tdq-plan/SKILL.md)
  + có thể cả các plan cũ đang dùng khuôn `(nN eNm)` (không bắt buộc sửa ngược, vì nN vốn đã
  optional — "thiếu `(nN)` thì bộ đọc coi như 5").

Phạm vi sửa:
1. Bỏ hẳn `(nN)` và thang neo mốc 1-10 khỏi plan-template.md và tdq-plan/SKILL.md.
2. Khuôn task chỉ còn `- [ ] **T1.1** (e6m) việc — Test: ...` (eNm đứng một mình).
3. Giữ nguyên định nghĩa eNm = phút Claude ước tính để THỰC THI (agent time), không tính chờ
   duyệt/interview — định nghĩa này vốn đã đúng, không đổi.
4. Bỏ luôn câu "Status line đọc điểm này để ước tính ETA theo trọng số" (nhắc tới nN) — thay
   bằng câu ETA = tổng eNm của các task chưa xong (đơn giản, không trọng số phức tạp nữa).
5. Không đụng `tdq_timing.py` (đã đúng, không phải vấn đề).
6. Không cần sửa các plan cũ đã có `(nN eNm)` — cú pháp cũ đọc được vẫn không lỗi (chỉ là
   annotation text, không có parser bắt buộc).

## Hỏi đáp

- Q: Vấn đề có phải ở `tdq_timing.py` (treo tường vs model)? → A: Không, đã tách đúng sẵn.
- Q: Đúng ý là gì? → A: Bỏ điểm nN theo thang neo mốc, ETA mỗi task chỉ tính theo eNm = ước
  tính thời gian Claude Code thực thi.

### Lộ trình

1. Sửa `skills/tdq-plan/references/plan-template.md`: bỏ mục nN + thang neo mốc, còn lại
   khuôn `(eNm)` đơn, luật chấm chỉ còn eNm.
2. Sửa `skills/tdq-plan/SKILL.md`: bỏ nhắc `nN`, đổi khuôn ví dụ task, sửa câu ETA.
3. Kiểm không còn chỗ nào khác trong repo nhắc `(nN)`/thang neo mốc liên quan plan task (đã
   grep sơ bộ, không có code phụ thuộc).
