# Brief: phiên bản npm mới nhất của 2 package

## Câu hỏi
1. Phiên bản mới nhất (dist-tag latest) của package npm `typescript` là bao nhiêu?
2. Phiên bản mới nhất (dist-tag latest) của package npm `@anthropic-ai/claude-code` là bao nhiêu?

## Ngữ cảnh
Cần số phiên bản chính xác đang phát hành trên npm registry tại thời điểm hiện tại (2026-07-31).
Mỗi claim phải nêu rõ tên package + số phiên bản.

## Tiêu chí rank
Ưu tiên nguồn npmjs.com (trang package chính thức) hoặc registry.npmjs.org.
Nguồn phụ: GitHub releases của chính project. Score cao khi evidence quote chứa đúng số phiên bản.

## Dữ kiện đã có
Không có — phải tìm từ nguồn web.
