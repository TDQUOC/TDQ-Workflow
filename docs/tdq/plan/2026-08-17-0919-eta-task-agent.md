# QUICK — Bỏ điểm nN, ETA task chỉ tính theo eNm (thời gian agent)

**Ngày:** 2026-08-17 · Brief: ../brief/2026-08-17-0919-eta-task-agent.md · Lane: quick
**Trạng thái:** CHỜ DUYỆT
**Ước tính sẽ dùng skill:** không có

## Phạm vi
- Trong: bỏ điểm độ phức tạp `(nN)` + thang neo mốc khỏi `skills/tdq-plan/references/plan-template.md`
  và `skills/tdq-plan/SKILL.md`; khuôn task chỉ còn `(eNm)`; câu ETA đổi thành tổng eNm của
  task chưa xong, thay cho câu "Status line đọc điểm này... theo trọng số".
- NGOÀI: không sửa `tdq_timing.py` (đã đúng); không sửa ngược các plan cũ đã có `(nN eNm)`;
  không viết code parser cho eNm (vẫn thuần annotation text).

## Task
- [x] **T1** Sửa `plan-template.md`: xoá mục "Điểm độ phức tạp `(nN)`" + thang neo mốc, đổi
  khuôn ví dụ và luật chấm chỉ còn `(eNm)` — Test: `grep -c "(nN)" skills/tdq-plan/references/plan-template.md` → `0` PASS
- [x] **T2** Sửa `tdq-plan/SKILL.md`: bỏ nhắc `nN`/`n3`, đổi khuôn ví dụ `(n3 e6m)` → `(e6m)`,
  sửa câu ETA — Test: `grep -c "nN\|n3 e" skills/tdq-plan/SKILL.md` → `0` PASS
- [x] **T3** Quét toàn repo xem còn chỗ nào khác nhắc `(nN)` liên quan khuôn task plan —
  Test: `grep -rn "(nN)" skills/ docs/tdq/reports 2>/dev/null` → `(rỗng)` PASS.
  Phát sinh thêm: tìm thấy `skills/tdq-build/SKILL.md` cũng nhắc `(nN)`, đã sửa luôn
  cho nhất quán (đổi ví dụ sang `(eNm)`, câu ETA sang tổng eNm).

## Definition of Done
- `plan-template.md` và `tdq-plan/SKILL.md` không còn nhắc điểm `nN` hay thang neo mốc
- Khuôn task mẫu trong cả 2 file thống nhất dùng `(eNm)` đơn, không kèm `nN`

## QC
- Q1 test từng task: PASS — `grep -c "(nN)" plan-template.md` → `0`; `grep -c "nN\|n3 e" tdq-plan/SKILL.md` → `0`; `grep -rn "(nN)" skills/ docs/tdq/reports` → rỗng
- Q2 DoD "plan-template.md và tdq-plan/SKILL.md không còn nhắc điểm nN hay thang neo mốc": PASS — `grep -c "nN\|thang neo mốc" skills/tdq-plan/references/plan-template.md skills/tdq-plan/SKILL.md` → `0` cả hai
- Q3 DoD "Khuôn task mẫu trong cả 2 file thống nhất dùng (eNm) đơn, không kèm nN": PASS — grep `(eNm)`/`(e...m)` cho thấy mọi ví dụ (`T1.1 (e6m)`, `T1.2 (e12m)`, `T2.1 (e20m)`) chỉ còn một số phút, không kèm điểm nN
