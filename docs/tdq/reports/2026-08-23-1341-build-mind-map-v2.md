# REPORT — thiết kế lớp sơ đồ giải thuật mind-map v2 (`2026-08-23-1341-build-mind-map-v2` · lane full · mode main · 11 task tick đủ)

Soul: chất lượng > runtime > context cost · luật gốc: skills/tdq-conventions/references/soul.md

**Đã làm:** P1 khung tài liệu và bảy luật khuôn file sơ đồ · P2 chữ ký bốn lệnh `sinh` `kiem`
`doi-chieu` `xem` cùng thuật toán đối chiếu offline · P3 ba chỗ cắm vào `tdq_state.py`, một chỗ
cắm vào `doc_lint.py`, skill mới và hai skill phải sửa · P4 file mẫu theo khuôn mới và bảng bàn
giao bảy dòng · P5 lint, kiểm ranh giới, ghi mem0.
**Kết quả:** tài liệu thiết kế 270 dòng (trần 320) · 6/6 mục đánh số · 4 lệnh đủ chữ ký · 7 luật
khuôn · bảng bàn giao 7 dòng.
**Kiểm:** `doc_lint.py` trên spec, plan, thiết kế, file mẫu, QC → 0 vi phạm, exit 0 · QC 12/12
PASS, không defect, không vòng sửa · `git status` xác nhận 0 file ngoài `docs/` bị đụng.
**Đầu ra:** `docs/tdq/knowledge/2026-08-23-thiet-ke-mind-map-v2.md` ·
`docs/tdq/mind-map/vi-du-login.md` · `docs/tdq/qc/2026-08-23-1341-build-mind-map-v2.md`
**Kết luận chính:** thiết kế xong, CHƯA apply. Ba điểm đáng chú ý nhất: cổng chặn `qc` nằm ở
`scripts/tdq_state.py` cạnh `_chan_worktree_con_mo` chứ KHÔNG ở hook; luật khuôn mới phải cắm vào
nhánh `is_output` của `lint_file()` chứ thêm vào `RULES` thì không chạy; `doi-chieu` tra thẳng
`graphify-out/graph.json` nên chạy offline, không cần LSP.
**Giới hạn:** đây là đề xuất trên giấy, chưa một dòng code nào chạy thật · khuôn file sơ đồ mới
chỉ được thử trên đúng một file mẫu · lệnh `dung-nguoc` và việc dựng sơ đồ ngược cho tính năng cũ
nằm ngoài phạm vi theo đúng câu trả lời của user · phép đo `mo-phong` chấm mode đội thắng nhưng
tôi đề xuất ngược lại, lý do đo sai đã ghi trong mục `Cụm song song` của plan.
**Git:** chưa commit.
