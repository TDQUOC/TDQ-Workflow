# REPORT — Luôn đánh số câu hỏi trắc nghiệm (`2026-08-22-2319-danh-so-cau-hoi` · lane full · mode subagent · 10/10 task tick đủ)

Soul: chất lượng > runtime > context cost · luật gốc: skills/tdq-conventions/references/soul.md

**Nguyên nhân gốc:** ảnh user gửi KHÔNG phải thiếu luật — `scope-round.md` đã có sẵn luật
"gộp câu 1 và các câu bối cảnh vào một khối, đánh số liên tục". Luật bị vi phạm vì nó chỉ nằm
ở 2 file con đi hỏi, chưa bao giờ được nâng lên file khuôn gốc, và không có bước đọc lại bản
nháp trước khi gửi.

**Đã làm:** P1 — thêm luật trang trí thứ 8 (mọi câu có option đều đánh số `1.`, `2.`, liên tục
cả khối, áp cả khi chỉ 1 câu) + bước tự-soát 3 điểm bắt buộc vào `user-facing-block.md`, và
đồng bộ mục 1 của `~/.claude/CLAUDE.md` · P2 — khớp 5 file mẫu câu hỏi: `interview.md`
(bỏ điều kiện "several questions", thêm ví dụ trả lời "1a 2b"), `scope-round.md`,
`lane-decision.md` và `mode-gate.md` (thêm dòng số trước khối option — 2 file này trước đây
hỏi 1 câu nên không có số), `approval.md` · P3 — build lại portable, khoá luật bằng test.

**Kết quả:** luật đánh số 2 file con → 1 file khuôn gốc + 5 file con + CLAUDE.md · khối câu hỏi
1 câu: không số → có số · luật trang trí 7 → 8 · bước tự-soát trước khi gửi: không có → bắt buộc.

**Kiểm:** `pytest tests/ -q` → 37 đỏ / 1315 xanh, đúng mốc nền 37 đỏ (toàn bộ ở
`tests/test_skill_router.py`, có từ trước request) · `test_user_facing_block.py` 11 passed /
58 subtests · `doc_lint.py` 7 file + `--pair` exit 0 · QC độc lập bằng sub-agent: 9/9 hạng mục
Q1–Q9 + 4/4 hạng mục cố định PASS, 5 defect mức thấp, đã sửa 3 (D1 D3 D5).

**Đầu ra:** `skills/tdq-conventions/references/user-facing-block.md` (luật 8 + tự-soát) ·
`docs/tdq/qc/2026-08-22-2319-danh-so-cau-hoi.md` · Backup ngoài repo: `~/.claude/CLAUDE.md`
sửa trực tiếp mục 1 theo đúng lựa chọn 4b của user, không có bản backup riêng.

**Bằng chứng luật được khoá thật:** thêm `test_hard_rules_giu_buoc_tu_soat` — xoá dòng 95–102
của file luật thì test ĐỎ, khôi phục thì XANH. Trước đó xoá cả bước tự-soát mà suite vẫn xanh.

**Việc này tự chứng minh vấn đề:** giữa lượt, ở chính cổng hỏi mode, user trả lời "1" (số câu,
thiếu chữ cái) → không rõ nghĩa, phải hỏi lại. Đó là ca ngược của ảnh ban đầu và là lý do
`mode-gate.md` nay ghi rõ `nhắn "1a" / "1b"`.

**Giới hạn:** không ép cú pháp trả lời gộp (user chọn 2c) — chỉ bắt buộc có số · không có
lint/hook nào quét được văn bản chat, luật chỉ neo bằng file + test + bước tự-soát, nên vẫn
phụ thuộc agent chịu đọc lại bản nháp · D4 (3 dòng lặp trong `mode-gate.md`) bị bác: đó là 3
thành phần khác nhau của khuôn 5 thành phần, gộp lại là đổi khuôn, cần request riêng.

**Git:** CHƯA commit. 56 file đổi. Lưu ý: `build_portable.py` build lại đã cuốn theo tồn đọng
của commit `bac6de5` (`report-template.md`, `stop_gate.py`, `tdq_state.py` bản portable) —
commit đó không build portable, nên message commit phải nói rõ phần này (defect D2).

## Thời gian

| Phase | Wall clock | Model time | Times entered |
|---|---|---|---|
| idle | 3s | 0s | 1 |
| analyze | 14 min | 2 min | 1 |
| spec | 3 min | 1 min | 1 |
| plan | 3 min | 3 min | 1 |
| implement | 26 min | 20 min | 1 |
| qc | 4s | 4s | 1 |
| report | 3s | 0s | 1 |
| **Total** | **47 min** | **28 min** | |

Mode `subagent` chỉ giao được 1/10 task (T3.2 — QC độc lập); 9 task còn lại leader giữ vì
`file-luat` (sửa chính file luật leader đang chạy) hoặc `vung-khoa` (không khai vùng file
riêng). `mo-phong` dự đội thắng 2,0 phút; thực tế phần lớn thời gian implement là leader.
