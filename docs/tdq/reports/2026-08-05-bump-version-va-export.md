# REPORT — Bump 0.7.0 + bộ export Claude Code (`2026-08-05-bump-version-va-export` · lane full · mode main · 26/26 task tick đủ)

Đã làm: P1 bump `0.6.2`→`0.7.0` + CHANGELOG · P2–P4 viết `scripts/claude_export.py` (`build` + `check`, log ISO ra stderr) · P5 viết lại 3 file `claude-export/` · P6 sinh bundle thật + zip · P7 lint/graph/spec §7 · QC-2/QC-3 vá 2 lỗi QC báo rồi sinh lại bundle.
Kết quả: bundle 1709 file / 17 MB (zip 8,5 MB, sinh lại lần cuối lúc 10:27 cho khớp HEAD) · vá cả 8 lỗ hổng đã đo: hết `.DS_Store`/`state.json`/`graphify-out` rác (dùng `git clone` thay `rsync --exclude`), có `.git`, có `config/mcp-servers.json` 2 server, manifest 5→8 khoá (thêm `plugin_version`, `repo_commit`, `exported_at`, `source_files` sha256), `.remember` 10 file đã lọc `tmp/`+`logs/`, 0 file lộ key.
Kiểm: `unittest discover` 567 test 0 fail (trước: 521) · `doc_lint.py claude-export` exit 0 · `check` ngay sau build ra `0 mục lệch` · `unzip -t` No errors · QC độc lập 11/12 PASS theo câu chữ, 12/12 theo phạm vi request, 2 defect đã sửa (manifest hỏng nay exit 2 không traceback; bỏ chuỗi `(+?)`).
Đầu ra: `scripts/claude_export.py` · `tests/test_claude_export.py` (46 ca) · `claude-export/{INSTRUCTIONS.md,README.template.md,MANIFEST.template.json,EXPORT_LOG.md}` · `~/Documents/claude-code-export{,.zip}` · Backup: `~/Documents/claude-code-export.bak-20260805` và `.zip.bak-20260805` (xoá được sau khi user kiểm xong); không sửa gì trong `~/.claude`.
Giới hạn: Q3 FAIL theo đúng câu chữ — `doc_lint.py docs/tdq` exit 1 vì 101 vi phạm R5/R2 nợ cũ của request TRƯỚC, 0 vi phạm thuộc request này; không sửa vì `requests/*.md` chứa nguyên văn lời user · `check` chỉ đo drift phía nguồn, không phát hiện bundle bị rơi file lúc truyền (đã ghi spec §7, cần lệnh `verify` ở request sau) · chưa dựng thử máy đích thật (không có máy thứ hai).
Git: 8 commit, chưa push · 2 commit đầu tự tạo để gỡ chặn (`git clone` chỉ lấy phần đã commit): `61244c6` bộ export 0.7.0, `c4d57c2` vá 2 lỗi QC. Sau đó user yêu cầu commit tiếp: `e88aa25` + `64384b7` tài liệu request, `f757f2b` vá `_cli_approve` (duyệt lại spec sau QC nay ghi được sha256 mới, 569 test xanh), `6e6e154` + `3cce851` mốc export, 2 commit `chore` rebuild graph.
