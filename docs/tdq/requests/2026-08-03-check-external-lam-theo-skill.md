# Request — 2026-08-03-check-external-lam-theo-skill

Nguyên văn: "hãy check xem nếu ko có đưa skill vào thì external có implement theo skill đc như plan ko?"

Cách hiểu đầu tiên:
- Mục tiêu: phân tích + kết luận — khi gói external KHÔNG mang skill vào, engine ngoài (codex/agy) có làm đúng phần việc mà plan giao kèm khối `Dùng: <skill>` không? Nếu không thì hỏng ở mức nào (task sai, verify bắt được không, fallback có cứu không).
- Phạm vi đoán: soi khối `Dùng:` thật trong các plan gần đây, đối chiếu nội dung skill tương ứng với những gì gói external chứa (mục tiêu/file/test), xét từng loại skill (trong repo / plugin / built-in) — thuần phân tích, không sửa.
- Chỗ chưa rõ: không.
