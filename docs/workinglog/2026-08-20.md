# Working log 2026-08-20

## 00:50

Đo tuân thủ: sửa lỗi bộ đo F5 — giám khảo mù bối cảnh nên chấm oan cả hai nhánh (L218 gán cho ca chạy ở phase spec). cham_phien gắn ca vào phiên; L218/L220 trả khong-ap-dung khi phase_dau != idle; L136 trả khong-ap-dung khi prompt là câu duyệt rõ ràng. Sửa thêm: bao-cao gộp nhiều cờ (--dem --phu --chi-phi) không còn nuốt lặng mục sau. Test tests/test_tdq_eval.py: 94 passed, 118 subtests. Vòng chạy 27/60 phiên, 16,76 USD (~0,62 USD/phiên).

## 02:09

Đo tuân thủ: sửa provenance test của audit, tách con số bộ mã đăng ký trước (p=0.5000) khỏi bộ đầy đủ (p=0.6047), ghi lại audit từ 60 bản ghi; thêm F7 ghi số lần chạy lại vào bản ghi + `bao-cao --dem` in chạy lại; loại tdq_eval.py khỏi bản portable (nó đặt CLAUDE_PLUGIN_ROOT nên rewrite làm hỏng); demo chạy lại 1 ca thật (0.51 USD) OK. Test: tests/test_tdq_eval.py 109 xanh; full suite 1167 xanh / 25 đỏ đều là test_skill_router.py đỏ sẵn ở HEAD.

## 02:14

QC vòng 1: 12/13 hạng mục PASS kèm bằng chứng lệnh thật; Q4 FAIL đúng một vế — ca duyet-spec-mo-ho chỉ ghép cặp được 2 mã vì turn đúng luật của nó không ghi gì. Full suite 1167 xanh, 25 đỏ đã đối chiếu worktree HEAD là đỏ sẵn. Dừng hỏi user vì cả hai hướng vá đều đổi phạm vi đã duyệt.
