# Working log 2026-09-06

## 13:53

Mở request them-language-server lane full: brief (kiểm kê repo 0 file 5 ngôn ngữ, đọc tdq_lsp.py, research 4 hướng), vòng interview 7 câu đã chốt, viết spec 1.0 chờ duyệt

## 14:12

Duyệt spec 1.0; viết plan 20 task / 5 phase / ETA ~126 phút, mode đề xuất subagent theo simulate (đội thắng 2.0 phút), chờ duyệt plan

## 14:34

Thi hành plan them-language-server mode main: cài go 1.27.1, gopls 0.23.0, dotnet 10.0.400, csharp-ls 0.27.0, docker-language-server 0.20.1, typescript-language-server 6.0.0 + typescript 5.9.3 (ghim v5 vì v7 tsgo không còn tsserver.js), yaml-language-server 1.24.0; thử khô 10 mục bằng agent-lsp doctor rồi mới ghi 14 mục args vào khoá gốc mcpServers.lsp của ~/.claude.json (có .bak-20260906-142440); thêm khoá dockerfile vào LANG_SERVER+LANG_CONFIG và đổi lệnh cài go sang đường brew trong scripts/tdq_lsp.py, đỏ→xanh 39/39; còn treo csharp vì cần thêm PATH+DOTNET_ROOT trong ~/.zshrc (spec R4 bắt hỏi user)

## 14:42

QC độc lập bằng agent tdq-qc-tester: 8/11 PASS, agent bắt được lỗi tuân thủ luật số 7 (đã ghi mục csharp:csharp-ls chưa qua thử khô vào ~/.claude.json) → đã gỡ mục đó, args còn 13 mục, doctor 9 ok 0 failed, 4 MCP server vẫn Connected; Q2/Q3 chờ user quyết ~/.zshrc, Q9 là nợ suite có sẵn từ HEAD

## 14:42

Thêm P6 vào plan: T6.1 đã gỡ mục csharp chưa qua thử khô (xong), T6.2 chờ user quyết ~/.zshrc, T6.3 ghi nhận nợ suite 290 test là việc của request khác

## 14:47

T6.2 xong sau khi user duyệt 1a: thêm DOTNET_ROOT + PATH vào ~/.zshrc (có .bak), thử khô csharp trong shell đăng nhập thật cho Status ok rồi mới ghi mục csharp vào args → đủ 14 mục, doctor 10 ok 0 failed, 4 MCP server vẫn Connected; DoD lên 10/11, chỉ còn Q9 là nợ suite có sẵn

## 14:52

Viết báo cáo cuối docs/tdq/reports/2026-09-06-1326-them-language-server.md: args 4→14 mục đều ok, QC 10/11 PASS (Q9 là nợ suite có sẵn), hai vòng QC độc lập, còn hạn chế cần restart Claude Code

## 15:16

Kiểm sau restart: args 14 mục đã có hiệu lực (PID 93433) nhưng tiến trình MCP không thừa hưởng DOTNET_ROOT từ ~/.zshrc → csharp-ls chết mã 131 làm mcp__lsp__start_lsp hỏng TOÀN BỘ. Đã thêm khoá env {DOTNET_ROOT} cho server lsp trong ~/.claude.json (có .bak-20260906-151531); đo lại csharp Status ok, 6 MCP server Connected, thang LSP 6/7. Cần restart thêm 1 lần

## 15:30

Kiểm hiệu ứng LSP sau restart lần 2: start_lsp OK; css/yaml/typescript/go/html/json trả lời thật bằng probe tạm (đã dọn); phát hiện csharp-ls thiếu trong tiến trình 94821 (13/14 server), chứng minh do tiến trình chứ không do cấu hình — tiến trình agent-lsp mới với đúng 14 args sinh đủ 14 server và định tuyến .cs đúng. Ghi kết quả vào docs/tdq/qc/2026-09-06-1326-them-language-server.md

## 15:40

Truy nguyên vì sao csharp-ls không lên: tái hiện được crash 'abort trap' của csharp-ls khi list_symbols lên file .cs không thuộc project; agent-lsp không hồi sinh server đã chết nên .cs rơi về clangd. Bác bỏ giả thuyết dotnet khởi động nguội (sentinel 14:21:53). Chứng minh cấu hình+env đúng qua 7 tiến trình agent-lsp mới đều 14/14. Sửa lại mục nguyên nhân trong docs/tdq/qc/2026-09-06-1326-them-language-server.md

## 16:50 — Request `2026-09-06-1619-setup-omnisharp` (lane quick, chore, mode main)

Duyệt: "duyệt quick" + "main a" → thi hành inline, **không mở nhánh mới** (giữ `chore/cai-uv-pyenv-graphify`, cây bẩn 22 file do người dùng tự xử lý).

Plan đã duyệt (9 bước): (1) tải `omnisharp-osx-arm64-net6.0.tar.gz` v1.39.15 → `~/.local/share/omnisharp/`; (2) gỡ quarantine + chmod; (3) symlink `~/.local/bin/omnisharp`, xác nhận cờ LSP; (4) **cổng chặn** `agent-lsp doctor` phải ok trước khi đụng `~/.claude.json`; (5) đo hiệu ứng thật trên project .NET tối thiểu, kể cả case file `.cs` mồ côi; (6) đổi args `csharp:csharp-ls` → `csharp:omnisharp,<cờ>` (backup + ghi atomic); (7) cập nhật `LANG_SERVER["csharp"]` đỏ→xanh; (8) chạy test suite, so baseline; (9) working log + QC.

Cơ sở chọn bản net6.0-arm64: bản này tự chứa .NET 6 nên **không cần mono**; bản 17 MB `omnisharp-osx.tar.gz` mới cần mono và chỉ có x64.

### Thi hành — 9/9 bước xong

| # | Việc | Kết quả |
|---|---|---|
| 1 | Tải `omnisharp-osx-arm64-net6.0.tar.gz` v1.39.15 (44 MB, sha256 `ae9ccca3ef1c…`) → `~/.local/share/omnisharp/` | ✅ |
| 2 | `xattr -dr com.apple.quarantine` + `chmod +x` | ✅ |
| 3 | `ln -sf .../OmniSharp ~/.local/bin/omnisharp`; `--help` xác nhận cờ `-lsp\|--languageserver` | ✅ |
| 4 | **Cổng chặn** `agent-lsp doctor "csharp:omnisharp,-lsp"` | ✅ `Status: ok`, OmniSharp v1.39.15 |
| 5 | Đo hiệu ứng thật (xem dưới) | ✅ |
| 6 | `~/.claude.json`: `csharp:csharp-ls` → `csharp:omnisharp,-lsp` (backup `.bak-20260906-165350`, ghi atomic) | ✅ 14 args, `env.DOTNET_ROOT` giữ nguyên |
| 7 | `LANG_SERVER["csharp"]` trong `scripts/tdq_lsp.py`, đỏ→xanh | ✅ đỏ 2/41 → xanh 41/41 |
| 8 | Suite đầy đủ + so baseline `git archive HEAD` | ✅ **0 fail mới** |
| 9 | Working log + QC | ✅ |

## QC

**Sai một giả định của tôi, đã sửa tại chỗ**: tôi nói bản net6.0 là "tự chứa". **Sai** — nó là **framework-dependent**, chạy trần thì chết `libhostfxr.dylib could not be found`. Nhưng `OmniSharp.runtimeconfig.json` khai `"rollForward": "LatestMajor"`, nên chỉ cần `DOTNET_ROOT` là nó chạy trên .NET 10.0.11 sẵn có — **không cần cài .NET 6, không cần mono**. `env.DOTNET_ROOT` đã có sẵn trong `mcpServers.lsp` từ request trước nên không phải thêm gì.

**Đo hiệu ứng thật** (nói MCP JSON-RPC trực tiếp với tiến trình `agent-lsp` mới):

| Case | Truy vấn | csharp-ls (cũ) | OmniSharp (mới) |
|---|---|---|---|
| Project có `.csproj` | `get_diagnostics` | — | ✅ `CS0029 Cannot implicitly convert type 'string' to 'int'`, Source `csharp` |
| | `list_symbols` | — | ✅ `class Probe` |
| | `find_symbol("Loi")` | — | ✅ `method Loi()` |
| **File `.cs` mồ côi** | `list_symbols` | ❌ **`abort trap`, server chết** | ✅ trả `class Mocoi`, **server còn sống** |

Đây là lý do chính để đổi: bug `abort trap` của csharp-ls làm mọi truy vấn `.cs` sau đó âm thầm rơi về clangd. OmniSharp không dính.

**Lưu ý capabilities**: `doctor` báo OmniSharp chỉ có 1 capability (`workspaceSymbolProvider`) so với 3 của csharp-ls, vì OmniSharp đẩy chẩn đoán theo lối *push* (`publishDiagnostics`) chứ không phải *pull*. Đo thật cho thấy agent-lsp vẫn bắc cầu được — `get_diagnostics` trả lỗi thật. Không phải hồi quy.

**Chưa nghiệm thu được**: máy không có Unity nên không đo được `UnityEngine` có resolve không. Với project Unity, vẫn phải *Regenerate project files* để có `.csproj`.

**Suite**: 1525 test · 294 fail — baseline HEAD là 296 fail; `comm` cho **0 fail mới**. Hai test biến mất là cặp `test_tdq_eval` đã biết là nhiễu thứ tự, không phải cải thiện.

**Còn phải làm**: cấu hình mới chỉ có hiệu lực sau khi **restart Claude Code**. Muốn quay lại csharp-ls thì đổi args về `csharp:csharp-ls` — binary vẫn nằm ở `~/.dotnet/tools`.

## 16:59

Setup OmniSharp làm language server C#: cài v1.39.15 (osx-arm64-net6.0) vào ~/.local/share/omnisharp + symlink ~/.local/bin/omnisharp; doctor ok; đo hiệu ứng thật (CS0029, list_symbols, find_symbol) và chứng minh OmniSharp KHÔNG sập với file .cs mồ côi — case đã giết csharp-ls; đổi args ~/.claude.json sang csharp:omnisharp,-lsp; cập nhật LANG_SERVER['csharp'] đỏ→xanh (41/41); suite 0 fail mới so baseline

### 17:05 — Kiểm sau restart (PID 19398, khởi động 17:01:53)

| Mục | Kết quả |
|---|---|
| args | `csharp:omnisharp,-lsp` ✅ |
| `env.DOTNET_ROOT` | `/opt/homebrew/opt/dotnet/libexec` ✅ |
| `start_lsp` | thành công · **14/14** server con · `omnisharp -lsp` (PID 19831) đang chạy ✅ |
| `list_symbols` trên `.cs` | ✅ trả `class Probe` |
| `get_diagnostics` trên `.cs` | ⚠️ 0 lỗi — xem dưới |

**Vì sao 0 chẩn đoán ở tiến trình live**: tôi dựng `tdq_csprobe/probe.csproj` **sau** khi OmniSharp đã khởi động. OmniSharp quét project lúc khởi động và không nhận project sinh sau — kể cả khi đã bắn `did_change_watched_files` và chờ 25s.

**Chứng minh cấu hình đúng**: chạy tiến trình `agent-lsp` mới với **đúng 14 args của config live**, `root_dir` = cả repo, project có sẵn từ đầu → sau ~15s trả `CS0029|Cannot implicitly convert type 'string' to 'int'`, Source `csharp`. Tức OmniSharp làm việc thật trên repo lớn, chỉ cần csproj tồn tại trước lúc mở phiên.

**Ghi nhận thêm**: `find_symbol` trả 0 kết quả trên repo lớn ngay cả ở ca chạy đúng (workspaceSymbol của OmniSharp cần nạp lâu hơn). `list_symbols` và `get_diagnostics` là hai đường dùng được.

Đã xoá `tdq_csprobe/` sau khi đo.

## 17:04

Kiểm sau restart: PID 19398 mang args csharp:omnisharp,-lsp + DOTNET_ROOT, start_lsp cho 14/14 server con với omnisharp -lsp chạy thật; list_symbols OK; get_diagnostics ở tiến trình live trả 0 vì csproj sinh sau khi OmniSharp khởi động — chứng minh bằng tiến trình mới (root=cả repo, csproj có sẵn) trả CS0029 sau ~15s. Đã xoá thư mục probe.

### 17:10 — Thử đóng nốt khoảng trống: nạp project vào tiến trình live

Mục tiêu: chứng minh `get_diagnostics` chạy được ngay trong tiến trình MCP của phiên này, chứ không chỉ ở tiến trình `agent-lsp` phụ.

Đã thử: dựng lại `tdq_csprobe/` (csproj + Program.cs có lỗi CS0029) → `add_workspace_folder` (workspace nhận 2 folder) → `open_document` → chờ 20s → `get_diagnostics`.

Kết quả: **vẫn 0 chẩn đoán**. Mô tả của chính `add_workspace_folder` nói rõ chỉ gopls, rust-analyzer, typescript-language-server tự nạp lại folder mới; OmniSharp không nằm trong danh sách đó. `restart_lsp_server` cũng không giúp vì trong cấu hình nhiều server nó chỉ khởi động lại server mặc định (clangd).

Kết luận: không có đường nào bắt OmniSharp của tiến trình live nạp một project sinh sau lúc nó khởi động. Muốn nghiệm thu ngay trong phiên thì phải **restart Claude Code khi csproj đã có sẵn trên đĩa**. Giữ nguyên `tdq_csprobe/` để lần restart tới xác nhận được.

## 17:06

Thử nạp project C# vào tiến trình MCP live bằng add_workspace_folder + open_document + chờ 20s: vẫn 0 chẩn đoán, vì OmniSharp không nằm trong nhóm server tự nạp lại folder mới và restart_lsp_server chỉ khởi động lại server mặc định. Giữ tdq_csprobe/ để xác nhận ở lần restart tới.

## 17:16

Đóng request setup-omnisharp: xoá thư mục thử tdq_csprobe/, khép state về idle. Rà soát setup TDQ workflow: tdq_checkstatus report = TIẾP TỤC ĐƯỢC; tdq_lsp check 6/7 bậc đạt (bậc 5 lumen cảnh báo SAI vì MODEL_LUMEN trong tdq_lsp.py:42 vẫn là ordis/jina-embeddings-v2-base-code trong khi ~/.config/lumen/config.yaml đã dùng qwen3-embedding:0.6b — lumen search chạy thật OK); doc_lint 675 file 0 vi phạm; i18n_check 172 dòng tiếng Việt/81 file exit=1 (câu luật thứ tự tìm kiếm trong tdq-spec + tdq-plan thiếu marker i18n-allow mà tdq-intake đã có); portable_codex 145 file + antigravity_portable 87 file CLEAN (hooks.json của antigravity build dưới home khác, cần build_portable.py trước khi cài); plugin cache 0.45.0 dựng từ clone marketplace tại commit 012a85b nên bản vá csharp→omnisharp/go/dockerfile trong scripts/tdq_lsp.py mới chỉ sống ở repo, chưa vào plugin đang chạy; MCP claude.ai Google Drive chưa uỷ quyền.

## 17:28

Mở request `2026-09-06-1721-sua-canh-bao-lumen` (lane nhanh, loại bugfix, giữ nhánh
`chore/cai-uv-pyenv-graphify` theo yêu cầu — cây bẩn 23 file nên không mở nhánh request).
Plan duyệt lúc 17:28 ("duyệt nhanh và giữ nhánh hiện tại"), 3 task chạy inline vì cùng chạm
`scripts/tdq_lsp.py` + `tests/test_tdq_lsp.py`:
T1 `_model_lumen()` đọc model theo thứ tự config.yaml → `$LUMEN_EMBED_MODEL` → mặc định;
T2 `_duong_dan_manifest()` tách `ns/name:tag` đúng chuẩn manifest ollama;
T3 `bac5_lumen()` in tên model động trong thông điệp và lệnh `ollama pull`.
DoD: `tdq_lsp.py check` ra 7/7 bậc ĐẠT · 0 cảnh báo, và `tests/test_tdq_lsp.py` xanh toàn bộ.

## 17:34

Sửa cảnh báo lumen sai ở bậc 5 (lane nhanh, bugfix): thêm _model_lumen() đọc model theo thứ tự ~/.config/lumen/config.yaml → $LUMEN_EMBED_MODEL → MODEL_LUMEN_MAC_DINH, thêm _duong_dan_manifest() tách ns/name:tag đúng chuẩn Ollama (library/qwen3-embedding/0.6b), bậc 5 và lệnh nha dùng tên model động. QC: 7/7 bậc ĐẠT (trước 6/7 + 1 cảnh báo oan), tests/test_tdq_lsp.py 49 test OK, toàn suite 290 failures + 4 errors = đúng nền cũ, không đỏ mới. Vá thêm ngoài plan: lệnh nha trước đây stop nhầm model mặc định nên nhả trượt.
