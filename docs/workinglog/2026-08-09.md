# Working log 2026-08-09

## 00:15 — đóng request giảm over-engineer workflow

Ngữ cảnh: phiên bắt đầu ngày 08, qua nửa đêm mới đóng — phần P4-P8 và vòng fix ghi ở
`docs/workinglog/2026-08-08.md`, mục này ghi phần chạy sau 00:00.

User chọn phương án A: khôi phục state bị đè (init lại request, ghi lại đúng nguyên văn
hai câu duyệt cũ "duyệt spec" và "duyệt plan mode main", set phase=qc) rồi chạy vòng fix 1.

File đổi: `docs/tdq/spec/<slug>.md` và `docs/tdq/plan/<slug>.md` (F1 sửa lệnh DoD sai,
4+9 chỗ; tick F1-F5), `skills/tdq-intake/references/quick-lane.md`,
`skills/tdq-intake/SKILL.md`, `scripts/tdq_state.py`,
`skills/tdq-conventions/references/phases.md` (F2 bỏ luật "vòng fix kể cả khi user bỏ QC",
F3 sửa mâu thuẫn trần vòng fix), `agents/tdq-reviewer.md` (F4 knowledge/requests → brief),
`docs/tdq/qc/<slug>.md` (kết quả vòng fix + sửa bảng số đo),
`docs/tdq/reports/<slug>.md` (mới), `graphify-out/*` (extract lại: 16 file).

Lý do: agent `tdq-qc-tester` chạy độc lập trả FAIL với 5 khiếm khuyết thật, đã đối chiếu
lại từng cái bằng lệnh riêng trước khi sửa.

Test đã chạy: `python3 -m unittest discover -s tests -t tests -q` → 410 test OK; bản
`HOME=/nonexistent` → 410 OK; `python3 scripts/doc_lint.py skills` và 5 file doc → exit 0;
`grep -rn "kể cả khi user" skills/ scripts/` rỗng; đo lại suite liền nhau: 704ac3f
600 test/80,0 giây/1 đỏ so với bản mới 410 test/58,2 giây/0 đỏ. Plan không còn `[ ]` nào.
Phase đã về `idle`. CHƯA commit, CHƯA push — đang chờ user trả lời.

## 00:25 — đồng bộ CLAUDE.md, bump 0.10.0, commit

Tầng `nhỏ` (không mở request): sửa hằng số và file cấu hình, không đổi hành vi.

File đổi: `~/.claude/CLAUDE.md` bỏ mode `external` ở §7 và đổi dòng "Chi tiết lane, mode
external, deep search, QC" thành "Chi tiết lane, tầng `nhỏ`, QC" — nay §7 khớp từng ký tự
với `docs/claude-md-mau.md`. `.claude-plugin/plugin.json` 0.9.0 → 0.10.0; `CHANGELOG.md`
thêm mục 0.10.0 có phần "Phá vỡ tương thích" (xoá mode external, deep search, `portable/`).

Lý do: user yêu cầu đồng bộ CLAUDE.md, bump version và commit.

Test đã chạy: `python3 -m unittest discover -s tests -t tests -q` → 410 test OK;
`test_docs_consistency.py` → 3 test OK (manifest và changelog khớp 0.10.0);
`doc_lint CHANGELOG.md` exit 0; `diff` §7 hai file CLAUDE.md → khớp.

Commit `3d4912d` trên `main`, 35 file, CHƯA push. Cố ý để ngoài commit: `claude-export/`,
`scripts/claude_export.py`, `tests/test_claude_export.py`, `.codex/`, `docs/tdq/external/`,
doc `2026-08-05-*`, `docs/workinglog/2026-08-05.md` — 26 mục dirty khác phạm vi.

## 00:52

Mở request 2026-08-09-cat-token-thua-workflow (lane full, phase analyze). Viết brief: 6 điểm nghẽn C1-C6, kiểm kê năng lực, lộ trình, xác minh doc_lint R8/pair không chặn C1-C2. Chưa sửa code, đang chờ trả lời 4 câu interview.

## 00:56

Chốt 4 câu interview (1A 2A 3A 4A), viết spec 2026-08-09-cat-token-thua-workflow: 9 đầu ra, 11 hạng mục DoD. Lint spec exit 0. Chờ user duyệt spec.

## 00:58

Ghi nhận duyệt spec; viết plan 2026-08-09-cat-token-thua-workflow: 5 phase, 15 task, 11 dòng DoD. doc_lint và --pair exit 0. Chờ user duyệt plan kèm mode.

## 01:13

QC 11/11 PASS + report cho request cat-token-thua-workflow

## 01:28 — Bump 0.11.0 và commit

Ngữ cảnh: user duyệt commit sau khi xem ước tính tiết kiệm token/time.
File đổi: `.claude-plugin/plugin.json` (0.10.0 → 0.11.0), `CHANGELOG.md` (mục 0.11.0 kèm
"Phá vỡ tương thích": plan cũ còn `- Nạp:`, script parse output `phases-doc`).
Lý do tách 2 commit: `1135d98` chỉ chứa việc cắt token của request này; `b410113` gom tài
liệu/artefact còn sót từ các request 2026-08-05 và 2026-08-07 để không lẫn phạm vi.
Test đã chạy: 412 test unittest xanh, `doc_lint.py` exit 0, QC 11/11 PASS (trước commit).
Chưa push.

## 11:23 — Mở request sửa mô tả skill trong kiểm kê năng lực

Ngữ cảnh: user hỏi bộ TDQ có "skill router" quét skill để phân bổ vào spec/plan không.
Rà thật: cơ chế tên là "Kiểm kê năng lực (B0)" — `scripts/skill_inventory.py` → brief
`### Năng lực dùng được` → spec §3b → khối hợp đồng 5 trường trong plan → QC chạy trường
`Kiểm` → `doc_lint.py` R8 và `--pair` cưỡng chế. Đo trên 268 SKILL.md tại máy user:
description trung vị 358 ký tự nên `DESC_MAX = 60` chỉ giữ 17%; 146/211 skill có cụm
trigger nằm sau ký tự thứ 60 (mất tín hiệu định tuyến ở ~55% skill); parser frontmatter
không đọc được YAML block scalar `description: |` làm 18 skill rỗng mô tả (10 firecrawl,
7 tavily, `mongodb-search-and-ai`). Cũng phát hiện `plugin-routing.md` ghi plugin Airflow
là `data-engineering` trong khi prefix gọi Skill là `astronomer-data:`.
So 3 phương án: A vá parser + cắt có nhận biết trigger (~25 dòng, không state) · B subagent
scan + cache doc (chất lượng cao nhất/token nhưng thêm cache phải invalidate, ngược hướng
simplify của 0.10.0/0.11.0) · C không làm gì. User chốt A.
File đổi: `docs/tdq/brief/2026-08-09-sua-mo-ta-skill-inventory.md` (mới, mục `## Nguyên văn`).
Chưa `init` state — đang chờ user chốt lane (đề xuất quick).
Test đã chạy: chưa có thay đổi mã nguồn nên không chạy test; các phép đo dùng script tạm
trong scratchpad, không ghi vào repo.

## 11:26 — Mini-plan quick cho sua-mo-ta-skill-inventory

Ngữ cảnh: user chốt lane quick. `init` ghi đè request cũ `cat-token-thua-workflow`
(phase idle, không mất gì).
File đổi: `docs/tdq/plan/2026-08-09-sua-mo-ta-skill-inventory.md` (mới, 5 task / 7 DoD),
`docs/tdq/state.json` (qua `tdq_state.py init`).
Lý do: gộp spec+plan 1 file vì phạm vi đã hết mơ hồ từ phần phân tích, chỉ đụng
`scripts/skill_inventory.py` + `tests/test_skill_inventory.py`.
Số nền đo trước khi sửa (dùng làm mốc DoD): output 270 dòng · 18 dòng skill sai số cột và
mất description · 44 dòng còn giữ cụm trigger.
Test đã chạy: `doc_lint.py` trên file plan → exit 0. Chưa sửa mã nguồn, chờ user duyệt.

## 11:35 — Build quick: vá parser + rút gọn có nhận biết trigger

Ngữ cảnh: user duyệt quick lúc 11:29. Thực thi T1–T5 của mini-plan
`docs/tdq/plan/2026-08-09-sua-mo-ta-skill-inventory.md`.
File đổi: `scripts/skill_inventory.py` — `_frontmatter` đọc hết khối frontmatter (trần 80
dòng) và hiểu YAML block scalar `|`/`>` cùng dòng thụt vào; thêm `_condense()` giữ 60 ký tự
đầu rồi nối ` … ` + 50 ký tự từ chỗ cụm trigger nếu trigger nằm ngoài ngưỡng; `|` trong mô
tả đổi thành `/`. `tests/test_skill_inventory.py` — thêm lớp `DescriptionTest`, 5 test.
Lý do trần 80 dòng: frontmatter dài nhất trên máy vẫn dưới ngưỡng, giữ cận trên để SKILL.md
hỏng không kéo cả file vào bộ nhớ.
Kết quả đo: 18 → 0 dòng mất mô tả · 18 → 0 dòng sai số cột · 44 → 195 dòng giữ được cụm
trigger · tổng vẫn 270 dòng · ô mô tả dài nhất 113 ký tự · output 24.639 → 36.231 ký tự
(~6.2k → ~9.1k token, tăng ~2.9k mỗi lần kiểm kê).
Test đã chạy: `python3 -m unittest discover -s tests -q` → Ran 417 tests OK;
`doc_lint.py` trên plan → exit 0; QC 7/7 PASS vòng 1, ghi ở mục `## QC` của plan.
Ngoài phạm vi: xoá 7 file `.DS_Store` do Finder sinh, chúng làm
`tests/test_docs_consistency.py` đỏ; không phải lỗi của request này.
Chưa commit.

## 11:36 — Validate lại sau restart Claude Code

Ngữ cảnh: user restart Claude Code, yêu cầu chạy lại kiểm chứng.
File đổi: `docs/tdq/plan/2026-08-09-sua-mo-ta-skill-inventory.md` (thêm mục
`## QC vòng 2`). Không đụng mã nguồn.
Test đã chạy: 417 test OK · 4 mốc DoD trùng vòng 1 (0 sai cột · 0 ô rỗng · 195 trigger ·
270 dòng) · stderr 0 dòng · `doc_lint.py docs/` exit 1 nhưng 25/25 lỗi nằm trong
`docs/archive/v0.1/` (di sản, có từ trước) · diff tên skill bản cũ (git stash) vs bản mới:
giống hệt · chạy 2 lần cho output byte-identical.
Thêm kiểm biên 5 ca SKILL.md giả: thiếu frontmatter, frontmatter không đóng, folded `>-`,
mô tả bọc nháy kép, `description:` sau dòng 80. Ca cuối cho ô rỗng — đúng thiết kế trần
`FRONTMATTER_MAX_LINES`, đã ghi thành giới hạn đã biết trong plan.
Chưa commit.

## 11:38 — Đo A/B bản cũ vs bản mới của skill_inventory

Ngữ cảnh: user yêu cầu định lượng mức cải thiện.
Cách đo: `git stash` bản cũ, chạy song song, đối chiếu với description đầy đủ đọc bằng
parser độc lập (`scratchpad/ab.py`) để phép đo không tự chấm điểm mình.
Kết quả: giữ cụm trigger 24,6% → 99,5% (+303,8%) · ô mô tả vô nghĩa 56 → 0 · dòng sai cột
18 → 0 · % ký tự gốc giữ lại 18,1% → 28,5% · chi phí 6.199 → 9.192 token (+48,3%) ·
hiệu quả 8,4 → 22,8 skill-có-trigger trên mỗi 1k token (+171%).
Đính chính: con số "18 ô mất mô tả" báo lúc 11:35 là thiếu — đếm đủ cả dấu `>` (17) và
`>-` (21) thì bản cũ có 56 ô vô nghĩa.
Còn 1 khiếm khuyết 0,5%: `huggingface-trackio` có `Use when` ở ký tự 58, vắt qua ngưỡng 60
nên không bắt được; sửa bằng cách tìm từ `DESC_MAX - 15`, chưa làm vì ngoài phạm vi plan.
File đổi: plan (mục `## Đo A/B`), working log. Không đụng mã nguồn.

## 11:45 — Fix ca trigger vắt ngưỡng, đóng nốt 0,5% còn lại

Ngữ cảnh: user yêu cầu fix khiếm khuyết 1/211 phát hiện lúc đo A/B.
File đổi: `scripts/skill_inventory.py` — thêm `TRIGGER_LOOKBACK = 15`, dò trigger từ
`DESC_MAX - TRIGGER_LOOKBACK`; khi trigger vắt ngưỡng thì cắt phần đầu ngay trước nó để
không lặp cụm hai bên dấu nối. `tests/test_skill_inventory.py` — thêm
`test_trigger_straddling_cutoff_kept` (2 assert).
Kết quả: giữ cụm trigger 210/211 → **211/211 = 100%** · ô lặp cụm 24 → 11 (11 ca còn lại
là thật: description gốc có hai cụm khác nhau) · ô dài nhất vẫn 113 · sai cột 0 · ô rỗng 0
· stderr 0 · chạy 2 lần byte-identical.
Hiệu quả chốt so bản cũ: trigger 24,6% → 100% (+305,8%) · ô vô nghĩa 56 → 0 · ký tự gốc
giữ lại 18,1% → 29,0% · chi phí 6.199 → 9.264 token (+49,4%) · 8,4 → 22,8 skill-có-trigger
trên mỗi 1k token (+171%).
Test đã chạy: `python3 -m unittest discover -s tests -q` → Ran 418 tests OK; `doc_lint.py`
trên plan exit 0.
Chưa commit.

## 11:53 — Bump 0.11.1 và commit

Ngữ cảnh: user yêu cầu bump version + commit sau khi fix xong bảng kiểm kê năng lực.
File đổi: `.claude-plugin/plugin.json` (0.11.0 → 0.11.1), `CHANGELOG.md` (mục 0.11.1:
parser block scalar, rút gọn nhận biết trigger, `|` → `/`, kèm số đo chi phí).
Chọn patch chứ không minor: chỉ sửa lỗi trong một script, không đổi khuôn bảng, gate duyệt
hay hợp đồng lint.
Test đã chạy: `python3 -m unittest discover -s tests -q` → Ran 418 tests OK;
`doc_lint.py CHANGELOG.md` exit 0.
Commit: 1 commit trên `main`, gồm cả brief/plan/working log và artefact graphify.
