# Working log — 2026-07-27

## ~16:30 — Lập spec cho TDQWorkflow plugin
- **Ngữ cảnh**: Phân tích `idea.md`, research (docs Claude Code plugin/hooks v2.1+, Superpowers, BMAD, Spec Kit, Graphify), interview người dùng (4 câu: lane kích hoạt, mức enforcement, ngôn ngữ, Graphify; + 1 câu: Claude tự thực thi).
- **File thay đổi**: tạo `docs/spec/tdq-workflow-plugin.md` (spec v0.1, trạng thái CHỜ DUYỆT); tạo file working log này.
- **Lý do**: theo quy trình — spec trước, chờ duyệt rồi mới lập plan.
- **Kiểm tra**: chưa chạy test (chỉ tạo doc, chưa có code).

## ~16:50 — Đổi đường dẫn working log theo yêu cầu người dùng
- **Ngữ cảnh**: người dùng yêu cầu working log lưu trong `doc/workinglog`; interview chốt: giữ root `docs/`, log về `docs/workinglog/`.
- **File thay đổi**: sửa `docs/spec/tdq-workflow-plugin.md` (v0.1 → v0.1.1: cây doc mục 7, deliverable mục 10); di chuyển `docs/superpowers/workinglog/2026-07-27.md` → `docs/workinglog/2026-07-27.md`, xóa thư mục `docs/superpowers/` rỗng.
- **Kiểm tra**: `ls -R docs` xác nhận cấu trúc mới; chưa có test code để chạy.

## ~17:10 — Bổ sung quy tắc khai thác Tavily vào spec
- **Ngữ cảnh**: người dùng yêu cầu research cách tận dụng sức mạnh Tavily và bổ sung vào spec. Đã load schema 5 tool MCP (search/extract/crawl/map/research) + search docs chính thức Tavily (Agents guide, Best Practices Search/Extract/Crawl/Research, skill tavily-best-practices chính chủ).
- **File thay đổi**: `docs/spec/tdq-workflow-plugin.md` v0.1.2 — thêm mục 9 "Quy tắc khai thác Tavily" (ma trận chọn tool, tối ưu search, 4 pattern chuẩn, failover), renumber mục cũ 9→10, 10→11, 11→12; mục 5.1 trỏ về mục 9.
- **Kiểm tra**: đối chiếu tham số với schema tool thực tế trên server tavily-primary (chỉ ghi tham số tồn tại thật); chưa có test code để chạy.

## ~17:25 — Check lazy load, bổ sung mục 3.1 vào spec
- **Ngữ cảnh**: người dùng hỏi plugin đã lazy load để save token chưa. Kết luận: cơ chế nền Claude Code lazy sẵn (skill chỉ nạp metadata, hook 0 token khi im lặng, agent nạp khi spawn) nhưng spec chưa ràng buộc; 3 điểm always-on (metadata skills/agents, SessionStart, UserPromptSubmit) chưa có budget.
- **File thay đổi**: `docs/spec/tdq-workflow-plugin.md` v0.1.3 — thêm mục 3.1 (bảng rule lazy load: description ≤ 2 dòng, body ≤ 500 dòng + references/ progressive disclosure, hook im lặng mặc định, không preload conventions, budget idle < ~800 token) + mục 10 thêm test #4 đo token budget.
- **Kiểm tra**: chưa có test code để chạy (sẽ đo ở bước QC theo mục 10.4).

## ~17:35 — Approve gate luôn hướng dẫn user lệnh duyệt
- **Ngữ cảnh**: người dùng yêu cầu gate approve phải hướng dẫn user nhập gì để duyệt.
- **File thay đổi**: `docs/spec/tdq-workflow-plugin.md` v0.1.4 — mục 4 thêm UX duyệt (khối hướng dẫn "➤ Để duyệt: gõ /tdq-workflow:tdq-approve spec|plan · Để chỉnh sửa: nhắn góp ý" + argument-hint và xử lý gõ sai arg); mục 5.2/5.3 bắt buộc kèm hướng dẫn duyệt cuối message trình spec/plan; hook #2 inject kèm lệnh duyệt khi đang chờ; hook #4 deny reason kèm hướng dẫn duyệt.
- **Kiểm tra**: chưa có test code (case này sẽ nằm trong smoke test e2e mục 10.3).

## ~17:50 — Approve validate bằng state + detail file; vá lỗ hổng state.json
- **Ngữ cảnh**: người dùng yêu cầu (1) khi duyệt phải check bằng state có status approved + detail file spec/plan; (2) check khi đang block write/edit thì Claude còn viết được spec/plan/doc không. Khi rà (2) phát hiện lỗ hổng: `state.json` nằm trong `docs/**` → model có thể Write đè để tự duyệt.
- **File thay đổi**: `docs/spec/tdq-workflow-plugin.md` v0.1.5 — mục 4 mở rộng schema state (`spec_file`/`plan_file`, `*_sha256`, `*_approved_at`) + quy tắc: skill đăng ký file trước khi mời duyệt, `approve_gate.py` validate (đúng thứ tự spec→plan, file tồn tại không rỗng) rồi mới set approved kèm detail, fail → block expansion báo lỗi VI, state không đổi; spec đổi sau duyệt (sha lệch) → cảnh báo trình duyệt lại, plan vẫn cho tick/append theo QC loop. Hook #4 thêm ngoại lệ deny Edit/Write trực tiếp `state.json`; hook #5 (đổi tên `git_naming_gate.py` → `bash_gate.py`) chặn thêm Bash ghi state.json; mục 10 thêm fixture red/green cho approve + state.json; mục 12 cập nhật risk.
- **Kiểm tra**: chưa có test code (fixture đã định nghĩa ở mục 10.2, sẽ chạy ở bước QC).

## ~18:05 — Lane quick có cửa duyệt nhẹ + ghi summary plan vào working log trước implement
- **Ngữ cảnh**: người dùng yêu cầu lane quick vẫn bắt buộc: phân tích → trình summary plan (việc sẽ làm + quick validate/test) ngay trong chat, ngắn gọn nhất có thể → chờ duyệt; khi duyệt thì ghi summary plan vào working log trước rồi mới implement.
- **File thay đổi**: `docs/spec/tdq-workflow-plugin.md` v0.1.6 — mục 4: state thêm `quick_approved`/`quick_approved_at`, approve_gate nhận `quick` (đòi lane quick + request đang mở), sau duyệt quick phải append summary plan vào working log trước khi sửa file ngoài `docs/**` (enforce bằng mtime log > `quick_approved_at`), `argument-hint: [spec|plan|quick]`; mục 5.0: mô tả flow quick mới (plan ≤ 10 dòng trong chat: việc sẽ làm, file đụng tới, cách quick validate/test, hướng dẫn duyệt); mục 3.1 + hook #2: UserPromptSubmit inject cả khi quick đang chờ duyệt; hook #3/#4: thêm nhánh validate + deny cho quick (chưa duyệt → chặn code; duyệt rồi mà chưa ghi log → chặn nhắc ghi log); mục 10: thêm fixture red/green lane quick + smoke test chạy cả 2 lane; mục 12: ghi nhận giới hạn mtime check.
- **Kiểm tra**: chưa có test code (fixture đã định nghĩa ở mục 10.2, sẽ chạy ở bước QC).

## ~18:20 — User duyệt spec v0.1.6; lập plan
- **Ngữ cảnh**: user nhắn "duyệt spec" → spec chốt v0.1.6.
- **File thay đổi**: `docs/spec/tdq-workflow-plugin.md` (trạng thái → ĐÃ DUYỆT); tạo `docs/plan/tdq-workflow-plugin.md` (plan CHỜ DUYỆT: 5 phase A–E, 19 task có checkbox + test/validate từng task, DoD theo spec mục 10; tự review phát hiện lỡ tick sẵn [x] → đã sửa về [ ]).
- **Kiểm tra**: chưa chạy test (chưa implement; test chạy khi thực thi plan).

## ~19:00 — Thực thi plan end-to-end: hoàn thành plugin v0.1.0 (19/19 task)
- **Ngữ cảnh**: user nhắn "duyệt plan" (+ interrupt bổ sung rule tick ngay khi task xong) → thực thi toàn bộ plan trong 1 turn, tick `[x]` ngay sau khi test từng task pass.
- **File thay đổi**: tests/ (test_state, test_approve_gate, test_edit_gate, test_bash_gate, test_context_hooks, test_stop_gate, test_e2e_chain + 7 fixtures); skills/ 10 skill (tdq-conventions + references/tavily.md, tdq-start, tdq-analyze, tdq-spec, tdq-plan, tdq-implement, tdq-qc, tdq-report, tdq-approve, tdq-status); agents/ 3 agent; README.md; docs/notes/user-level-install.md; docs/qc/{skill-budget,smoke-e2e,token-budget}.md; docs/reports/tdq-workflow-plugin.md; docs/plan/tdq-workflow-plugin.md (tick 19 task + trạng thái HOÀN THÀNH).
- **Kiểm tra**: `python3 -m unittest discover tests` → Ran 49 tests OK; `claude plugin validate . --strict` → PASS (sau mỗi phase C/D); smoke headless `claude -p --plugin-dir` 2 lần EXIT=0 (skill tdq-status trả đúng output VI); budget idle ≈ 520 tokens < 800; report 27/50 dòng.

## ~19:10 — Commit v0.1.0 vào main
- **Ngữ cảnh**: user duyệt commit sau khi plugin hoàn thành (49 tests OK, validate strict PASS).
- **Thay đổi**: commit toàn bộ repo lên branch `main` — message `feat: tdq-workflow plugin v0.1.0 — 10 skills, 6 hooks, 3 agents, 49 tests`.
- **Kiểm tra**: `git status` sạch sau commit, `git log` xác nhận 1 commit đầu tiên.
