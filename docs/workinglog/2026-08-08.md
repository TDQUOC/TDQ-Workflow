# Working log 2026-08-08

## 15:36 — đóng sổ request 2026-08-07-siet-qc-lane-quick (commit 704ac3f)

Ngữ cảnh: user chốt option A "commit, không push" cho thành quả lane-quick QC.

File đổi trong turn này: `docs/workinglog/2026-08-08.md` (file này). Không sửa code —
turn này chỉ commit thành quả đã QC xong từ 2026-08-07 và set `phase=idle`.

Commit `704ac3f` gồm 23 file thuộc ĐÚNG request này: `.claude-plugin/plugin.json` (0.9.0),
`CHANGELOG.md`, `scripts/tdq_state.py`, `hooks/scripts/_common.py`, `tests/test_quick_qc.py`,
5 nguồn sự thật (2 bản `quick-lane.md`, `tdq-intake/SKILL.md`, `portable/workflow/01-intake.md`,
2 bản `phases.md`), 7 doc `docs/tdq/*/2026-08-07-siet-qc-lane-quick.md`,
`docs/workinglog/2026-08-07.md`, `docs/tdq/STATE.md`, `graphify-out/*`.

Lý do stage thủ công thay vì `git add -A`: cây làm việc còn thay đổi của 7 request cũ
(`claude-export/`, `scripts/claude_export.py`, `plugin-routing.md`, `.codex/`, docs
`2026-08-05-*`) — gộp vào commit này là lẫn phạm vi. Những file đó vẫn dirty, chờ user
quyết. `claude-export/EXPORT_LOG.md` mà hook nhắc thuộc nhóm này, KHÔNG do turn này sửa.

Test đã chạy: không chạy lại test trong turn này. Bằng chứng QC lấy từ phiên 2026-08-07
(`docs/tdq/qc/2026-08-07-siet-qc-lane-quick.md`): `test_quick_qc` 15/15 OK, suite 618 test
với đúng 1 failure có sẵn ngoài phạm vi, `doc_lint` exit 0, 2 bản `phases.md` khớp
`phases-doc`. Sau commit: `phase=idle`, KHÔNG push.

Ghi chú: cảnh báo `[TDQ:APPROVE]` spec sha256 lệch là artifact — spec mtime 17:21 cùng
phút với lúc ghi duyệt và không có lần ghi nào sau đó, nội dung không đổi sau khi user
duyệt. Không tự ghi lại hash vì đó là tự xác nhận duyệt.

## 21:53

Mở request 2026-08-08-giam-over-engineer-workflow (intake Phần A): ghi docs/tdq/requests/2026-08-08-giam-over-engineer-workflow.md từ kết quả 2 turn phân tích read-only sản phẩm TDQ 0.9.0 — 6 nhóm vấn đề đo được (context 12.022 tok/vòng, 168 mệnh lệnh trong skills/, mâu thuẫn skills vs portable về luật (mcp), output 37,6 KB/request, 156 test cho 746 dòng hook + 72 test chỉ assert chuỗi .md, lane quick hết nhẹ) + 5 chỗ chưa rõ cần interview. Đã trình đề xuất lane full và DỪNG chờ user chốt lane; chưa chạy init nên state vẫn idle. Kiểm: chưa chạy test (turn chỉ tạo 1 file .md request, không đụng code).

## 21:59

Lane full đã chốt (user trả lời A). Chạy init + set phase=analyze. Viết docs/tdq/knowledge/2026-08-08-giam-over-engineer-workflow.md (bảng năng lực, 7 vùng số liệu đo được, 6 quyết định đã chốt, 4 phương án đã loại, lộ trình, kiểm cổng) và docs/tdq/questions/2026-08-08-giam-over-engineer-workflow.md. Bỏ research web vì việc thuần nội bộ. Phát hiện thêm khi chạy: doc_lint lint_file dòng 333 chỉ miễn R1-R7 cho thư mục spec/, 7 thư mục output còn lại vẫn chịu đủ luật văn phong — đã sửa request thêm làm vấn đề #6. Kiểm: doc_lint 3 file exit 0. DỪNG chờ user trả lời 5 câu interview.

## 22:13

Ghi trả lời interview vòng 1, chốt knowledge, viết spec 1.0 giảm over-engineer/over-test (8 đầu ra D1-D8, 10 hạng mục QC). Kiểm: doc_lint spec exit 0.

## 22:19

Duyệt spec 1.0; viết plan: 8 phase, 30 task, mode đề xuất main. Kiểm: doc_lint --pair spec/plan exit 0.

## 22:40

P2+P3: xoá nhánh external (20 file: external_task/external_models/search_task/4 agent runner/3 reference/5 test) và xoá portable/ (18 file) + tests/test_portable_sync.py; portable/claude-md/CLAUDE.md → docs/claude-md-mau.md (bỏ external, bỏ deep search); dọn VALID_MODES còn main|subagent ở tdq_state/hook/skill; sinh lại phases.md bằng --plugin-root; viết lại 23 test theo bề mặt mới, test_claude_md_core hermetic. Kiểm: python3 -m unittest discover -q → 440 test OK; doc_lint skills exit 0

## 22:41

Nới miễn trừ doc_lint sang docs/workinglog/ (cùng loại biên bản như docs/tdq/) + 1 test.

## 22:42

Nới miễn trừ R1-R7 của doc_lint sang docs/workinglog/ và graphify-out/ (biên bản + file máy sinh), gom vào hằng OUTPUT_DIRS; thêm 1 test.

## 23:05

P4-P7 (gộp 1 turn): D2 QC bám DoD — số hạng mục QC = số dòng DoD, cộng đúng 1 hạng mục
chạy full-suite; vòng fix chỉ chạy lại hạng mục FAIL + hạng mục bản fix có thể làm hỏng,
trần 3 vòng (sửa `scripts/tdq_state.py` PHASE_TABLE, `tdq-build/SKILL.md`,
`tdq-build/references/qc.md`, `tdq-plan/SKILL.md`, `tdq-intake/SKILL.md`,
`tdq-intake/references/quick-lane.md`, sinh lại `phases.md` bằng `--plugin-root`).
D5 gộp brief: `docs/tdq/brief/<slug>.md` thay requests+knowledge+questions — sửa
PHASE_TABLE, `edit_gate.py`, 4 reference của tdq-intake, tdq-spec, README.
D3 tầng `nhỏ`: mục mới đầu `tdq-intake/SKILL.md` (4 điều kiện + luật thoát bắt buộc);
`lane-decision.md` đổi thành "Chọn cỡ request: nhỏ, quick hay full".
D6 gọn skill: `tdq-conventions/SKILL.md` 7.345 → 5.912 byte, §10 tách ra
`references/context-budget.md`.
D8 cắt test: xoá `tests/test_skill_docs.py`, `tests/test_agent_digest_sync.py` (git rm),
viết lại `tests/test_docs_consistency.py` còn 3 phép kiểm toàn vẹn repo thật, bỏ 3 test
trùng trong `tests/test_context_hooks.py`.

Lệch plan (khai báo): kéo T7.1 lên sớm vì P6 cắt §10 làm 5 test wording của
`test_skill_docs.py` đỏ, mà P7 vốn xoá file đó. T7.1 giữ lại 3 phép kiểm thật trong
`test_docs_consistency.py` thay vì xoá cả file. T7.3 chỉ cắt 3 test trùng đã đối chiếu,
không cắt cho đủ số.

Kiểm: `python3 -m unittest discover -s tests -t tests -q` → 410 test OK (30s), bản
`HOME=/nonexistent` → 410 OK; `python3 scripts/doc_lint.py skills` exit 0;
`grep -rn "3 hạng mục" skills/` rỗng. Viết `docs/tdq/qc/<slug>.md`: Q1-Q10 PASS, có bảng
số đo trước/sau (skill lõi 51.070 → 48.278 byte, toàn `skills/` 102.166 → 81.467 byte,
file output/request 7 → 5, suite 618 test/55s → 410 test/30s). Ghi 1 fact mem0
(project TDQWorkflow) về 4 quyết định kiến trúc; đã search lại xác nhận có.

## 23:18

T8.3: agent `tdq-qc-tester` chạy độc lập xong (59 lệnh, 887 giây) — trả **FAIL**, 9/10
PASS, Q10 FAIL vì report chưa viết. Đã đối chiếu lại từng khiếm khuyết bằng lệnh riêng và
xác nhận 5 cái đúng: lệnh DoD `unittest discover -q` ở gốc repo trả exit 5 NO TESTS RAN
(ghi sai trong spec+plan); D2 làm thiếu (luật "vòng fix bắt buộc kể cả khi user tắt QC"
vẫn còn ở quick-lane.md, tdq-intake/SKILL.md, PHASE_TABLE); mâu thuẫn trần vòng fix
quick-lane.md:15 vs qc.md:58; agents/tdq-reviewer.md còn khuôn knowledge/requests; số đo
suite cũ sai (đo lại tại 704ac3f: 600 test/49,8s/1 đỏ, không phải 618/55s).

Đổi file: `docs/tdq/qc/<slug>.md` thêm mục "Vòng 2 — agent chạy độc lập" + sửa bảng số đo;
`docs/tdq/plan/<slug>.md` tick T8.3 và thêm mục "QC vòng 1 — fix" (F1-F5).

Cũng phát hiện: lúc 22:59 lệnh test hook Q9 của tôi chạy `tdq_state.py init
2026-08-08-hook-check full` THIẾU `TDQ_PROJECT_DIR` nên đè state thật — active_request
sang `2026-08-08-hook-check`, phase về `idle`, mất dấu duyệt spec/plan. Doc/code/test/commit
không ảnh hưởng. Chưa tự khôi phục vì dấu duyệt là của user; đã hỏi user cách xử lý.
Vòng fix F1-F5 chưa chạy vì phase đang `idle` (không được sửa code khi chưa có request mở).

Kiểm: `doc_lint` trên qc + plan exit 0. Turn này chỉ sửa doc, không đụng code nên không
chạy lại suite.

## 00:15 (2026-08-09)

User chọn A: khôi phục state (init + approve spec/plan đúng nguyên văn cũ + set phase=qc)
rồi chạy vòng fix 1. F1 sửa lệnh DoD sai ở spec (4 chỗ) và plan (9 chỗ); F2 bỏ luật "vòng
fix kể cả khi user bỏ QC" ở quick-lane.md, tdq-intake/SKILL.md, PHASE_TABLE và dòng _info
của `approve quick --no-qc`, sinh lại phases.md; F3 sửa mâu thuẫn trần vòng fix
(quick-lane.md dòng 15); F4 agents/tdq-reviewer.md đổi knowledge/requests thành brief;
F5 chạy lại hạng mục FAIL + hạng mục bị ảnh hưởng. Viết docs/tdq/reports/<slug>.md.

Kiểm: `python3 -m unittest discover -s tests -t tests -q` → 410 test OK; bản
`HOME=/nonexistent` → 410 OK; `doc_lint` skills + spec + plan + qc + report exit 0;
`grep -rn "kể cả khi user" skills/ scripts/` rỗng. Đo lại suite liền nhau cùng điều kiện:
bản 704ac3f 600 test/80,0 giây/1 đỏ so với bản mới 410 test/58,2 giây/0 đỏ.
