# Working log 2026-08-26

## 00:16

Mở request quick: test ranking khả năng find LSP/lumen/grep — brief + mini-plan chờ duyệt

## 00:23

Test thực nghiệm LSP/lumen/grep trên 3 loại truy vấn (symbol chính xác, khái niệm mơ hồ, ai gọi hàm) — ranking xong, QC PASS

## 00:39

Mở request quick: thử tạm đổi model lumen sang qwen3-embedding:0.6b, test lại — brief + mini-plan chờ duyệt

## 00:43

T1 backup config lumen xong (PASS); T2 đổi model sang qwen3-embedding:0.6b, đang reindex 1152 file (chạy nền, đợi Monitor báo)

## 01:00

Đổi tạm model lumen sang qwen3-embedding:0.6b, reindex, test lại T2 — cải thiện rõ (0/5 → 3/5 trúng code thật), QC PASS 3/3

## 01:03

Mở brief: đổi rule ưu tiên tìm kiếm LSP/lumen + index sẵn — chờ user trả lời interview trước khi chọn lane

## 01:11

Viết mini-plan đổi rule LSP+lumen song song, phát hiện lumen tự incremental-reindex nên bỏ bước reindex script; chờ duyệt

## 01:23

Đổi rule ưu tiên tìm kiếm: LSP+lumen gọi song song mọi câu hỏi tìm code, gộp kết quả, grep cuối; đồng bộ 5 chỗ móc; pytest test_tdq_lsp_skill.py PASS

## 01:27

Bump 0.34.0: rebuild hai bản portable theo version mới, chạy pytest xác nhận không có hồi quy (61 fail cũ trong test_skill_router.py là drift môi trường có sẵn từ trước, không do bản đổi này)
