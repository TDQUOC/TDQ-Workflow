# Working Log — 2026-08-04

## 12:17 — Mở request export Claude Code setup
- Ngữ cảnh: user yêu cầu recheck toàn bộ Claude Code hiện tại + lên spec tạo
  bản export đầy đủ (manifest, dependency, README) để setup máy khác chạy y
  hệt máy này.
- File đổi: `docs/tdq/requests/2026-08-04-export-claude-setup.md` (ghi nguyên
  văn yêu cầu + cách hiểu đầu tiên, các điểm chưa rõ cần hỏi).
- Lý do: mở request TDQ intake (phase `no_state`), chuẩn bị hỏi lane trước khi
  phân tích tiếp.
- Test: không áp dụng (turn chỉ ghi tài liệu request, chưa code).

## 12:27 — Phase analyze hoàn tất (lane full)
- Ngữ cảnh: user chọn lane full. Đã kiểm kê năng lực (bảng skill), khảo sát toàn bộ cấu
  hình Claude Code máy nguồn (Explore agent: global `~/.claude/`, `~/.claude.json`, project
  `.claude/` TDQWorkflow, CLI dependency), research 4 truy vấn qua tavily-primary (settings
  hierarchy, plugin marketplace CLI, MCP secret best-practice, backup/restore cộng đồng), và
  2 vòng interview (5 câu hỏi qua AskUserQuestion) chốt phạm vi/secret/OS/hình thức/vị trí
  export.
- File đổi: `docs/tdq/knowledge/2026-08-04-export-claude-setup.md` (bảng năng lực + khảo
  sát + quyết định chốt + kiểm cổng), `docs/tdq/research/2026-08-04-export-claude-setup.md`,
  `docs/tdq/questions/2026-08-04-export-claude-setup.md`.
- Lý do: hoàn tất phase `analyze` theo skill tdq-intake, chuyển sang phase `spec`.
- Test: không áp dụng (turn chỉ phân tích/ghi tài liệu, chưa code).

## 12:30 — Bổ sung quyết định: bộ công cụ export lưu trong repo
- Ngữ cảnh: user bổ sung yêu cầu — cần file "instruction" (quy trình dựng export, tái dùng
  được) + README, lưu cố định trong thư mục `claude-export/` tại repo TDQWorkflow để lần
  sau export lại được; đường dẫn đích của BUNDLE thực tế vẫn là tham số/placeholder (ngoài
  repo), không hardcode.
- File đổi: `docs/tdq/knowledge/2026-08-04-export-claude-setup.md` (sửa quyết định 4-5:
  tách rõ "bộ công cụ export" trong `claude-export/` của repo vs "bundle thực tế" sinh ra
  ở đường dẫn đích ngoài repo; cập nhật lại phần kiểm cổng "phạm vi cuối").
- Lý do: fold thêm yêu cầu của user vào phase analyze trước khi viết spec (đã revert
  phase spec→analyze rồi set lại spec sau khi cập nhật xong).
- Test: không áp dụng (turn chỉ chỉnh tài liệu phân tích, chưa code).

## 12:45 — Viết spec + review + sửa theo 5 góp ý tdq-reviewer
- Ngữ cảnh: viết `docs/tdq/spec/2026-08-04-export-claude-setup.md` (bản 1.0), chạy
  `doc_lint.py` pass, gọi agent `tdq-reviewer` — nhận 5 góp ý (1 critical: chưa xử lý
  rewrite path `extraKnownMarketplaces.tdq-local` sau khi copy repo sang máy đích).
  Research thêm 2 truy vấn (Windows native vs WSL2 chính thức; Codex CLI cài đa nền)
  để đóng góp ý #4, rồi sửa cả 5: thêm bước rewrite path + QC Q8, bỏ số "50 plugin"
  hardcode, định nghĩa cơ chế tắt log cụ thể (`TDQ_EXPORT_NO_LOG=1`) + QC Q9, thêm QC
  Q10 kiểm bundle không lẫn dữ liệu loại trừ, cập nhật §5 rủi ro theo research mới.
- File đổi: `docs/tdq/research/2026-08-04-export-claude-setup.md` (thêm truy vấn 5-6),
  `docs/tdq/spec/2026-08-04-export-claude-setup.md` (bản 1.0, đã sửa theo review).
- Lý do: hoàn tất bước 1-3 của skill tdq-spec, chuẩn bị trình bản tóm tắt cho user duyệt.
- Test: `python3 scripts/doc_lint.py docs/tdq/spec/2026-08-04-export-claude-setup.md`
  → exit 0.

## 13:05 — Viết plan (mode main) + fix bug doc_lint.py chặn pair-check
- Ngữ cảnh: user chọn mode thực thi `main`. Viết
  `docs/tdq/plan/2026-08-04-export-claude-setup.md` (4 phase, 22 task, đủ khối hợp đồng
  cho 5 skill DÙNG ở spec §3b). Khi chạy `doc_lint.py --pair` phát hiện bug có sẵn: hàm
  `_plan_contracts` không xử lý đúng nhãn `(mcp)` cuối dòng `- Dùng:` (cú pháp chuẩn từ
  spec 2026-08-03), làm sai tên skill đọc được → pair-check báo lỗi sai với task T1.4
  (dùng `tavily-search` qua MCP). Đã hỏi user, được duyệt sửa ngay (gỡ chặn kỹ thuật
  cho chính request này) — sửa 3 dòng trong `_plan_contracts` + thêm 1 unit test
  regression (`test_pair_ok_with_mcp_label`).
- File đổi: `docs/tdq/plan/2026-08-04-export-claude-setup.md` (mới),
  `scripts/doc_lint.py` (fix `_plan_contracts`), `tests/test_doc_lint.py` (test mới).
- Lý do: hoàn tất bước 1-3 của skill tdq-plan; fix doc_lint.py là gỡ chặn kỹ thuật để
  tự-kiểm plan đạt yêu cầu trước khi trình duyệt.
- Test: `python3 -m unittest discover -s tests` → 440 passed;
  `python3 scripts/doc_lint.py --pair docs/tdq/spec/2026-08-04-export-claude-setup.md
  docs/tdq/plan/2026-08-04-export-claude-setup.md` → exit 0.

## 13:15 — Sửa plan theo 7 góp ý tdq-reviewer + đăng ký state
- Ngữ cảnh: gọi agent `tdq-reviewer` trên plan, nhận 7 góp ý; áp dụng đủ cả 7: (1)
  ghi chú lệch câu chữ spec §6/Q1 "6 bước" vs §2 "7 bước" ngay dưới T1.1 (không sửa
  lại spec đã duyệt); (2) T2.3 bổ sung thu thập version thật từng CLI dependency
  (`claude/node/python3/git/uv/graphify --version` + `codex`/`agy` nếu có); (3) làm
  rõ T1.4 chỉ chạy khi phát sinh gap thật lúc viết T1.3, không mâu thuẫn quyết định
  "không dùng thêm" của spec §3b; (4) T3.2 bổ sung điều kiện số plugin README khớp
  100% `manifest.json`; (5) T2.1 làm cụ thể tiêu chí xác nhận đường dẫn đích (ghi
  dòng riêng vào `EXPORT_LOG.md` trước khi `mkdir`); (6) T4.3 viết lại — bỏ tuyên bố
  "1 lệnh shell nối chuỗi Q1–Q10" không thực tế, tách rõ Q2/Q4/Q5 là lệnh đơn exit 0,
  còn Q1/Q3/Q6/Q7/Q8/Q9/Q10 là đối chiếu thủ công làm tuần tự; (7) dòng Q5 trong
  Definition of Done gắn thêm tham chiếu task (T2.4/T2.5/T3.1/T3.2).
- File đổi: `docs/tdq/plan/2026-08-04-export-claude-setup.md`.
- Lý do: hoàn tất bước 3 của skill tdq-plan trước khi trình duyệt.
- Test: `python3 scripts/doc_lint.py --pair docs/tdq/spec/2026-08-04-export-claude-setup.md
  docs/tdq/plan/2026-08-04-export-claude-setup.md` → exit 0;
  `python3 scripts/tdq_state.py set plan_file=docs/tdq/plan/2026-08-04-export-claude-setup.md`
  → ghi thành công.

## 14:03 — Duyệt plan (mode main), chuyển phase implement
- Ngữ cảnh: user duyệt bằng "duyệt plan mode main". Ghi nhận qua `tdq_state.py`,
  cập nhật header plan sang ĐÃ DUYỆT, chuyển state sang phase `implement`.
- File đổi: `docs/tdq/plan/2026-08-04-export-claude-setup.md` (header trạng thái).
- Lý do: hoàn tất bước 6 của skill tdq-plan; bước kế tiếp là tdq-build ở turn mới.
- Test: `python3 scripts/tdq_state.py approve plan --mode main --by "duyệt plan mode main"`
  → OK; `python3 scripts/tdq_state.py set phase=implement` → `phase: "implement"`,
  `implement_mode: "main"`.

## 14:24 — Chặn kỹ thuật T4.2: rsync T2.5 lọt data loại trừ vào bundle
- Ngữ cảnh: QC Q10 (T4.2) phát hiện `rsync` ở T2.5 chỉ `--exclude='.git'`, không áp
  danh sách loại trừ spec §1 cho chính nội dung repo — lọt vào bundle: 1 file
  `docs/tdq/qc/claude-md-backup-2026-08-02.bak` (khớp `*.bak*`, dù file này có track
  git), 4 thư mục `__pycache__` (khớp tinh thần loại trừ cache, untracked), thư mục
  `.remember/logs/` (khớp `logs/`, log runtime của plugin remember, untracked), và
  `graphify-out/cache/` (khớp `cache/`, cache runtime graphify, untracked). Tự chọn
  phương án: xoá các mục đã lọt khỏi bundle đã sinh + thêm 4 `--exclude` tương ứng
  vào lệnh rsync của `claude-export/INSTRUCTIONS.md` Bước 3 để lần export sau không
  lặp lại — không hỏi user vì đây là chặn kỹ thuật có phương án rõ ràng, rủi ro thấp
  (chỉ loại dữ liệu untracked/regenerate-được, không đụng nội dung repo cần giữ).
- File đổi: `claude-export/INSTRUCTIONS.md` (Bước 3, thêm 4 `--exclude`),
  `~/Documents/claude-code-export/tdqworkflow-repo/` (xoá 6 mục đã lọt).
- Lý do: gỡ chặn để T4.2 (QC Q10) pass, theo luật "chặn kỹ thuật → tự chọn đề xuất,
  ghi quyết định, không hỏi" của skill tdq-build.
- Test: sweep lại `find` toàn bộ danh sách loại trừ §1 trên bundle → rỗng (exit 0).

## 14:26 — Hoàn tất P4/QC/Report, đóng sổ plan
- Ngữ cảnh: chạy đủ 10 mục QC (Q1–Q10 spec §6), tất cả PASS — ghi
  `docs/tdq/qc/2026-08-04-export-claude-setup.md`. Chạy toàn bộ test suite (440
  passed), `graphify extract . --code-only` (3 file re-extract, graph cập nhật).
  Viết `docs/tdq/reports/2026-08-04-export-claude-setup.md` (47 dòng). Đổi header
  plan sang HOÀN THÀNH, tick T4.2–T4.4.
- File đổi: `docs/tdq/qc/2026-08-04-export-claude-setup.md` (mới),
  `docs/tdq/reports/2026-08-04-export-claude-setup.md` (mới),
  `docs/tdq/plan/2026-08-04-export-claude-setup.md` (tick nốt, header HOÀN THÀNH),
  `graphify-out/{graph.json,.graphify_analysis.json}` (rebuild).
- Lý do: hoàn tất Phần A/B/C của skill tdq-build cho request export-claude-setup.
- Test: `python3 -m unittest discover -s tests` → 440 passed; `wc -l` report → 47
  dòng (≤ 50); QC 10/10 PASS.

## 19:36 — Mở request approval-gate-bug, hoàn tất phase analyze
- Ngữ cảnh: user báo lỗi — Claude đôi khi tự coi góp ý/bổ sung (không phải câu duyệt) là
  đã duyệt rồi tự next phase. Đã commit export-claude-setup trước (yêu cầu user), rồi mở
  request mới lane full. Đọc toàn bộ hook `prompt_context.py`/`bash_gate.py`/`stop_gate.py`/
  `_common.py`/`session_start.py`: phát hiện `prompt_context.py` đã có sẵn
  `looks_like_approval()` (đọc payload thô, đáng tin) nhưng chỉ in gợi ý mềm, không lưu lại
  để `bash_gate.py` đối chiếu khi thấy lệnh `tdq_state.py approve`/`set phase=`. Soi git log:
  gate cứng cũ (v0.1.4–0.1.6) đã bị bỏ ở v0.3.0 vì đọc transcript gây chặn nhầm. Research 3
  truy vấn tavily-primary (MAST failure taxonomy, pattern tách ý định/thực thi cho HITL).
  Interview 3 câu qua AskUserQuestion: user chọn (1) chỉ siết soft-reminder — không thêm gate
  cứng, (2) áp cho cả 3 gate spec/plan/quick, (3) không audit lịch sử cũ.
- File đổi: `docs/tdq/requests/2026-08-04-approval-gate-bug.md`,
  `docs/tdq/research/2026-08-04-approval-gate-bug.md`,
  `docs/tdq/knowledge/2026-08-04-approval-gate-bug.md`,
  `docs/tdq/questions/2026-08-04-approval-gate-bug.md`.
- Lý do: hoàn tất phase `analyze` của skill tdq-intake, chuyển sang phase `spec` (viết ở
  turn khác, không cùng turn với plan).
- Test: không áp dụng (turn chỉ phân tích/ghi tài liệu, chưa code).

## 20:10 — Viết spec approval-gate-bug (bản 1), đã trình chờ review
- Ngữ cảnh: viết `docs/tdq/spec/2026-08-04-approval-gate-bug.md` từ knowledge đã chốt —
  hướng kỹ thuật: `prompt_context.py` lưu kết quả `looks_like_approval()` vào turn ledger
  (dòng `kind="signal"`), `bash_gate.py` đối chiếu tín hiệu này khi thấy lệnh
  `tdq_state.py approve <spec|plan|quick>`, nhắc lại mã `TDQ:APPROVE` (không thêm mã mới,
  không chặn cứng) nếu tín hiệu nói prompt gần nhất không phải câu duyệt. Nhân tiện sửa lại
  1 đánh giá sai trong knowledge doc (mục Đọc code + Quyết định): kiểm tra kỹ lại
  `_common.py`/`prompt_context.py` cho thấy KHÔNG có bẫy dedupe `already_reminded()` như
  ghi trước đó, vì `prompt_context.py` dùng `_emit()` nội bộ (chỉ `print()`), không gọi
  `_common.remind()`, nên không ghi dòng `kind="remind"` nào để bị dedupe. Đã gọi agent
  `tdq-reviewer` trên spec, đang chờ kết quả.
- File đổi: `docs/tdq/spec/2026-08-04-approval-gate-bug.md` (mới),
  `docs/tdq/knowledge/2026-08-04-approval-gate-bug.md` (sửa 2 đoạn theo phát hiện trên).
- Lý do: hoàn tất bước 1-2 của skill tdq-spec trước khi trình bản tóm tắt cho user duyệt.
- Test: `python3 scripts/doc_lint.py docs/tdq/spec/2026-08-04-approval-gate-bug.md` → exit 0.

## 19:52 — Sửa spec theo 3 góp ý tdq-reviewer, đăng ký spec_file, trình duyệt
- Ngữ cảnh: agent `tdq-reviewer` trả 3 góp ý cho spec bản 1, quan trọng nhất (nghiêm
  trọng): spec chỉ đối chiếu lệnh `tdq_state.py approve <target>`, bỏ sót đường vòng
  `tdq_state.py set phase=<X>` — lệnh này không tự kiểm `*_approved` trước khi ghi
  (đọc lại `scripts/tdq_state.py` xác nhận), nên là đường Claude có thể "next phase"
  thật sự bỏ qua bước approve — đúng kịch bản bug user báo cáo. Đã sửa spec lên bản
  2: thêm nhánh đối chiếu `set phase=plan` (↔ tín hiệu target spec) và
  `set phase=implement` (↔ tín hiệu target plan) vào §1/§2/§3/§6; thêm cờ
  `mode_conflict` vào schema tín hiệu cho nhánh plan lệch mode (góp ý 2); thêm QC Q5
  test fail-open khi ledger rỗng (góp ý 3). Đăng ký `spec_file` vào state.
- File đổi: `docs/tdq/spec/2026-08-04-approval-gate-bug.md` (bản 1 → bản 2).
- Lý do: hoàn tất bước 2-3 của skill tdq-spec, chuẩn bị trình tóm tắt cho user duyệt.
- Test: `python3 scripts/doc_lint.py docs/tdq/spec/2026-08-04-approval-gate-bug.md` →
  exit 0; `python3 scripts/tdq_state.py set spec_file=...` → ghi thành công.

## 19:52 — Duyệt spec (bản 2), chuyển phase plan
- Ngữ cảnh: user duyệt bằng "duyệt spec". Ghi nhận qua `tdq_state.py approve spec`,
  đổi header spec sang ĐÃ DUYỆT, chuyển state sang phase `plan`.
- File đổi: `docs/tdq/spec/2026-08-04-approval-gate-bug.md` (header trạng thái).
- Lý do: hoàn tất bước 5 của skill tdq-spec; bước kế tiếp là tdq-plan ở turn mới
  (hỏi mode thực thi trước khi viết plan).
- Test: `python3 scripts/tdq_state.py approve spec --by "duyệt spec"` → OK;
  `python3 scripts/tdq_state.py set phase=plan` → `phase: "plan"`.

## 19:54 — Fix lệch sha256 spec sau khi tự sửa header (gỡ vướng, user cho phép)
- Ngữ cảnh: mình tự sửa dòng header "Trạng thái" của spec SAU khi đã ghi nhận duyệt
  (chỉ đổi CHỜ DUYỆT → ĐÃ DUYỆT + timestamp, không đổi nội dung §1-§7), khiến
  `spec_sha256` lưu trong state lệch với file thật — hook cảnh báo
  `[TDQ:APPROVE] Spec đã đổi sau khi duyệt`. Đã hỏi user xác nhận, user duyệt lại
  ("duyệt spec") và cho phép mình tự chạy `tdq_state.py` để gỡ vướng kỹ thuật tương
  tự (không phải quyết định thay user) cho các task sau này mà không cần hỏi lại mỗi
  lần. Đã `set spec_approved=false` rồi `approve spec` lại để `spec_sha256` khớp
  đúng file hiện tại.
- File đổi: `docs/tdq/state.json` (qua CLI, không sửa tay).
- Lý do: gỡ vướng kỹ thuật do chính mình gây ra, đã được user duyệt lại rõ ràng.
- Test: đối chiếu `sha256sum` thủ công của file spec với `tdq_state.py get
  spec_sha256` → khớp (`1a9d993d...`).

## 20:08 — Sửa plan theo 5 góp ý `tdq-reviewer` vòng 1 (chưa duyệt)
- Ngữ cảnh: viết `docs/tdq/plan/2026-08-04-approval-gate-bug.md` (3 phase, mode
  `subagent` user chọn), gửi review lần 1 → 5 góp ý, nghiêm trọng nhất là
  `hooks/scripts/edit_gate.py:71` cũng gọi `_common.remind()` với mã `TDQ:APPROVE`
  (lý do khác), có thể nuốt mất nhắc mới cùng mã do `already_reminded()` dedupe
  không phân biệt hook nguồn. Đã sửa: thêm hàm `remind_force()` (không dedupe theo
  mã) chỉ dùng cho 2 điều kiện mới trong `bash_gate.py`, kèm test regression T2.8;
  bổ sung test "set phase= im lặng khi khớp" (T2.6); làm rõ T1.4 (append-only, đọc
  dòng signal gần nhất); nêu rõ thứ tự chèn khối nhắc mới TRƯỚC khối GIT/STATE sẵn
  có (vì `remind()`/`remind_force()` thoát ngay sau khi in); bỏ task T3.1 cũ (grep
  vô nghĩa). Không sửa lại spec đã duyệt — bẫy dedupe là chi tiết triển khai, không
  đổi cam kết đầu ra của spec. Đã gửi lại `tdq-reviewer` lần 2 để xác nhận, đang chờ
  kết quả trước khi trình plan cho user duyệt.
- File đổi: `docs/tdq/plan/2026-08-04-approval-gate-bug.md`.
- Lý do: áp dụng góp ý review đúng quy trình `tdq-plan` bước 3 trước khi trình duyệt.
- Test: `python3 scripts/doc_lint.py --pair docs/tdq/spec/2026-08-04-approval-gate-bug.md
  docs/tdq/plan/2026-08-04-approval-gate-bug.md` → exit 0.

## 20:11 — Review lần 2 xác nhận sạch, đăng ký plan_file, trình duyệt
- Ngữ cảnh: gửi lại `tdq-reviewer` xác minh 5 fix ở mục 20:08 — kết quả sạch, không
  phát sinh gap mới; chỉ 1 điểm nhỏ không bắt buộc (regex `SETPHASE_CLI` cố định
  `phase=` ngay sau `set`, nên tổng quát hoá để bắt cả vị trí khác trong lệnh) — đã
  áp dụng luôn vì rẻ. Đăng ký `plan_file`, trình tóm tắt plan (3 phase/15 task, mode
  `subagent`) trong chat, chờ duyệt.
- File đổi: `docs/tdq/plan/2026-08-04-approval-gate-bug.md` (sửa regex T2.5);
  `docs/tdq/state.json` (qua CLI: `plan_file`).
- Lý do: hoàn tất bước 3–4 skill tdq-plan trước khi trình duyệt.
- Test: `python3 scripts/doc_lint.py --pair ...` (lại) → exit 0.

## 20:12 — Duyệt plan (mode main, đổi từ đề xuất subagent), chuyển phase implement
- Ngữ cảnh: user nhắn "duyệt plan mode main" — lệch mode chốt sẵn trong plan
  (`subagent`) nên hook cảnh báo, mình hỏi lại xác nhận; user nhắn "tiếp tục" (chưa
  rõ, chưa ghi nhận), rồi lặp lại đúng "duyệt plan mode main" lần 2 → coi là xác nhận
  rõ ràng, ghi nhận duyệt với mode `main`. Cập nhật header plan (mode + trạng thái
  ĐÃ DUYỆT), sửa lại gây lệch `plan_sha256` nên tự fix bằng
  `set plan_approved=false` rồi `approve` lại (theo quyền tự gỡ vướng kỹ thuật user
  đã cho phép ở mục 19:54), rồi chuyển state sang phase `implement`.
- File đổi: `docs/tdq/plan/2026-08-04-approval-gate-bug.md` (header mode + trạng
  thái); `docs/tdq/state.json` (qua CLI: `plan_approved`, `plan_sha256`,
  `implement_mode`, `phase`).
- Lý do: hoàn tất bước 6 skill tdq-plan; bước kế tiếp là tdq-build (mode main) ở
  turn mới.
- Test: `python3 scripts/tdq_state.py approve plan --mode main --by "duyệt plan mode
  main"` → OK; `python3 scripts/tdq_state.py set phase=implement` →
  `phase: "implement"`, `implement_mode: "main"`.

## 20:23 — Implement xong P1–P3 (mode main), toàn bộ task đã tick

- Ngữ cảnh: làm hết plan `2026-08-04-approval-gate-bug.md` trong 1 turn, red→green
  từng task. P1: thêm dòng `kind="signal"` vào turn ledger trong
  `hooks/scripts/prompt_context.py` (bắt `matched = looks_like_approval(...)`, ghi
  ngay sau khi tính; nhánh mode-mismatch ghi thêm dòng `mode_conflict=True`, append
  không ghi đè). P2: thêm `remind_force()` (không dedupe theo mã) vào
  `hooks/scripts/_common.py`; thêm `APPROVE_CLI`/`SETPHASE_CLI` +
  `NEXT_PHASE_TARGET` + `_latest_signal()`/`_check_signal_mismatch()` vào
  `hooks/scripts/bash_gate.py`, chèn TRƯỚC khối GIT/STATE sẵn có, tra dòng signal
  GẦN NHẤT theo target — bắn nhắc `TDQ:APPROVE` khi `matched=False` hoặc
  `mode_conflict=True`, im lặng khi khớp thật, fail-open khi ledger rỗng. P3: chạy
  toàn bộ suite — phát hiện 1 test cũ (`test_compliance_protocol.py::
  test_prompt_clears_session_rows`) giả định sai hành vi cũ (không có dòng signal),
  đã sửa assertion cho khớp hành vi mới (đây là thay đổi hành vi có chủ đích của
  chính plan này, không phải regression).
- File đổi: `hooks/scripts/prompt_context.py`, `hooks/scripts/_common.py`,
  `hooks/scripts/bash_gate.py`, `tests/test_prompt_context.py` (mới:
  `TestSignalWritten`), `tests/test_bash_gate.py` (6 test mới T2.1–T2.8),
  `tests/test_compliance_protocol.py` (sửa 1 assertion), `docs/tdq/plan/
  2026-08-04-approval-gate-bug.md` (tick T1.1–T3.1).
- Lý do: hoàn tất Phần A (Implement) của skill tdq-build; bước kế tiếp là QC.
- Test: `python3 -m unittest discover -s tests -p "test_*.py"` → 448 passed
  (từ thư mục `tests/`).

## 20:25 — QC + Report, đóng sổ plan

- Ngữ cảnh: chạy đủ Q1–Q7 (spec §6) — 7/7 PASS (Q2 case (b) mode_conflict=True
  kiểm tay bằng lời gọi `bash_gate.py` trực tiếp, không có test đơn riêng nhưng
  đã xác nhận đúng cam kết). Ghi `docs/tdq/qc/2026-08-04-approval-gate-bug.md`.
  Viết `docs/tdq/reports/2026-08-04-approval-gate-bug.md` (48 dòng, ≤ 50). Đổi
  header plan sang HOÀN THÀNH; toàn bộ checkbox đã tick từ phase implement.
- File đổi: `docs/tdq/qc/2026-08-04-approval-gate-bug.md` (mới),
  `docs/tdq/reports/2026-08-04-approval-gate-bug.md` (mới),
  `docs/tdq/plan/2026-08-04-approval-gate-bug.md` (header HOÀN THÀNH).
- Lý do: hoàn tất Phần B/C của skill tdq-build.
- Test: `python3 -m unittest discover -s tests -p "test_*.py"` → 448 passed;
  `python3 scripts/doc_lint.py docs/tdq/spec/2026-08-04-approval-gate-bug.md` →
  exit 0; `wc -l` report → 48 dòng.
