# Working log 2026-09-11

## 00:08

Spec bản 1.1: bỏ tầng sub-agent và worktree song song theo góp ý vòng 2; mode codex chạy tuần tự trong turn leader, hợp đồng vai sang skills/, mốc hoàn tác git thay worktree tạm; 13 đầu ra, 8 module, 22 hạng mục QC; ghi vòng hỏi đáp 2 vào brief.

## 00:26

Spec mode codex implement lên bản 1.2: sửa theo 18 finding của tdq-reviewer. Tách scripts/tdq_vungfile.py thành module riêng (M2), đưa tdq_state.py lên M1 phụ thuộc = không và cấm nó import tdq_codex, nâng chuỗi ADAPTER_CODEX thành file thật hooks/scripts/codex_edit_gate.py thay vì thêm codex_gate.py cùng vai, gom cờ dòng lệnh về một bảng duy nhất, đổi yêu cầu '< /dev/null' thành 'stdin không phải TTY', ghim nguồn tên model, thêm lệnh cleanup cho CODEX_HOME, thêm modes --json làm nguồn máy đọc được cho cổng chọn mode, hệ số bench thành tuỳ chọn, bỏ cơ chế tự hạ cấp, mở rộng tầng luật lên 8 file. QC từ 22 lên 26 hạng mục, đổi Q6/Q13/Q18 cũ thành hạng mục đo được. doc_lint exit 0. Hạ spec_approved về false: bản 1.1 đã duyệt không còn trên đĩa, chờ user duyệt lại.

## 11:31

Spec mode codex implement lên bản 1.3 (user chọn 1a 2a): gộp 7 chỗ tìm ra ở vòng đo thêm. A hậu kiểm phải so với mốc của chính task chứ không so HEAD (nếu không task sau FAIL oan vì việc chưa commit của task trước). B hoàn tác bằng git restore --source --worktree thay vì git checkout -- file vì checkout ghi cả index làm mù hậu kiểm. C .codex/hooks.json ở gốc repo chỉ hai event PreToolUse, cấm sinh bằng hàm sinh bundle vì hàm đó cắm cả stop_gate.py và prompt_context.py vào lượt codex exec. D leader viết test đỏ trước, Codex chỉ làm xanh, file test thành vùng khoá. E .codex/config.toml gốc repo là file viết tay đã tracked, build không được đè. F nói đúng ba vùng ghi được repo + /tmp + TMPDIR. G log ghi sha256 prompt thay nguyên văn vì repo công khai. Đo thêm hai chỗ và bác bỏ: inherit=core không cắt biến môi trường tự đặt, Codex không commit được vì sandbox chặn .git/index.lock. Đầu ra 13 lên 20, QC 22 lên 31. doc_lint exit 0.

## 12:45

Cắt lại plan mode codex implement theo spec 1.3 đã duyệt: 10 phase theo thứ tự module M1-M9 (state trước, tdq_vungfile.py thứ hai, tdq_codex.py thứ ba), 35 task với nhịp đỏ-xanh chia đôi leader/Codex, hợp đồng graphify, luật file nóng cho 3 đường dẫn, DoD một ô tick cho mỗi mục QC (Q1-Q26 + Q14b-Q14e + Q21b + hồi quy Hub). simulate: đội thắng, cách main 40.6 phút. doc_lint --pair exit 0, plan_file đã đăng ký, phase=plan, chờ duyệt.
