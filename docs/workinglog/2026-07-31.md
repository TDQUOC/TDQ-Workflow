# Working log — 2026-07-31

## 14:14–14:22 — Research (không đổi repo, ghi gộp ở entry sau)
- Trả lời 2 câu research của user: (1) Gemini CLI đã khai tử 18/06/2026 cho tài khoản
  cá nhân, agy là bản thay thế — không đáng cài; (2) probe thật agy headless 2 lần:
  `search_web` + `read_url_content` THỰC THI THẬT (kết quả khớp ground truth npm +
  mốc shutdown), ~17s/call flash-low → search agent bằng agy KHẢ THI.

## 14:23–14:30 — TDQ intake + analyze: request 2026-07-31-agy-search-agent (lane full)
- User chốt: tạo search agent dùng agy cho advanced search/ranking, tích hợp TDQ
  workflow, Claude tự trigger, chịu model cấp thấp. Lane full.
- Request cũ 2026-07-30-fix-agy-adddir-sync-agent đã xong + commit từ trước (state
  vốn idle) → init request mới không mất gì.
- File mới: docs/tdq/{requests,research,questions,knowledge}/2026-07-31-agy-search-agent.md;
  state init full → phase analyze → spec.
- Interview vòng 1 (4 câu, user chốt cả 4 theo đề xuất): bổ sung theo tầng với Tavily;
  script điều phối multi-call; flash-low + escalation; script verify URL + spot-check.
- Kiểm tra đã chạy: skill_inventory.py, external_models.py list agy, probe agy thật
  (2 lần, pass), 5 truy vấn tavily-primary. Chưa sửa code — phase analyze chỉ ra hồ sơ.

## 14:34–14:45 — Phase spec: 2026-07-31-agy-search-agent (bản 1.1, CHỜ DUYỆT)
- User bổ sung 3 yêu cầu (ghi vào requests/questions/knowledge): deep search mặc định
  → search agent; max 3 agent song song config qua settings.json (env
  TDQ_SEARCH_MAX_AGENTS); mỗi agent nhận FULL brief, chỉ chia route.
- Viết docs/tdq/spec/2026-07-31-agy-search-agent.md: search_task.py (split/run/merge,
  rank tất định bằng code), schema all-required ép URL có path, agent search-runner,
  deep-search.md, tích hợp tavily.md/tdq-intake/CLAUDE.md §10, config env, 8 mục QC.
- Review: tdq-reviewer trả 9 finding → áp dụng đủ 9 (cap enforce bằng subcommand
  split; thống nhất luật URL một chỗ; công thức rank tất định; QC cho fallback
  Tavily + spot-check; siết PASS Q3; log per-agent chống race; preflight cả model
  escalation; test timeout/env rác; note restart phiên cho settings.json).
- Kiểm tra: doc_lint spec exit 0 (fix 1 lỗi R8 lý do đóng); spec_file đăng ký vào
  state qua tdq_state.py. Chưa sửa code.

## 14:47–14:55 — Phase plan: 2026-07-31-agy-search-agent (CHỜ DUYỆT)
- User chốt mode thực thi: main (script/schema/test/doc phụ thuộc chặt, đụng file chung).
- Viết docs/tdq/plan/2026-07-31-agy-search-agent.md: 8 phase / 22 task, 5 khối hợp
  đồng năng lực (external scripts, agy CLI, Agent tool, tavily, graphify).
- tdq-reviewer trả 6 finding → áp dụng 5 (T3.7 thêm run-id regex + brief.md; T2.1
  thêm case env rác qua split; T8.3 thêm lệnh --pair; T5.2 mở rộng 6→8 mục doc;
  T3.1 ghi rõ effort nằm trong slug). Finding 6 (T4.2 gộp việc) reviewer tự đánh
  giá chấp nhận được → giữ nguyên.
- Kiểm tra: doc_lint plan + --pair spec/plan đều exit 0 (fix 2 lỗi R5 câu >40 từ);
  plan_file đăng ký vào state qua tdq_state.py. Chưa sửa code — chờ duyệt plan.

## 15:00–15:25 — Build + QC 2026-07-31-agy-search-agent (mode main)
- Implement 22 task / 8 phase một lượt: `scripts/search_task.py` + `search_report_schema.json` + `tests/test_search_task.py` (41 test) + agent `search-runner` + `deep-search.md` + cập nhật tavily.md, tdq-intake B3 (kèm portable/01-intake), CLAUDE.md §10, `portable/workflow/06-deep-search.md`, README portable, `.claude/settings.json` (env TDQ_SEARCH_*), plugin.json 0.5.0 + CHANGELOG 0.5.0 (fix test docs_consistency).
- Test: suite `Ran 326 tests … OK`; doc_lint + `--pair spec plan` exit 0.
- E2E thật: mini-run Python 3.14.6 (log ISO, LOG=0 tắt OK); run `2026-07-31-npm-versions` 2 agent song song → merged khớp 100% `npm view` (typescript 7.0.2, claude-code 2.1.220), route github trả not_found=true; fail-path slug sai 2× → engine-failed + fallback tavily (report demo).
- QC Q1–Q8 PASS (`docs/tdq/qc/…`, Q3/Q6 phần trigger Agent tool PENDING chờ /reload-plugins); report ≤50 dòng tại `docs/tdq/reports/…`.

## 15:22–15:35 — QC vòng 2: fix trigger search-runner qua Agent tool
- Sau restart, agent search-runner đã nạp; trigger thật lần 1+2 FAIL: subagent chạy wrapper nền rồi kết thúc turn (đợi notification) → task nền bị kill, run chết giữa chừng (bằng chứng: run-dir chỉ còn brief.md, không còn process agy).
- Fix QC2.1 `agents/search-runner.md`: wrapper nền ghi marker `agent-<k>.exit`, bắt buộc watcher FOREGROUND chặn turn đến khi marker xuất hiện; ghi rõ notification không tới subagent. Lint + test khuôn OK. Lưu ý: định nghĩa agent cache theo phiên — bản sửa ăn từ phiên sau; test chốt hiện tại chạy qua SendMessage ép foreground.
- Recheck trước đó: suite 326 OK, doc_lint 7 file + pair exit 0, settings.json + plugin 0.5.0 OK.

## 15:36–15:40 — Trigger test PASS + đóng QC vòng 2 + commit 0.5.0
- Trigger search-runner qua Agent tool PASS (SendMessage ép foreground): run
  `docs/tdq/research/search/2026-07-31-trigger-test/` — agent-1.json exit 0,
  2 findings URL sống, JSON trả verbatim; merge OK (1 finding sau dedup).
- Tick QC2.1 trong plan; QC file: Q3/Q6 bỏ PENDING + thêm mục "QC vòng 2"
  (2 lần fail background-kill làm bằng chứng bug, fix, lần 3 pass); report cập
  nhật mục PENDING; sửa 2 câu dài R5 (plan, report) → doc_lint exit 0.
- Validate cuối: suite 326 test OK; report 39 dòng (≤50).
- Commit toàn bộ hồ sơ + code 0.5.0 theo yêu cầu user (recheck ổn → commit).

## 15:39 — Benchmark deep search: Run A (agy) khởi động
- User yêu cầu test deep search "công nghệ speech to text word level realtime",
  đo thời gian + token Claude, so sánh với bản Claude tự search (≤3 agent).
- Tạo `docs/tdq/research/search/2026-07-31-stt-wordlevel/brief.md`; split 3
  route; spawn 3 agent search-runner song song lúc 15:39:35 (phiên mới đã nạp
  search-runner.md bản fix watcher foreground — đây là validate thật cho fix).
- Chưa chạy test (turn chỉ khởi động run; kết quả + merge ở turn kế khi agent
  báo xong).

## 15:43 — Benchmark: Run A (agy) xong, Run B (Claude) khởi động
- Run A `2026-07-31-stt-wordlevel`: 3 agent exit 0, watcher foreground hoạt
  động đúng ở phiên mới (validate fix QC2.1 thành công). Wall 233s
  (15:39:35→15:43:28), 15 findings thô → merge 9, token Claude 3 subagent
  = 93.106 (31.232 + 31.022 + 30.852).
- Run B `2026-07-31-stt-wordlevel-claude`: copy brief, spawn 3 agent
  general-purpose tự search qua tavily-primary lúc 15:43:36, cùng route +
  schema JSON để merge so sánh.
- Test: merge Run A exit 0 (9 findings, not_found=false).

## 15:47 — Benchmark xong: Run B (Claude) + so sánh A/B
- Run B `2026-07-31-stt-wordlevel-claude`: 3 agent general-purpose (tavily-primary)
  exit OK, wall 200s (15:43:36→15:46:56), 18 findings thô → merge 16, token
  Claude = 189.427 (61.745 + 59.949 + 67.733), toàn bộ URL curl 200.
- So sánh: Run A (agy) 233s / 93.1k token / 9 findings (sót Deepgram+AssemblyAI);
  Run B 200s / 189.4k token (~2×) / 16 findings, phủ đủ vendor + số liệu latency.
- Recheck: search-runner.md bản fix chạy đúng ở phiên mới (3/3 agent foreground
  watcher OK, không còn background-kill); merge script ăn được cả output Claude
  (schema tương thích). Artifact run mới chưa commit (user chưa yêu cầu).
- Test: 2 lần merge exit 0; không sửa code nên không chạy lại suite.

## 15:54 — Intake request mới: hybrid deep search
- User đề xuất flow hybrid: agy default flash-medium + Claude scout bao quát
  (≤3 agent, 1 đợt) điều phối agy search chi tiết; hỏi khả thi/tối ưu.
- Check ground truth: `external_models.py list agy` → `gemini-3.6-flash-medium`
  có thật; search_task.py hiện default flash-low, escalation flash-high.
- Tạo `docs/tdq/requests/2026-07-31-hybrid-deep-search.md`; trình đánh giá
  khả thi + đề xuất lane trong chat, DỪNG chờ user chọn lane.
- Test: chưa (turn intake, chưa sửa code).

## 16:00 — Bổ sung request hybrid: agy thêm vai trò tổng quát
- User bổ sung: agy vừa search sâu theo điều hướng Claude, vừa search tổng
  quát để bù lớp bao quát; hỏi có ổn hơn không + ưu nhược điểm.
- Cập nhật `docs/tdq/requests/2026-07-31-hybrid-deep-search.md` (mục Bổ sung);
  trình ưu/nhược + khuyến nghị chạy agy-tổng-quát SONG SONG với scout trong
  chat; vẫn DỪNG chờ user chọn lane.
- Test: chưa (turn intake, chưa sửa code).

## 16:01 — Chốt phase 1 hybrid: 1 Claude ∥ 1 agy
- User chốt: phase 1 chỉ 1 agent Claude scout song song 1 agent agy đi rộng.
- Cập nhật requests/2026-07-31-hybrid-deep-search.md (mục Chốt thêm); vẫn
  chờ user chọn lane quick/full.
- Test: chưa (turn intake, chưa sửa code).

## 16:04–16:10 — Analyze xong request hybrid-deep-search (lane full)
- Init state `2026-07-31-hybrid-deep-search` lane full (ghi đè request cũ đã
  idle); phase analyze → spec.
- Viết knowledge (bảng năng lực B0 + hiện trạng code + quyết định), research
  (2 truy vấn tavily-primary: pattern orchestrator-worker, hệ research đa
  agent Anthropic + benchmark nội bộ), questions (interview vòng 1).
- User chốt 4 quyết định: cap agy P2 = 3; escalation medium→high; findings
  phase 1 gộp vào kết quả cuối; LUÔN chạy đủ 2 phase (không đường tắt).
- Test: chưa (phase analyze, chưa sửa code). Spec sẽ lập ở turn mới.

## 16:11–16:16 — Spec hybrid-deep-search bản 1.1
- Viết `docs/tdq/spec/2026-07-31-hybrid-deep-search.md` (flow 2 phase slot cố
  định 1/2, format file agent cho scout, --start-agent, degrade 3 nhánh,
  0.6.0). tdq-reviewer trả 8 findings — áp dụng cả 8 (format agent-file thay
  schema thô, đo slot trước merge + token ≤250k, ngoại lệ luật split, test
  degrade, phạm vi LOG=0, docstring/USAGE, phân biệt đọc-tín-hiệu vs merge).
- doc_lint spec exit 0; đăng ký spec_file vào state; trình tóm tắt chờ duyệt.
- Test: doc_lint (turn spec, không sửa code).

## 16:2x — Plan hybrid deep search (phase plan)
- Xác nhận approve spec đã ghi (16:21), set phase=plan qua tdq_state.py.
- User chọn mode thực thi: main (AskUserQuestion).
- Viết `docs/tdq/plan/2026-07-31-hybrid-deep-search.md` (6 phase, 18 task) từ spec 1.1.
- tdq-reviewer trả 9 findings → áp dụng hết: URL-alive chấp nhận 403/405, dòng degrade ghi sau merge, tách T2.2, luật scout dùng extract vào needles, grep flash-low toàn file, Q8 tách âm/dương, sửa header spec ĐÃ DUYỆT, cập nhật knowledge format file agent, T6.1 thành rà soát.
- Kiểm: doc_lint spec/knowledge + `--pair spec plan` exit 0; đăng ký plan_file vào state.
- Chưa chạy suite (plan chưa duyệt, chưa sửa code).

## 17:25 — Build + QC hybrid deep search 0.6.0 (phần E2E + QC, mode main)
- E2E hybrid run thật `docs/tdq/research/search/2026-07-31-vectordb-local-rag/`: phase 1 (agy 53 findings ∥ scout 13) → brief-phase2.md 3 route → phase 2 split --start-agent 3 (11/12/13 findings) → merge 1 lần: 31 findings; URL 31/31 sống; spot-check 2 nguồn top khớp.
- QC vòng 1: token 263.972 > 250k → fix QC1.1 `agents/search-runner.md` (tools: Bash, Read + trả tóm tắt thay JSON nguyên văn) + test needle mới; re-measure PENDING reload. Suite 338 OK.
- Degrade drill (scratchpad): agent 1 slug sai → engine-failed, flow tiếp bằng scout, dòng degrade SAU merge trong run.log.
- File đổi: plan (tick T5.1–T5.5, QC1.1–QC1.2, T6.1, header HOÀN THÀNH), tests/test_search_task.py (+1 test), agents/search-runner.md, qc/ + reports/ 2026-07-31-hybrid-deep-search.md (lint pass, report 43 dòng).
- Kiểm đã chạy: unittest discover 338 OK; doc_lint qc/report/pair exit 0; curl 31 URL; tavily-extract 2 nguồn.

## 17:22 — Commit mốc 0.6.0 + đóng request
- User duyệt lại spec (sha lệch do header ĐÃ DUYỆT ghi sau khi approve) → cập nhật `spec_sha256` qua tdq_state.py.
- Commit `0aa075e` "feat: tdq-workflow 0.6.0 — hybrid deep search 2 phase (scout ∥ agy)": 63 file (gồm 2 run-dir benchmark STT + run-dir vectordb E2E); graphify rebuild qua hook post-commit; phase → idle.

## 17:30 — Intake request audit tổng thể workflow
- User yêu cầu recheck tổng thể tdq-workflow: tìm conflict/edge case, 1–2 sample
  E2E, note → nguyên nhân → fix, robust cả với model local tham số thấp.
- Tạo `docs/tdq/requests/2026-07-31-audit-full-workflow.md`; trình đề xuất lane
  full trong chat, DỪNG chờ user chọn lane (chưa init state).
- Test: chưa (turn intake, chưa sửa code).

## 17:31–17:4x — Analyze request audit-full-workflow (lane full)
- User chốt lane full → init `2026-07-31-audit-full-workflow` (đè request cũ đã idle).
- B0 skill inventory; khảo sát cấu trúc plugin (6 skill, 7 agent, 8 script, 5 hook,
  28 test file); research 2 truy vấn tavily-primary (MAST taxonomy, SLM prompting).
- Interview vòng 1 (AskUserQuestion): chốt (1) harden contract — không thêm engine
  local; (2) 2 sample E2E gọi engine thật trong sandbox TDQ_PROJECT_DIR; (3) gộp 2
  PENDING 0.6.0 (đo token, trigger search-scout).
- Issue thấy sớm #1: tdq_state.py:576 regex escape sai → phases.md tự sinh chứa
  literal `\1` (mất 3 dòng lệnh analyze/spec/plan).
- File: requests/ (turn trước), research/, questions/, knowledge/ cùng slug; phase → spec.
- Test: chưa (turn analyze, chưa sửa code).

## 17:36–17:4x — Spec audit-full-workflow bản 1.1
- Viết `docs/tdq/spec/2026-07-31-audit-full-workflow.md` (audit 3 lớp, 7 đầu ra,
  Q1–Q10). tdq-reviewer trả 8 findings — áp cả 8: chốt engine agy +
  `gpt-oss-120b-medium` (fallback gemini-3.5-flash-low), sửa số đếm file lớp 1,
  gộp đo-token + trigger-scout thành 1 run, thêm rủi ro wall-clock, lệnh validate
  report runnable, DoD ghi rõ PENDING-reload, Q4 đổi `! grep -q`, chốt xoá sandbox.
- doc_lint exit 0 (1 vòng sửa R8); đăng ký spec_file vào state.
- Test: doc_lint (turn spec, không sửa code).

## 17:44–17:5x — Plan audit-full-workflow
- User chọn mode main (AskUserQuestion). Viết `docs/tdq/plan/2026-07-31-audit-full-workflow.md`:
  6 phase / 19 task (P1 fix `\1` + sổ findings, P2 deep-search + S1 chạy nền sớm,
  P3 review tĩnh, P4 S2 + fix S/M, P5 log/suite, P6 QC/report).
- tdq-reviewer trả 7 findings → áp cả 7: T4.5 gồm S/M từ E2E, T4.4 thêm
  TDQ_EXTERNAL_TIMEOUT=60, T5.1 không chạy run mới (viện dẫn unit test),
  sửa trường Nạp T2.1, gate suite chỉ P1/P4, ngân sách cứng vào quy tắc 1,
  T2.4 ghi duyệt giả lập.
- doc_lint + --pair exit 0 (3 vòng sửa R5); đăng ký plan_file vào state.
- Test: doc_lint (turn plan, không sửa code).

## Build audit-full-workflow — đóng turn (T4.5.10→T6.3)
- ~18:35–19:0x: hoàn tất T4.5.10 (docs đồng bộ A25–A40: CLAUDE.md §10 tách câu R5,
  phases.md 2 bản regenerate qua `phases-doc [--plugin-root]`, deep-search.md run-dir +
  plugin-root path, qc.md A29, portable external-task/approval/README, intake A35,
  tdq-spec A36 + mirror 02-spec.md).
- T4.6: ghi Findings S2 (A42–A44) vào qc/<slug>.md; xoá sandbox s2-sandbox + worktree
  s2-wt (worktree remove trước, xác nhận không còn trên đĩa).
- T5.1: kiểm log thật — deep-search run.log 50 dòng ISO (24 call + 4 summary đủ trường),
  evidence-s1 5/5; nhánh tắt log viện dẫn test_search_task.py:530, test_external_task.py:303.
- T5.2: suite 367 OK; doc_lint 20 file sửa exit 0; --pair spec plan exit 0.
- T6.1: bảng QC Q1–Q10 PASS vòng 1 (bằng chứng trong qc/). T6.2: report 41 dòng
  (docs/tdq/reports/2026-07-31-audit-full-workflow.md), lint 0.
- File đổi: scripts/*, hooks/prompt_context.py, ~/.claude/scripts/plugin_tiers.py,
  agents/*.md (6), skills + portable docs, tests (367). Ghi chú: agent def PENDING reload.
- 18:57: user đồng ý commit → bump plugin.json 0.6.0→0.6.1, thêm mục CHANGELOG 0.6.1,
  commit `7812f7a` (66 file) gồm toàn bộ fix audit + docs TDQ + run-dir bằng chứng Q7/Q8.
  Hook post-commit tự rebuild graphify nền.
