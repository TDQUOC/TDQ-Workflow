# Working log — 2026-07-31

## 14:14–14:22 — Research (không đổi repo, ghi gộp ở entry sau)
- Trả lời 2 câu research của user: (1) Gemini CLI đã khai tử 18/06/2026 cho tài khoản
  cá nhân, agy là bản thay thế — không đáng cài; (2) probe thật agy headless 2 lần:
  `search_web` + `read_url_content` THỰC THI THẬT (kết quả khớp ground truth npm +
  mốc shutdown), ~17s/call flash-low → search agent bằng agy KHẢ THI.

## 14:23–14:30 — TDQ intake + analyze: request 2026-07-31-agy-search-agent (lane full)
- User chốt: tạo search agent dùng agy cho advanced search/ranking, tích hợp TDQ
  workflow, Claude tự trigger, chịu model cấp thấp. Lane full.
- Request cũ 2026-07-30-fix-agy-adddir-sync-agent đã xong + commit từ trước (state
  vốn idle) → init request mới không mất gì.
- File mới: docs/tdq/{requests,research,questions,knowledge}/2026-07-31-agy-search-agent.md;
  state init full → phase analyze → spec.
- Interview vòng 1 (4 câu, user chốt cả 4 theo đề xuất): bổ sung theo tầng với Tavily;
  script điều phối multi-call; flash-low + escalation; script verify URL + spot-check.
- Kiểm tra đã chạy: skill_inventory.py, external_models.py list agy, probe agy thật
  (2 lần, pass), 5 truy vấn tavily-primary. Chưa sửa code — phase analyze chỉ ra hồ sơ.

## 14:34–14:45 — Phase spec: 2026-07-31-agy-search-agent (bản 1.1, CHỜ DUYỆT)
- User bổ sung 3 yêu cầu (ghi vào requests/questions/knowledge): deep search mặc định
  → search agent; max 3 agent song song config qua settings.json (env
  TDQ_SEARCH_MAX_AGENTS); mỗi agent nhận FULL brief, chỉ chia route.
- Viết docs/tdq/spec/2026-07-31-agy-search-agent.md: search_task.py (split/run/merge,
  rank tất định bằng code), schema all-required ép URL có path, agent search-runner,
  deep-search.md, tích hợp tavily.md/tdq-intake/CLAUDE.md §10, config env, 8 mục QC.
- Review: tdq-reviewer trả 9 finding → áp dụng đủ 9 (cap enforce bằng subcommand
  split; thống nhất luật URL một chỗ; công thức rank tất định; QC cho fallback
  Tavily + spot-check; siết PASS Q3; log per-agent chống race; preflight cả model
  escalation; test timeout/env rác; note restart phiên cho settings.json).
- Kiểm tra: doc_lint spec exit 0 (fix 1 lỗi R8 lý do đóng); spec_file đăng ký vào
  state qua tdq_state.py. Chưa sửa code.

## 14:47–14:55 — Phase plan: 2026-07-31-agy-search-agent (CHỜ DUYỆT)
- User chốt mode thực thi: main (script/schema/test/doc phụ thuộc chặt, đụng file chung).
- Viết docs/tdq/plan/2026-07-31-agy-search-agent.md: 8 phase / 22 task, 5 khối hợp
  đồng năng lực (external scripts, agy CLI, Agent tool, tavily, graphify).
- tdq-reviewer trả 6 finding → áp dụng 5 (T3.7 thêm run-id regex + brief.md; T2.1
  thêm case env rác qua split; T8.3 thêm lệnh --pair; T5.2 mở rộng 6→8 mục doc;
  T3.1 ghi rõ effort nằm trong slug). Finding 6 (T4.2 gộp việc) reviewer tự đánh
  giá chấp nhận được → giữ nguyên.
- Kiểm tra: doc_lint plan + --pair spec/plan đều exit 0 (fix 2 lỗi R5 câu >40 từ);
  plan_file đăng ký vào state qua tdq_state.py. Chưa sửa code — chờ duyệt plan.

## 15:00–15:25 — Build + QC 2026-07-31-agy-search-agent (mode main)
- Implement 22 task / 8 phase một lượt: `scripts/search_task.py` + `search_report_schema.json` + `tests/test_search_task.py` (41 test) + agent `search-runner` + `deep-search.md` + cập nhật tavily.md, tdq-intake B3 (kèm portable/01-intake), CLAUDE.md §10, `portable/workflow/06-deep-search.md`, README portable, `.claude/settings.json` (env TDQ_SEARCH_*), plugin.json 0.5.0 + CHANGELOG 0.5.0 (fix test docs_consistency).
- Test: suite `Ran 326 tests … OK`; doc_lint + `--pair spec plan` exit 0.
- E2E thật: mini-run Python 3.14.6 (log ISO, LOG=0 tắt OK); run `2026-07-31-npm-versions` 2 agent song song → merged khớp 100% `npm view` (typescript 7.0.2, claude-code 2.1.220), route github trả not_found=true; fail-path slug sai 2× → engine-failed + fallback tavily (report demo).
- QC Q1–Q8 PASS (`docs/tdq/qc/…`, Q3/Q6 phần trigger Agent tool PENDING chờ /reload-plugins); report ≤50 dòng tại `docs/tdq/reports/…`.

## 15:22–15:35 — QC vòng 2: fix trigger search-runner qua Agent tool
- Sau restart, agent search-runner đã nạp; trigger thật lần 1+2 FAIL: subagent chạy wrapper nền rồi kết thúc turn (đợi notification) → task nền bị kill, run chết giữa chừng (bằng chứng: run-dir chỉ còn brief.md, không còn process agy).
- Fix QC2.1 `agents/search-runner.md`: wrapper nền ghi marker `agent-<k>.exit`, bắt buộc watcher FOREGROUND chặn turn đến khi marker xuất hiện; ghi rõ notification không tới subagent. Lint + test khuôn OK. Lưu ý: định nghĩa agent cache theo phiên — bản sửa ăn từ phiên sau; test chốt hiện tại chạy qua SendMessage ép foreground.
- Recheck trước đó: suite 326 OK, doc_lint 7 file + pair exit 0, settings.json + plugin 0.5.0 OK.

## 15:36–15:40 — Trigger test PASS + đóng QC vòng 2 + commit 0.5.0
- Trigger search-runner qua Agent tool PASS (SendMessage ép foreground): run
  `docs/tdq/research/search/2026-07-31-trigger-test/` — agent-1.json exit 0,
  2 findings URL sống, JSON trả verbatim; merge OK (1 finding sau dedup).
- Tick QC2.1 trong plan; QC file: Q3/Q6 bỏ PENDING + thêm mục "QC vòng 2"
  (2 lần fail background-kill làm bằng chứng bug, fix, lần 3 pass); report cập
  nhật mục PENDING; sửa 2 câu dài R5 (plan, report) → doc_lint exit 0.
- Validate cuối: suite 326 test OK; report 39 dòng (≤50).
- Commit toàn bộ hồ sơ + code 0.5.0 theo yêu cầu user (recheck ổn → commit).
