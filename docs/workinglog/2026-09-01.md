# Working log 2026-09-01

## 18:39

Duyệt spec (ghi qua tdq_state approve spec); viết plan 7 phase/22 task, lint --pair sạch, bench mo-phong ra Winner=đội gap 24.4 phút; cổng phase=plan của repo còn chặn vì đòi sơ đồ nên state giữ ở pha spec tới khi T1.2 gỡ.

## 18:40

Sửa dòng Mode thực thi khớp số bench đo lại (gap 16.2 phút).

## 19:06

Chạy hết plan bỏ pha sơ đồ (P1-P7, 22 task): `tdq_state.py` bỏ pha `diagram`, khoá state
`diagrams` và 3 lệnh CLI cũ (nay báo lỗi có nghĩa, thoát 2); state cũ tự nâng `phase=diagram`
về `spec`. Xoá `scripts/tdq_mindmap.py`, `scripts/mindmap_render.py`, `skills/tdq-diagram/`,
4 file test mind-map; `doc_lint.py` hết phụ thuộc `tdq_mindmap`. Dọn `phases.md`, tdq-spec,
tdq-plan, tdq-intake, quick-lane (bỏ bước 1b). Sửa 5 file test + 2 ca eval còn đi qua cổng sơ đồ.
Sinh lại 3 bundle portable (checkportable CLEAN cả ba), CHANGELOG 0.36.0 + cắt bớt về archive
cho dưới trần 500 dòng, plugin.json lên 0.36.0, graphify extract lại.

## 20:05 — QC hai vòng + đóng request gỡ pha sơ đồ

QC độc lập vòng 1 FAIL 3 mục. Lỗi thật: cổng vào pha `plan` trước nay chỉ được canh bằng danh
sách sơ đồ, gỡ pha sơ đồ đi là cổng rỗng — plan viết được trước khi user duyệt spec. Thêm P9 vào
plan: `_chan_spec_chua_duyet` gọi khi `set phase=plan` (đúng điều kiện `spec_approved = true` mà
`phases.md` vẫn ghi) kèm test nhánh nghịch; sửa `test_timing.py`, `test_next.py` cho duyệt spec
trước khi qua pha plan. Q10/Q11 thu hẹp lời kiểm về đúng phạm vi request vì 5 file vi phạm lint
là nợ có sẵn, byte y hệt HEAD. QC vòng 2 PASS cả 17 mục DoD.

`pytest tests/ -q` = 101 failed, 1450 passed — bằng mốc T1.1. Sinh lại 3 bundle (CLEAN 90/138/83),
CHANGELOG 0.36.0 bổ sung câu về cổng plan, graphify extract lại (`grep -c mindmap graph.json` = 0
sau `graphify update . --force`). Viết report, chuyển pha `report`. Chưa commit, chưa push.
