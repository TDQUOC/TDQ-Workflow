# Working log 2026-08-16

## 11:12

Mở request 2026-08-16-1110-skill-check-status (lane full): skill check-status dò request/phase đang dở và tiếp tục không mất dữ liệu. Đã ghi brief mục Nguyên văn, init state, vào phase analyze. Kiểm kê năng lực (6 skill tdq-*), đọc docs/kien-truc.md và bề mặt CLI tdq_state.py (đã sẵn plan_tick_state, sha256_file, repo_status_digest, find_shadow_states). Không có gì để commit — working tree sạch từ commit 404de31. Trình vòng scope, chờ user trả lời.

## 11:20

Analyze request skill-check-status: ghi kết quả vòng scope vào brief (mặt CHỌN chức năng+độ tin cậy+bảo trì+UX, skill riêng không thay tdq-status, làm cả bản portable, mức đầu tư đầy đủ). Đọc kien-truc.md, tests/test_skill_shape.py, doc_lint.SKILL_LINE_LIMITS, cây portable/. Trình vòng interview chi tiết, chờ user trả lời.

## 11:29

Xong analyze + viết spec 1.0 cho tdq-check-status: chốt 6 câu vòng 2 (tên tdq-check-status, vá sau 1 lần gật, nguồn dò = docs/tdq + git + working log, tiếp tục tuỳ phase, bỏ request mồ côi). Spec có 10 đầu ra, 11 ca lệch D1-D11, 21 hạng mục DoD; doc_lint exit 0; đã đăng ký spec_file. Trình spec, chờ user duyệt.

## 11:33

User duyệt spec (kèm clean code A = BẬT, đã ghi vào spec §4) → approve spec, sang phase plan và viết plan cùng turn. Plan: 7 phase, 19 task, ~285 phút, mode đề xuất main; DoD 21 hạng mục + QC-F1..F5. doc_lint plan exit 0, doc_lint --pair exit 0, đã đăng ký plan_file. Trình plan, chờ user duyệt.

## 11:59

Build xong skill tdq-check-status (P1-P7): scripts/tdq_checkstatus.py (11 ca lệch D1-D11, 3 mức kết luận, lệnh vá chỉ set/approve), skills/tdq-check-status/ (SKILL 7 bước + 2 reference), portable/workflow/05-check-status.md, tests/test_check_status.py (36 test). Nới trần description 900->1080, phát hành 0.21.0. Full suite 676 passed. QC 24/25 PASS, QC-F5 (agent tdq-qc-tester) đang chạy.

## 12:16

Đóng vòng fix QC1.1-QC1.5 của tdq-check-status: tách state hỏng khỏi không có state (D1 mức chan, cấm init), đọc schema_version từ JSON thô, danh sách trắng lệnh vá, việc kế tiếp gọi đích danh ca chặn, D10 hạ ok khi chỉ rỗng phase_history. Suite 687 passed. Viết report + đóng QC.

## 13:00

Đóng request skill-check-status (phase=idle sau commit 1d18bc6). Mở request mới 2026-08-16-1300-bo-cong-clean-code: brief ghi nguyên văn yêu cầu bỏ cổng hỏi clean code + xoá scripts/code_rule_scan.py, thay bằng luật SOLID model tự tuân. Quét thấy 9 file liên quan, 3 chỗ chờ user chốt. Đang chờ user chọn lane.

## 13:06

analyze request bo-cong-clean-code: user chốt scope 1abc 2a 3a 4a. Research SOLID có nguồn: SOLID là bộ OOP tâm điểm class, repo có 4 class/280 hàm nên phải viết bản đọc theo hàm+module; LSP cần kế thừa mới áp nguyên văn. Ghi research + phạm vi đã chốt vào brief. Chờ user trả lời vòng chi tiết.

## 13:11

Viết spec 1.0 cho bo-cong-clean-code: 12 đầu ra, 19 hạng mục QC. Chốt hai cột SOLID (class / hàm-module) vì repo 4 class/280 def; LSP gắn nhãn giới hạn; Tự kiểm = checklist 5 câu + mở rộng doc_lint R9. graphify xác nhận code_rule_scan.py là lá, xoá không gãy ai. Đăng ký spec_file, chờ user duyệt.

## 13:15

Duyệt spec (approve spec by 'duyệt spec'), phase=plan. Viết plan 16 task / 7 phase cho bo-cong-clean-code, mode đề xuất main. doc_lint --pair spec plan exit 0 sau khi thêm khối hợp đồng graphify + mem0-memory. Chờ user duyệt plan.

## 13:30

Build bo-cong-clean-code P1-P7: viết skills/tdq-conventions/references/clean-code.md (5 luật SOLID, bảng hai bản đọc class/hàm, ví dụ ĐÚNG-SAI trỏ file thật, checklist 5 câu) + tests/test_clean_code_rule.py 20 test. §11 conventions nạp luật, trần dòng 130->133. doc_lint R9 phủ clean-code.md (+3 test r9). Gỡ cổng hỏi khỏi tdq-spec/SKILL.md và spec-template.md. Thêm QC-F4 checklist vào skills/ và portable/ qc.md. Dọn rules/chung.md + index.md. Xoá scripts/code_rule_scan.py, tests/test_code_rule_scan.py, tests/test_clean_code_workflow.py. Phát hành 0.22.0. QC Q1-Q18 PASS, suite 703 passed 375 subtests, doc_lint exit 0. Đang chờ agent tdq-qc-tester cho Q19.

## 13:38

QC vòng 1 + report bo-cong-clean-code. Agent tdq-qc-tester (Q19) tìm 4 defect: 2 lệnh QC trong spec §6 viết sai (Q11 thiếu grep -v docs/workinglog, Q12 sai đường dẫn rules/), và checklist LSP/OCP chỉ hỏi bản đọc cho hàm nên bỏ sót ca có class kế thừa. Fix QC1.1 viết lại hai câu phủ cả hai cột + test test_checklist_phu_ca_hai_ban_doc (đỏ 2 subtest -> xanh), QC1.2 sửa hai lệnh trong spec. Q1-Q19 PASS, suite 704 passed 377 subtests, doc_lint exit 0. Ghi report + 1 fact mem0. Plan đóng HOÀN THÀNH, chưa commit.
