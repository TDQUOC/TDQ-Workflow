# Working log — 2026-07-28

## ~00:30 — Detect bug approve_gate không ghi state (báo từ project insightfaceserverv2)
- Ngữ cảnh: user gõ `/tdq-workflow:tdq-approve spec|plan` nhưng state.json vẫn false.
- Thay đổi repo: chỉ tạo nhầm `docs/tdq/state.json` (artifact thí nghiệm, đã gitignore, sẽ dọn trong plan fix). Không sửa code/doc nào khác.
- Thí nghiệm (scratchpad exp-approve): dump payload UserPromptExpansion thật + test 3 matcher song song.
- Kết luận: matcher `"tdq-approve"` trong hooks/hooks.json KHÔNG match (Claude Code full-match tên command đầy đủ `tdq-workflow:tdq-approve`); matcher đầy đủ và `.*` đều bắn. Pipe payload thật vào approve_gate.py → chạy đúng, approve thành công → script không lỗi, chỉ sai matcher.
- Test đã chạy: tái hiện headless `claude -p` 3 lần + chạy trực tiếp approve_gate.py với payload thật (PASS).
- Trạng thái: đã trình plan fix nhanh (lane quick) trong chat, chờ user duyệt.

## ~00:35 — Implement fix approve_gate matcher (user đã gõ lệnh duyệt quick; hook duyệt fail im lặng do chính bug này — live repro)
- Plan quick đã duyệt (7 bước, trình trong chat): fix matcher, regression test, hardening, test+validate, update bản cài, smoke thật, dọn artifact.
- File đổi:
  - `hooks/hooks.json`: matcher UserPromptExpansion `"tdq-approve"` → `"(tdq-workflow:)?tdq-approve"` (root cause: Claude Code full-match tên command đầy đủ có prefix plugin).
  - `tests/test_approve_gate.py`: +1 regression test `test_hooks_json_matcher_fullmatches_namespaced_command`.
  - `skills/tdq-approve/SKILL.md` + `hooks/scripts/prompt_context.py`: hardening — approval chỉ được coi là thật khi hook in `[TDQ] USER APPROVED …` / state ghi true; cấm suy diễn.
  - `.claude-plugin/marketplace.json`: thêm description (strict validate yêu cầu).
  - `.claude-plugin/plugin.json`: version 0.1.0 → 0.1.1 (để cache bản cài user-level nhận fix).
  - `docs/tdq/state.json`: reset artifact exp-fix (tạo nhầm khi thí nghiệm detect) bằng CLI `tdq_state.py reset`.
- Test đã chạy: unittest 50/50 PASS; `claude plugin validate . --strict` PASS; `claude plugin update tdq-workflow@tdq-local` 0.1.0→0.1.1 OK (cache 0.1.1 có matcher mới); smoke thật headless `claude -p "/tdq-workflow:tdq-approve plan"` trong project tạm → `plan_approved: true` (state ghi đúng bởi hook thật).
- Graphify: `graphify extract . --code-only` OK (5 file re-extracted).
- Lưu ý vận hành: session đang mở vẫn chạy 0.1.0 — cần restart Claude Code để nhận 0.1.1; bên insightfaceserverv2 sau restart chỉ cần gõ lại approve spec rồi plan.

## ~00:45 — Setup test live sau restart (user yêu cầu verify fix)
- File đổi: tạo `docs/tdq/spec/verify-approve-fix.md` (spec test đơn giản); init request `verify-approve-fix` lane full + đăng ký spec_file qua CLI `tdq_state.py`.
- Lý do: user restart xong, muốn tự gõ `/tdq-workflow:tdq-approve spec` trong session thật để xác nhận matcher fix (0.1.1) hoạt động.
- Test: chưa chạy — đang chờ user gõ lệnh duyệt; tiêu chí PASS: hook in `[TDQ] USER APPROVED SPEC …` + state `spec_approved: true`. Không đổi code → không cần graphify.

## ~00:45 — Verify live PASS + dọn test
- Kết quả: user gõ `/tdq-workflow:tdq-approve spec` trong session thật → hook in `[TDQ] USER APPROVED SPEC … sha256 b750912c…` + state `spec_approved: true` đúng timestamp. **Fix matcher 0.1.1 xác nhận hoạt động.**
- Dọn: reset state (CLI tdq_state.py) + xóa `docs/tdq/spec/verify-approve-fix.md` (spec chỉ để test). Không đổi code → không chạy graphify.

## ~09:04 — Request `fix-implement-mode-gate` (lane quick, ĐÃ DUYỆT)
Nguyên nhân user báo "Claude tự quyết implement mode không hỏi": (1) `tdq-implement:18` ghi `main` là **default**; (2) chỉ thị "hỏi user mode" chỉ nằm trong text `approve_gate.py:85`, mâu thuẫn hard rule "end-to-end 1 turn, never stop to ask"; (3) không có enforcement — `edit_gate`/gate không kiểm `implement_mode`, null vẫn implement.

Plan quick đã duyệt:
- [x] 1. `tdq-plan`: bắt buộc dòng `Mode thực thi: main|subagent` + lý do trong plan file và summary chat.
- [x] 2. `tdq-plan` bước 3: KHÔNG tự set `implement_mode` — gate tự đọc từ plan (siết hơn plan gốc, xem QC bên dưới).
- [x] 3. `approve_gate`: plan thiếu dòng mode → block (lý do VI); có → ghi `implement_mode` từ file + next-step "mode đã duyệt cùng plan → implement ngay, không hỏi lại".
- [x] 4. `tdq-implement`: bỏ "(default)" ở `main`; mode lấy từ plan đã duyệt; null/muốn đổi → dừng hỏi user.
- [x] 5. Test: 54/54 PASS (thêm 4 case mode: thiếu dòng → block, đọc mode từ file, state không bypass được file, file override state; sửa 2 test cũ + e2e chain theo contract mới).
- [x] 6. Bump 0.1.3 (0.1.2 → phát hiện lỗ hổng → 0.1.3) + `validate --strict` PASS + marketplace/plugin update + smoke thật + log.

**QC phát hiện thêm (fix ngay trong lane, không cần duyệt lại):** smoke đầu tiên cho thấy Claude tự chạy `tdq_state.py set implement_mode=main` được (key không bảo vệ) → gate chỉ check null là vô nghĩa. Siết: gate đọc mode TỪ NỘI DUNG plan user duyệt (regex `Mode thực thi: main|subagent`, chấp nhận **bold**/`code`), ghi đè vào state, sha256 pin lại lựa chọn.

Smoke thật trên bản cài 0.1.3 (payload UserPromptExpansion thật): A) plan thiếu dòng mode + state đã bị set `main` → exit 2, `plan_approved=false`; B) plan có `Mode thực thi: subagent` → exit 0, `implement_mode=subagent` đúng theo file.
File đổi: `hooks/scripts/approve_gate.py`, `skills/tdq-plan/SKILL.md`, `skills/tdq-implement/SKILL.md`, `tests/test_approve_gate.py`, `tests/test_e2e_chain.py`, `.claude-plugin/plugin.json` (0.1.1→0.1.3).

## ~09:20 — Mở request `2026-07-28-fix-invite-without-request` (lane quick, CHỜ DUYỆT)
- Issue user báo (screenshot): gõ `/tdq-workflow:tdq-approve quick` bị chặn "Chưa có request TDQ nào đang mở".
- Nguyên nhân: không có ràng buộc giữa việc Claude in dòng mời duyệt và việc state có request mở đúng lane; tôi trình plan sau khi đã `reset` state → dòng mời vô hiệu, message của gate lại chỉ in usage nên user bế tắc.
- Thay đổi repo turn này: chỉ init state (gitignore) + artifact graphify từ lần chạy trước; chưa sửa code.
- Plan khắc phục 6 bước (stop_gate chặn dòng mời không hợp lệ, message gate hướng dẫn hành động, siết tdq-start/conventions, test, bump 0.1.4) đã trình chat, chờ user duyệt.

### Plan ĐÃ DUYỆT 09:17 (Mode thực thi: main — 6 task tuần tự, cùng 2 file hook)
- [x] 1. `stop_gate`: thêm `last_assistant_text()` (đọc transcript jsonl) + `invite_problem()`; dòng mời duyệt mà state không đỡ nổi → block end-turn kèm hướng dẫn sửa.
- [x] 2. `stop_gate`: check mời duyệt chạy TRƯỚC, không còn return sớm khi chưa có active_request.
- [x] 3. `approve_gate`: 3 message chặn (chưa có request / sai lane quick / chưa đăng ký file) đổi thành hướng dẫn hành động cụ thể.
- [x] 4. `tdq-start`: init state bắt buộc TRƯỚC khi trình plan/spec + cấm in dòng mời khi state chưa sẵn sàng; `tdq-conventions`: thêm rule mở request trước khi mời duyệt + chỉ `reset` khi user đóng request, test workflow phải dùng `TDQ_PROJECT_DIR` ở thư mục tạm.
- [x] 5. Test: 59/59 PASS (thêm 5 case stop_gate: chưa có request / sai lane / plan chưa đăng ký → block; mời hợp lệ → im lặng; không mời → giữ logic cũ; thêm helper `write_transcript`).
- [x] 6. Bump 0.1.4 + `validate --strict` PASS + marketplace/plugin update (0.1.3→0.1.4) + smoke thật + log.

Smoke thật trên cache 0.1.4 (payload Stop thật, transcript ngoài project): A) mời duyệt khi chưa có request → block; B) mời quick khi lane full → block; C) lane quick + mời quick → im lặng (đúng).
File đổi: `hooks/scripts/stop_gate.py`, `hooks/scripts/approve_gate.py`, `skills/tdq-start/SKILL.md`, `skills/tdq-conventions/SKILL.md`, `tests/test_stop_gate.py`, `tests/helper.py`, `.claude-plugin/plugin.json` (0.1.3→0.1.4).

## ~09:25 — Đóng turn: graphify + dọn
- Chạy `graphify extract . --code-only` (artifact `graphify-out/` cập nhật theo code mới); xóa 2 thư mục smoke trong scratchpad.
- Request `2026-07-28-fix-invite-without-request` để nguyên (không reset — theo đúng rule vừa thêm: chỉ reset khi user đóng request).
- Trạng thái: 3 đợt fix (0.1.1 matcher, 0.1.3 implement mode, 0.1.4 invite gate) chưa commit, đang hỏi user.

## ~09:30 — Commit (user duyệt "okay commit")
- Commit `5da97cb` "fix: siết gate duyệt của tdq-workflow (v0.1.4)" gom cả 3 đợt fix: 17 file (hooks, skills, tests, plugin.json, marketplace.json) + working log hôm nay.
- Kiểm tra sau commit: message không chứa "generated with"/Co-Authored-By AI, author `quoctruong`, branch `main` (tên hợp lệ).
- Hook post-commit graphify tự rebuild → `graphify-out/{graph.json,manifest.json,GRAPH_REPORT.md}` dirty; commit tiếp theo hấp thụ với `GRAPHIFY_SKIP_HOOK=1` (commit chỉ đụng graphify-out + log nên không cần rebuild lại).

## ~14:03 — Detect "Claude tự quyết implement mode" (lane quick, CHỜ DUYỆT)
- Issue user báo sau restart (đã chạy 0.1.4): workflow không hỏi mode implement, state tự có `main`.
- Bằng chứng: `insightfaceserverv2/docs/tdq/state.json` → `implement_mode: "main"` @13:29; plan file dòng 17-18 có `**Mode thực thi: main**` do Claude tự viết. Gate chạy ĐÚNG thiết kế 0.1.3 → không phải bug code mà là lỗ hổng thiết kế: 0.1.3 chỉ chặn "set mode lén qua state", chưa chặn "tự quyết mode rồi nhét vào plan".
- Nguyên nhân chi tiết: (1) `tdq-plan` không có bước hỏi user; (2) `approve_gate.py:91-97` không phân biệt user chọn hay Claude chọn; (3) `approve_gate.py:107-109` in "do NOT ask again"; (4) `implement_mode` không thuộc PROTECTED_KEYS, lane quick không có mode.
- Thay đổi repo turn này: tạo `docs/tdq/requests/2026-07-28-ask-implement-mode.md`; init state request mới lane quick (state.json gitignore).
- User chốt: hướng **C** (hỏi tường minh + lệnh duyệt kèm mode), lane **quick**. Plan 7 bước đã trình chat, chờ duyệt. Chưa sửa code → chưa chạy test/graphify.

### Plan ĐÃ DUYỆT 14:07 (lane quick) — mode implement do user quyết (hướng C), bump 0.1.5
- [x] 1. `approve_gate`: đọc mode (main|subagent) từ command args của lệnh duyệt plan; thiếu → block kèm hướng dẫn cú pháp.
- [x] 2. `approve_gate`: dòng `Mode thực thi:` trong plan chỉ là ĐỀ XUẤT; user gõ khác → theo user + in rõ chênh lệch; bỏ câu "do NOT ask again".
- [x] 3. `tdq_state.py`: `implement_mode` vào PROTECTED_KEYS.
- [x] 4. `tdq-plan`: bắt buộc hỏi user chọn mode trước khi ghi plan; dòng mời duyệt kèm mode.
- [x] 5. `tdq-implement` + `tdq-start`: mode = chữ user gõ khi duyệt; null → dừng hỏi.
- [x] 6. `stop_gate`: chỉ nhận dòng mời duyệt thật (dòng bắt đầu `➤ Để duyệt:`) → hết false positive (bug #5 vừa lộ khi chính tôi bị chặn vì nhắc tên lệnh trong thân plan).
- [x] 7. Test: ~8 case mới + sửa e2e chain; `python3 -m unittest discover .` trong tests/.
- [x] 8. Bump 0.1.5 + validate --strict + marketplace/plugin update + smoke payload thật + log.

### Kết quả implement 14:14 (0.1.5)
- File đổi: `hooks/scripts/approve_gate.py` (mode lấy từ command args user gõ; dòng `Mode thực thi:` trong plan chỉ còn là ĐỀ XUẤT bắt buộc có; bỏ "do NOT ask again"; in rõ khi user chọn khác đề xuất), `hooks/scripts/stop_gate.py` (INVITE_LINE_RE — chỉ dòng bắt đầu `➤ Để duyệt:` mới là lời mời; mời duyệt plan thiếu mode → block), `scripts/tdq_state.py` (`implement_mode` vào PROTECTED_KEYS), `skills/tdq-plan|tdq-implement|tdq-start|tdq-approve|tdq-conventions/SKILL.md`, `README.md`, `tests/test_approve_gate.py|test_stop_gate.py|test_state.py|test_e2e_chain.py`, `.claude-plugin/plugin.json` (0.1.4→0.1.5).
- Test: 64/64 PASS (`python3 -m unittest discover .` trong tests/); `claude plugin validate . --strict` PASS; `claude plugin update tdq-workflow@tdq-local` 0.1.4→0.1.5 OK.
- Smoke thật trên cache 0.1.5: A) duyệt spec OK; B) `plan` thiếu mode → exit 2 + hướng dẫn cú pháp; C) CLI `set implement_mode=subagent` → từ chối (protected); D) `plan subagent` trong khi plan đề xuất `main` → theo user, state `implement_mode=subagent`, output nêu rõ chênh lệch; E) chỉ nhắc tên lệnh trong thân plan → stop_gate im lặng (hết false positive); F2) mời duyệt plan thiếu mode → block; G) mời duyệt kèm mode → im lặng.
- Ghi chú: `docs/spec/tdq-workflow-plugin.md:144` (spec gốc) đã yêu cầu "sau duyệt plan: hỏi user chọn sub-agent hay main-agent" — bước này bị rơi khi implement, đây chính là gốc của issue. Nay đã đưa vào cả skill lẫn enforcement bằng hook.
- Graphify: `graphify extract . --code-only` (8 file re-extract). Cần restart Claude Code để session hiện tại nhận 0.1.5.

## ~16:50 — Issue: gate 0.1.5 chặn duyệt plan vì regex dòng mode quá hẹp (lane quick, CHỜ DUYỆT)
- Hiện tượng (insightfaceserverv2, request `2026-07-28-kiosk-pose-guidance`): user gõ đúng `/tdq-workflow:tdq-approve plan main` 2 lần → bị chặn "Plan chưa có dòng đề xuất mode".
- Nguyên nhân: `approve_gate.PROPOSED_RE` chỉ khớp đúng chuỗi `Mode thực thi: main|subagent`; plan viết `Đề xuất mode: **main**` (dòng 4, ghép chung dòng header) — đúng ngữ nghĩa, sai chữ. Skill 0.1.5 lại nhấn mạnh chữ "ĐỀ XUẤT" nên model diễn đạt lại → regex trượt. Message chặn không nói rõ dạng chấp nhận và không trỏ file plan.
- Gỡ kẹt ngay: sửa header `insightfaceserverv2/docs/tdq/plan/2026-07-28-kiosk-pose-guidance.md` — tách dòng `Mode thực thi: main — <lý do>` đúng dạng (không đổi nội dung plan).
- Thay đổi repo TDQWorkflow turn này: chỉ init request mới (state.json gitignore) + log này. Plan fix 6 bước đã trình chat, chờ duyệt.

### Plan ĐÃ DUYỆT 16:58 (lane quick) — nới regex dòng mode + message chặn hữu ích (0.1.6)
- [x] 1. `approve_gate.PROPOSED_RE`: nhận mọi nhãn chứa `mode` + `:` + giá trị (bold/backtick tuỳ ý).
- [x] 2. Message chặn nêu rõ đường dẫn plan file + ví dụ dạng hợp lệ.
- [x] 3. `stop_gate`: cảnh báo sớm ngay ở turn trình plan nếu plan file chưa có dòng mode.
- [x] 4. `tdq-plan`: template dòng copy nguyên văn, đứng riêng một dòng.
- [x] 5. Test: 5 case mới + full suite.
- [x] 6. Bump 0.1.6 + validate --strict + marketplace/plugin update + smoke payload thật + log.

### Kết quả implement 17:01 (0.1.6)
- File đổi: `hooks/scripts/approve_gate.py` (PROPOSED_RE nới thành `\bmode\b[^\n:]{0,40}:\s*[*`_ ]*(main|subagent)` — mọi nhãn chứa "mode" + ":" đều hợp lệ; message chặn nêu đường dẫn plan + dạng mẫu), `hooks/scripts/stop_gate.py` (cùng regex; mời duyệt plan mà plan file chưa có dòng đề xuất mode → block ngay ở turn trình plan), `skills/tdq-plan/SKILL.md` (template dòng riêng, cấm ghép vào dòng header), `tests/test_approve_gate.py` + `tests/test_stop_gate.py` (+3 case: 5 biến thể chữ đều pass, "main" trần không tính là đề xuất, cảnh báo sớm ở stop_gate), `.claude-plugin/plugin.json` (0.1.5→0.1.6).
- Sửa kèm để gỡ kẹt session đang chạy: `insightfaceserverv2/docs/tdq/plan/2026-07-28-kiosk-pose-guidance.md` tách dòng `Mode thực thi: main — <lý do>` (không đổi nội dung task).
- Test: 67/67 PASS; `validate --strict` PASS; cài user-level 0.1.5→0.1.6 OK.
- Smoke thật trên cache 0.1.6: dòng header y hệt bản lỗi (`... · Đề xuất mode: **main**`) + `plan main` → duyệt OK, `implement_mode=main`; plan chỉ có chữ "main" trần (không nhãn) → chặn kèm đường dẫn file + dạng mẫu.
- Graphify: `graphify extract . --code-only`. Cần restart Claude Code để nhận 0.1.6.

## ~17:05 — Commit (user duyệt "okay commit")
- Commit `8e9bc66` "fix: mode implement do user quyết + nới nhận diện dòng mode (v0.1.6)": 18 file (hooks, skills, tests, tdq_state, README, plugin.json) + request doc + working log.
- Hook post-commit graphify rebuild → `graphify-out/*` dirty; commit tiếp theo hấp thụ với `GRAPHIFY_SKIP_HOOK=1`.

## ~18:00 — Bug #7: yêu cầu mới không đồng bộ lại state (lane full, giai đoạn SPEC)
- Bằng chứng: transcript `insightfaceserverv2/…/8294af46….jsonl` idx 2206 (mời duyệt quick) → idx 2207 Stop hook chỉ chặn vì working log → idx 2221 approve_gate "Sai lane". Replay chính chuỗi đó vào stop_gate 0.1.5 thì block đúng ⇒ script đúng, nguyên nhân là transcript chưa flush message cuối nên `last_assistant_text()` đọc trượt.
- Nguyên nhân: (1) không có bước đồng bộ state khi mở yêu cầu mới; (2) stop_gate chỉ đọc 1 message cuối; (3) message "Sai lane" thiếu lệnh sửa cụ thể.
- File thay đổi turn này: `docs/tdq/requests/2026-07-28-state-reset-on-new-request.md` (mới), `docs/tdq/spec/2026-07-28-state-reset-on-new-request.md` (mới), state (`init … full`, `phase=spec`, `spec_file`), log này.
- Chưa chạy test — turn này mới ở bước spec, chờ user duyệt trước khi lập plan.
- 18:02 spec ĐÃ DUYỆT (sha256 bc40092cc8bb). 18:04 lập plan `docs/tdq/plan/2026-07-28-state-reset-on-new-request.md` (4 phase / 13 task), đăng ký `plan_file`, mode đề xuất: main. Chờ user duyệt plan.

### Plan ĐÃ DUYỆT 18:05 (lane full, mode main) — implement 0.1.7
- [x] P1 state: `previous_request`, schema_version 2, `load()` bù khoá, `init` cảnh báo `⚠️ Ghi đè` khi đè request dở.
- [x] P2 stop_gate: `turn_assistant_texts()` quét cả lượt (lùi tới prompt user thật, bỏ qua tool_result / Stop hook feedback, tối đa 8 message) + message chặn nêu lệnh init.
- [x] P3: prompt_context in dòng "Request đang mở …"; approve_gate "Sai lane" kèm lệnh init; skills tdq-start/tdq-conventions bắt init cho mọi yêu cầu mới (hỏi user trước khi đè request dở).
- [x] P4: suite 67→77 PASS, bump 0.1.7, validate --strict PASS, cài user-level 0.1.7, smoke trên cache, QC + report.
- File đổi: `scripts/tdq_state.py`, `hooks/scripts/{stop_gate,prompt_context,approve_gate}.py`, `skills/{tdq-start,tdq-conventions}/SKILL.md`, `tests/{helper,test_state,test_stop_gate,test_context_hooks,test_approve_gate}.py`, `.claude-plugin/plugin.json`, docs `spec/plan/qc/reports` của request.
- Smoke trên cache 0.1.7: (a) transcript trễ + dòng mời quick khi lane full → block đúng; (b) `init … quick` → reset đủ khoá, `previous_request` giữ slug cũ, có cảnh báo ghi đè; (c) approve quick → PASS.
- Lưu ý: cần restart Claude Code để session hiện tại chạy 0.1.7. Chưa commit.
- 18:12 chạy `graphify extract . --code-only` (10 file re-extracted) → cập nhật `graphify-out/*`. Kết thúc lane full cho request `2026-07-28-state-reset-on-new-request` (phase report).

## ~18:25 — Mở request quick `2026-07-28-state-path-project-root` (chống "state bóng")
- Ngữ cảnh: sự cố bên `insightfaceserverv2` — CLI chạy từ thư mục con tạo state trùng theo cwd, model đọc nhầm file bóng nên tưởng chưa duyệt.
- File đổi: `docs/tdq/requests/2026-07-28-state-path-project-root.md` (mới), state (`init … quick`, đóng request 0.1.7 đã hoàn tất).
- Mini-plan 6 bước (resolve project root, không tạo state ở subdir, cảnh báo trùng, hook dùng chung, test, bump 0.1.8) đã trình chat — chờ user duyệt, chưa đụng code.
- Chưa chạy test (chưa implement).

### Plan ĐÃ DUYỆT 18:25 (lane quick) — 0.1.8 chống "state bóng"
- [x] 1. `resolve_project_dir()`: TDQ_PROJECT_DIR > thư mục có sẵn state > thư mục có `.git` > cwd; mọi lệnh CLI đi qua nó.
- [x] 2. `init` từ subdir ghi vào root đã resolve + in `ℹ️ Project root: …`.
- [x] 3. Cảnh báo khi repo có >1 state (bỏ node_modules/.git/venv).
- [x] 4. `_common.payload_cwd` dùng chung resolve.
- [x] 5. Test: chạy từ subdir không tạo file mới · TDQ_PROJECT_DIR thắng · không .git → fallback cwd · cảnh báo trùng · suite cũ PASS.
- [x] 6. Bump 0.1.8 + validate --strict + cài user-level + smoke từ subdir + log.

### Kết quả implement 18:28 (0.1.8)
- File đổi: `scripts/tdq_state.py` (`resolve_project_dir()` — TDQ_PROJECT_DIR > git root > thư mục có sẵn state > cwd; `find_shadow_states()`; CLI in `ℹ️ Project root` khi chạy từ subdir và `⚠️ Phát hiện state thừa`), `hooks/scripts/_common.py` (`payload_cwd` đi qua resolve), `tests/helper.py` (`run_state_cli_in`), `tests/test_state.py` (+6 case), `.claude-plugin/plugin.json` (0.1.7→0.1.8).
- Quyết định: **git root thắng** một state lỡ tạo ở thư mục con — đúng tình huống `insightfaceserverv2` (state bóng ở `frontend/`), tránh state con chiếm quyền.
- Test: 77→83 PASS; `validate --strict` PASS; cài user-level 0.1.8.
- Smoke trên cache 0.1.8: (1) `init` từ `frontend/src` → chỉ 1 state ở root + in Project root; (2) approve hook với cwd = subdir → ghi đúng state gốc; (3) `quick_approved=true` ở root; (4) có state bóng → CLI cảnh báo kèm đường dẫn.
- Cần restart Claude Code để session hiện tại nhận 0.1.8. Chưa commit.
- 18:29 chạy `graphify extract . --code-only` (5 file re-extracted) → cập nhật `graphify-out/*`. Kết thúc request `2026-07-28-state-path-project-root` (lane quick, 0.1.8).

## ~18:35 — Request mới (lane full): bỏ hard gate → nhắc nhở, duyệt bằng chat
- Interview: user chọn lane **full**, giữ **Stop remind** (block 1 lần/turn cho working log), bỏ mọi deny, và **Claude tự ghi nhận duyệt** khi user nói duyệt trong chat.
- Căn cứ kỹ thuật: doc hook Claude Code (code.claude.com/docs/en/hooks) — PreToolUse hỗ trợ `permissionDecision: "allow"` kèm `additionalContext` (nhắc mà không chặn); Stop vẫn hỗ trợ `decision: block`.
- File đổi: `docs/tdq/requests/2026-07-28-soft-gates-remind.md` (mới), `docs/tdq/spec/2026-07-28-soft-gates-remind.md` (mới), state (`init … full`, phase=spec, spec_file). Request quick 0.1.8 trước đó đã hoàn tất nên bị đóng.
- Chưa chạy test (mới ở bước spec, chưa đụng code).

## ~18:40 — Plan 0.2.0 (soft gates) đã soạn, chờ duyệt
- Spec được duyệt 18:37:17 (sha256 `bcc682c732f6`). Đã hỏi mode thực thi → user chọn **main**.
- File đổi: `docs/tdq/plan/2026-07-28-soft-gates-remind.md` (mới, 4 phase / 15 task), state (`plan_file`, `phase=plan`).
- Thứ tự thực thi trong plan: CLI `approve` (P1) → 4 hook chuyển deny→allow/remind + xoá `approve_gate.py` (P2) → skills/README (P3) → nghiệm thu + bump 0.2.0 (P4).
- Chưa chạy test: chưa được duyệt plan nên chưa đụng code.

## ~18:41–18:52 — Implement 0.2.0 (soft gates), plan duyệt mode main
- Plan duyệt 18:41:15 (sha256 `f130bda0ebb7`), mode **main**. 16/16 task tick `[x]`.
- **CLI** `scripts/tdq_state.py`: schema_version 3, thêm `spec/plan/quick_approved_by`, bỏ `PROTECTED_KEYS`, thêm lệnh `approve <spec|plan|quick> [--mode] [--by]` (idempotent exit 0; sai lane/thiếu file/plan trước spec → cảnh báo stderr nhưng vẫn ghi).
- **Hook**: `_common.deny()` → `remind()` (allow + additionalContext); `edit_gate.py`, `bash_gate.py` viết lại theo hướng nhắc; **xoá** `hooks/scripts/approve_gate.py`, mục `UserPromptExpansion` trong `hooks/hooks.json`, `tests/test_approve_gate.py`, `tests/fixtures/approve.json`; `stop_gate.py` bỏ toàn bộ `check_invite`/`INVITE_*`/`PROPOSED_RE`/`MODE_RE`; `prompt_context.py` + `session_start.py` nhắc chạy `approve … --by`.
- **Skills/docs**: `tdq-conventions` (mục mới "Ghi nhận duyệt", bỏ mục field bảo vệ), `tdq-approve` (viết lại thành phím tắt), `tdq-plan`, `tdq-spec`, `tdq-start`, `tdq-status`, `tdq-implement`, `README.md` → dòng mời tự nhiên `➤ Duyệt: nhắn "duyệt plan mode main"`.
- **Test**: `cd tests && python3 -m unittest discover .` → **65 test PASS** (83 → 65 do xoá test của approve_gate + nhánh invite; các test deny đổi sang assert allow + additionalContext). `grep -rn '"deny"' hooks/ scripts/` sạch.
- **Đóng gói**: plugin 0.1.8 → **0.2.0**, `validate --strict` PASS, marketplace + `claude plugin update` OK. Smoke bản cài: (a) edit chưa duyệt → allow + nhắc, (b) `approve quick` 2 lần đều rc 0, (c) Stop vẫn block khi thiếu working log, (d) không còn `approve_gate.py` trong cache.
- Doc mới: `docs/tdq/plan|qc|reports/2026-07-28-soft-gates-remind.md`. Đã chạy `graphify extract . --code-only` (264 nodes / 504 edges).
- Lưu ý: session đang chạy phải **restart** mới nạp hook 0.2.0.

## ~18:54 — Xác minh hook 0.2.0 sau restart + khôi phục state bị smoke test ghi đè
- Test thật: `Write tmp_gate_check.txt` (ngoài `docs/`, state đang "chưa duyệt") → file **được tạo**, hook chỉ trả `additionalContext` nhắc. Xác nhận không còn deny cứng. Đã xoá file tạm.
- Sự cố: lệnh smoke 18:49 chạy `tdq_state.py init smoke quick` KHÔNG kèm `TDQ_PROJECT_DIR` ở lần gọi đầu → ghi đè state thật của project thành request `smoke`/quick.
- Khắc phục: `init` lại `2026-07-28-soft-gates-remind` lane full + `set` khôi phục nguyên các mốc duyệt gốc (spec 18:37:17, plan 18:41:15, mode main, `*_approved_by` = đúng lệnh user đã gõ), phase=report. `plan_sha256` để null vì plan đã tick `[x]` sau khi duyệt nên hash cũ không còn ý nghĩa.
- Bài học: mọi lệnh smoke phải đặt `TDQ_PROJECT_DIR` ở TỪNG lệnh, không dùng `||` fallback.

## ~23:20 — Rà soát tổng thể plugin + mở request mới "instruction-hardening-7b" (phase analyze)
- Rà soát read-only toàn bộ plugin (10 skill, 3 agent, 5 hook, CLI, tests, docs, manifest): code sạch, không dead code, 65 test PASS cả khi chạy từ root lẫn từ `tests/`. Phát hiện: doc v0.1 ở `docs/{spec,plan,qc,reports}/` + `idea.md` đã lạc hậu (còn mô tả `approve_gate.py`), `docs/.DS_Store`, `~/.claude/CLAUDE.md` §10 còn luật gate cứng mâu thuẫn 0.2.0, `docs/notes/user-level-install.md` §3 cũng vậy, state.json bị gitignore → mất dấu vết duyệt, chưa có CHANGELOG, `tdq-start:33`/`tdq-plan:8`/`marketplace.json`/README còn câu chữ "hook chặn/gate cứng", `tdq-status` chưa hiện `implement_mode`/`*_approved_by`.
- User ra yêu cầu mới: hook = remind + instruction bắt agent tuân theo, bỏ skill `tdq-approve`, viết lại instruction/skills đủ chi tiết để model yếu (7B) đi đúng workflow. Lane **full**; cho phép sửa `~/.claude/CLAUDE.md`; gộp hết mục rà soát vào request này.
- State: `init 2026-07-28-instruction-hardening-7b full` (đè request `2026-07-28-soft-gates-remind` đã ở phase report), phase analyze → spec.
- File mới: `docs/tdq/requests|research|questions|knowledge/2026-07-28-instruction-hardening-7b.md`.
- Research 4 truy vấn `tavily-primary` + WebFetch doc chính thức. Kết quả then chốt: doc Claude Code xác nhận **PreToolUse CÓ hỗ trợ `additionalContext`** (issue GitHub #15664 nói ngược là đã lạc hậu) → kiến trúc 0.2.0 hợp lệ; Stop hook cũng nhận `additionalContext`. Nguồn khác: instruction văn xuôi không phải cơ chế bảo đảm (càng nhiều rule tuân thủ càng giảm), model nhỏ cần scaffolding cứng + one-task-per-step, doc Anthropic khuyến nghị thân SKILL.md < 500 dòng + progressive disclosure + checklist copy được.
- Interview 2 vòng (7 câu) → chốt: model nhỏ chạy THẬT (giữ plugin + sinh bản portable `portable/AGENTS.md`), cơ chế tuân thủ = hook nhắc kèm mã + agent echo + Stop soát, gộp 9 skill → 5 với thân gọn + `references/`, thêm lệnh `tdq_state.py next` in tiếng Việt kèm checklist, nghiệm thu bằng lint doc tự viết trong `tests/`.
- Chưa đụng code. Bước kế: viết spec (turn sau, theo quy ước spec/plan không cùng turn).

## ~23:35 — Viết spec 0.3.0 (instruction-hardening-7b)
- File mới: `docs/tdq/spec/2026-07-28-instruction-hardening-7b.md` (8 mục, có in/out-of-scope, bảng mã nhắc, DoD Q1–Q10). Đăng ký `spec_file` vào state.
- Nội dung chốt: giao thức `[TDQ:<CODE>]` + echo `✓ [TDQ:<CODE>]` + soát ở stop_gate (chỉ TDQ:LOG mới block); lệnh `tdq_state.py next`; skills 9→5 + `references/`; xoá `tdq-approve`; bản `portable/`; `scripts/doc_lint.py` + test; gộp dọn dẹp T2–T4/C1–C5/D1–D2; bump 0.3.0.
- Chưa đụng code, chờ user duyệt spec. Chưa spawn `tdq-reviewer` (session có chỉ thị không tự gọi subagent).

## ~23:45 — Spec 0.3.0 bản 1.1: bổ sung thiết kế state file
- Góp ý user: cần state đọc trực tiếp được để agent tự biết đang ở đâu, xử lý file chuẩn, chi tiết đến mức model yếu không sai.
- Thêm §2.8 vào `docs/tdq/spec/2026-07-28-instruction-hardening-7b.md`: bản mirror `docs/tdq/STATE.md` (tự sinh sau mỗi lần ghi, markdown, không sửa tay); quy tắc đọc/ghi cho agent; 8 yêu cầu kỹ thuật S1–S8 (ghi nguyên tử `os.replace`, tự phục hồi file hỏng → `state.json.corrupt-<ts>`, backfill + giữ khoá lạ, enum sai không chết, in đường dẫn project, cảnh báo state trùng/xung đột 2 session, không exit≠0 vì trạng thái); bảng quyết định phase (điều kiện vào / việc duy nhất được làm / lệnh chuyển tiếp / điều cấm) làm hằng trong code.
- Cập nhật kéo theo: §1.2, §1.3 (out-of-scope: không làm khoá đa phiên), §3 (lý do chọn mirror), §4 (yêu cầu 5–6), §5 (RR8–RR11), §6 (Q11–Q12, DoD Q1–Q12).
- Chưa đụng code; spec vẫn chờ duyệt.

## ~23:58 — Spec 0.3.0 bản 1.2: rà kỹ toàn bộ, sửa lỗi thiết kế cơ chế kiểm tuân thủ
- Yêu cầu user: rà spec kỹ hơn để mọi model (kể cả local) làm đúng behavior, nhưng vẫn tối ưu token.
- Viết lại `docs/tdq/spec/2026-07-28-instruction-hardening-7b.md` thành bản 1.2. Bảy phát hiện (ghi ở §8):
  - **P1 (nghiêm trọng)**: bản 1.1 bắt `stop_gate` đọc transcript tìm dòng echo — đúng cơ chế đã bị xoá ở 0.2.0 vì transcript trễ gây chặn nhầm; model yếu còn có thể in echo giả. → Đổi sang **kiểm bằng hiệu ứng**: sổ turn `docs/tdq/.tdq-turn.jsonl` ghi cả `remind` (hook phát mã) lẫn `observe` (hành động thật: `edit:<path>`, `log_written`, `state_cli`, `next_run`) do chính 2 hook PreToolUse ghi — không thêm hook mới; `stop_gate` đối chiếu hai bên, chỉ block khi repo đổi mà chưa ghi log. Echo `✓ [TDQ:…]` hạ xuống thành chỉ dẫn hành vi, không hook nào đọc.
  - P2 thêm §2.7 ngân sách token (trần theo từng hook/file + test đo Q13); P3 thêm §2.9 định nghĩa chính xác (slug, dấu hiệu duyệt + phản ví dụ, "repo đổi", exit code, "một turn"); P4 thêm §2.8 giao thức một turn 6 dòng; P5 siết 3 rule lint quá gắt (R2 chấp nhận inline-code/bảng, R4 bỏ "có thể" và chỉ soát mục bước, R7 nêu đích danh 3 skill); P6 đặc tả `get <key>` + `next --brief`; P7 thêm RR12–RR13, yêu cầu §4.7 (hook không bao giờ làm hỏng tool call) + test Q14.
- Đánh số lại: §2.3 state file (cũ 2.8), QC lên Q1–Q15, out-of-scope thêm "không đọc transcript" + "không thêm hook mới".
- Chưa đụng code; spec vẫn chờ duyệt.
