# Working log — 2026-08-12

## 12:08 — Mở intake request hoàn thiện product document trên Excalidraw

Ngữ cảnh: các turn trước đã dựng 5 khối trên canvas Excalidraw (flow plugin, ưu điểm,
nền tảng/test-dev, manifest & dependency, sequence diagram 19 message đã đánh số).
User yêu cầu lập request để bổ sung phần còn thiếu và sắp xếp toàn bộ thành một product
document hoàn chỉnh.

File đổi: `docs/tdq/brief/2026-08-12-hoan-thien-doc-excalidraw.md` (mới)

Lý do: TDQ intake — ghi nguyên văn yêu cầu, liệt kê 5 khối đang có, 8 mảng còn thiếu
(tổng quan sản phẩm, kiến trúc thư mục, state machine, getting started, giới hạn,
roadmap, troubleshooting, ví dụ thực tế) và 3 chỗ chưa rõ cần hỏi user.

Test đã chạy: `python3 scripts/tdq_state.py next` → phase idle, xác nhận đây là request
mới hợp lệ. Chưa `init` state vì đang chờ user chốt lane (đề xuất full).

## 12:11 — Phase analyze: kiểm kê năng lực, đọc code, research, mở vòng interview 1

Ngữ cảnh: user chốt lane **full**. Đã `init 2026-08-12-hoan-thien-doc-excalidraw full`
+ `set phase=analyze`.

File đổi:
- `docs/tdq/brief/2026-08-12-hoan-thien-doc-excalidraw.md` — điền mục `## Hiểu & kiến thức`
  (bảng năng lực B0, bảng dữ kiện-nguồn cho 7 khối mới, tóm tắt research) và mở
  `## Hỏi đáp` vòng 1
- `docs/tdq/research/2026-08-12-hoan-thien-doc-excalidraw.md` — sub-agent
  `general-purpose` ghi, 4 truy vấn tavily về chuẩn cấu trúc product documentation

Lý do: lane full bắt buộc rời phase analyze với zero chỗ đoán. Cần chốt nguồn dữ kiện
thật cho từng khối sẽ vẽ (tránh bịa) và chốt thứ tự chương theo chuẩn Diátaxis.

Kết quả đọc code: `PHASE_TABLE` (`scripts/tdq_state.py:475`) có 7 phase + nhánh `quick`;
`state.json` schema v3 18 field; repo có 6 hook script, 7 script, 6 skill.

Test đã chạy: `python3 scripts/tdq_state.py next` → phase analyze, đúng vị trí. Chưa
chuyển `phase=spec` vì vòng interview 1 chưa có câu trả lời của user.

## 12:14 — Đóng interview, viết spec 13 chương

Ngữ cảnh: user trả lời vòng 1 nguyên văn "1A 2A 3A 4A" → bổ sung đủ 8 mảng thiếu, di
chuyển 5 khối cũ (không vẽ lại), bố cục một cột dọc, có export ra `docs/diagrams/`.
Không còn câu hỏi làm đổi kết quả → `set phase=spec`.

File đổi:
- `docs/tdq/brief/...md` — thêm `### Lộ trình` (bỏ subagent, bỏ QC độc lập, bỏ review
  sâu; nêu lý do từng cái) và ghi câu trả lời interview vòng 1
- `docs/tdq/spec/2026-08-12-hoan-thien-doc-excalidraw.md` — spec bản 1.0, 17 đầu ra,
  11 hạng mục QC

Lý do bỏ subagent: mọi thao tác đi qua một canvas Excalidraw dùng chung, nhiều agent ghi
song song sẽ tranh z-order và tọa độ.

Quyết định kỹ thuật ghi vào spec §3: cấm dùng `update_element` cho `x/y/width/height/text`
— đã tái hiện 2 lần lỗi silent-fail (trả "success", version tăng, nhưng `get_element`
cho thấy thuộc tính không đổi). Thay bằng delete + `batch_create_elements`.

Test đã chạy: `python3 scripts/doc_lint.py docs/tdq/spec/...md` → exit 0.
`python3 scripts/tdq_state.py set spec_file=...` → OK. Đang chờ user duyệt spec.

## 12:18 — User duyệt spec, viết plan 6 phase / 22 task

Ngữ cảnh: user nhắn "duyệt spec". Ghi nhận bằng
`tdq_state.py approve spec --by "duyệt spec"` rồi `set phase=plan`, viết plan ngay trong
cùng turn theo luật gộp gate.

File đổi: `docs/tdq/plan/2026-08-12-hoan-thien-doc-excalidraw.md` (mới)

Nội dung plan: bảng lưới toạ độ chốt sẵn cho 14 khối (một cột dọc, rộng 2640px, tổng cao
~15.200px), 6 phase — P1 backup + script kiểm, P2 di chuyển 5 khối cũ, P3-P5 vẽ 8 chương
mới + mục lục, P6 kiểm toàn cục và export. Mode ĐỀ XUẤT `main`.

Lý do đề xuất mode main: mọi task ghi vào cùng một canvas qua MCP, thứ tự tạo quyết định
z-order; sub-agent chạy trong git worktree không thấy canvas và không có MCP tool này.

Quyết định plan: P1 dựng đường red→green trước khi động vào canvas — viết
`scripts/check_canvas_layout.py` + test của nó, và export backup scene, để nếu bước di
chuyển làm rơi phần tử thì phát hiện được bằng lệnh chứ không bằng mắt.

Test đã chạy: `python3 scripts/doc_lint.py --pair <spec> <plan>` → exit 0 (mọi đầu ra
spec §2 đã ánh xạ tới task, 2 skill DÙNG đều có khối hợp đồng đủ 5 trường).
`tdq_state.py set plan_file=...` → OK. Đang chờ user duyệt plan kèm mode.

## 12:32 — P2 xong: 5 khối cũ đã về đúng chương 2/5/7/9/10

File đổi:
- `scripts/canvas_move_block.py` (mới) — dời một khối: chọn element theo TÂM nằm trong
  vùng nguồn, dịch toạ độ canh giữa khung 2640px, đổi id sang `ch<N>-frame` /
  `ch<N>-title` / `ch<N>-e<i>` kèm ánh xạ lại `containerId` · `boundElements` ·
  `startBinding` · `endBinding`, rồi DELETE + POST batch (luật 7: cấm `update_element`)
- `scripts/canvas_layout_apply.py` (mới) — chạy cả 5 phép dời trong một lượt ghi
- `docs/tdq/plan/...md` — cập nhật bảng lưới theo kích thước ĐO THẬT, thêm mục số liệu
  gốc, tick T2.1–T2.5

Sự cố + cách sửa: chạy dời từng khối một thì lệnh dời ch7 (vùng y 1780–3340) cuốn luôn
ch2 vừa đặt ở y 1420–2420 → mất 36 phần tử. Khôi phục bằng
`mcp-excalidraw-server import docs/diagrams/_backup-2026-08-12.excalidraw --replace`
(về đúng 214). Sửa gốc: tính mọi phép dời trên cùng một ảnh chụp scene rồi ghi một lần,
và chặn trước nếu một element bị hai vùng cùng chọn. Bài học này ghi vào plan P2.

Sự cố phụ: `--region -220,...` bị argparse hiểu là tên cờ (giá trị bắt đầu bằng dấu trừ)
→ nay vùng nguồn nằm trong bảng `MOVES` của script, không truyền qua dòng lệnh nữa.

Đổi lưới: chiều cao thật của khối flow là 1857 (gồm 2 nhãn mũi tên ở x âm) chứ không phải
1745 như ước lượng → đẩy chương 6–13 xuống, giữ khe ≥120px.

Test đã chạy: `check_canvas_layout.py http://127.0.0.1:17739/api/elements --count-by-region
--overlap --order` → PASS, tổng 214 phần tử không đổi, không phần tử nào nằm ngoài khung.
Screenshot đã xem: một cột dọc, thứ tự 2→5→7→9→10, không cắt chữ.

## 12:41 — T3.1: vẽ Ch.1 Tổng quan sản phẩm

File đổi: canvas Excalidraw (14 element mới, prefix `ch1-`), `docs/tdq/plan/...md` (tick T3.1).

Nội dung 4 thẻ 2×2 — vấn đề giải quyết · đối tượng dùng · giá trị cốt lõi · vị trí sản
phẩm — mọi dữ kiện trích từ `README.md` và `.claude-plugin/plugin.json` (0.11.2), không
tự nghĩ.

Sửa lệch: `--contain` bắt được `ch5-e059` tràn 5px lên trên mép khung chương 5 (frontend
ước lượng lại chiều cao text sau khi tạo). Nới khung ch5 thành `y=4560 h=1918`, hạ ch4
xuống `y=3320` để giữ khe 120px. Đây đúng là loại lỗi vài pixel mà screenshot ở mức zoom
nhỏ không thấy — lý do spec chốt kiểm bằng script chứ không bằng mắt.

Test đã chạy: `check_canvas_layout.py http://127.0.0.1:17739/api/elements --contain
--overlap --order` → PASS (228 phần tử).

## 12:52 — P4→P6 hoàn tất: 13 chương + mục lục, export ra docs/diagrams/

**P4** — vẽ Ch.6 (ví dụ thực tế request `2026-08-11-cai-tdq-project-level`, 7 bước,
29 phần tử) và Ch.8 (cây thư mục + 6 hook card, 23 phần tử). Ch.6 phải vẽ lại 3 lần:
7 cột rồi 4 cột đều làm `fit()` cảnh báo tràn; chốt ở 4 cột với mọi dòng ≤ 42 ký tự.
Sửa plan T4.2 từ "7 script" thành "11 script" — `ls -1 scripts/*.py` đếm 11 sau khi
thêm 3 script canvas trong chính request này.

**P5** — Ch.11 Giới hạn (6 mục, mỗi mục kèm file nguồn), Ch.12 Troubleshooting (6 cặp
hỏi-đáp), Ch.13 Roadmap (3 mốc 0.11.0/0.11.1/0.11.2 chép nguyên từ CHANGELOG.md, ghi
rõ không bịa mốc tương lai), Ch.0 Mục lục. Ch.13 phải vẽ lại một lần vì 2 dòng vượt
70% bề rộng thẻ. Mục lục ban đầu FAIL `--toc`: phép kiểm đòi dòng cho MỌI `ch<N>-title`,
kể cả `ch0-title`, nên mục lục có 14 dòng (Ch.0–Ch.13) chứ không phải 13 như plan viết —
đã sửa lại câu chữ T5.4.

**P6** — 5 phép kiểm hình học trên scene sống đều PASS (436 phần tử trong khung, 91 cặp
khung không chồng lấn, 14 khung đúng thứ tự dọc, 14 tiêu đề khớp 14 dòng mục lục).
Export `docs/diagrams/tdq-workflow-product-doc.excalidraw` (450 phần tử) và `.png`
(2660×15440, 3.4 MB — dưới trần 20 MB nên giữ nguyên scale). Chụp kiểm từng chương bằng
cách cắt PNG theo bounding box của `ch<N>-frame` (Pillow, cài vào `.venv`) vì
`set_viewport` không crop được ảnh: xem đủ 14/14 ảnh, không chương nào có chữ bị cắt.
Lưu 1 fact vào mem0 về lỗi `update_element` silent-fail.

**Px** — `.venv/bin/python -m pytest -q`: 457 passed, 140 subtests passed. Không test
nào chuyển pass → fail.

Lưu ý số liệu: chương 5 còn 63 phần tử thay vì 65 như đo ở P1 — hai phần tử mất là
`ch5-e059`/`ch5-e060`, bound label mồ côi (containerId trỏ tới arrow nằm cách xa), bị
frontend bố trí lại ~75px mỗi lần sync nên trôi ra khỏi khung; đã gỡ khỏi `boundElements`
của arrow chứa rồi xoá hẳn. Bốn khối còn lại giữ nguyên số: ch2=55, ch7=60, ch9=19, ch10=15.

## 12:58 — Mở request mới: đổi khổ tài liệu sang bề ngang A4 dọc

User phản hồi tài liệu vừa hoàn thiện: zoom vừa bề ngang thì chữ thân quá nhỏ, muốn
design lại theo bề ngang khổ A4 dọc.

Chẩn đoán: `fontSize` không sai, sai ở tỉ lệ. Khung rộng 2640px bóp vào cửa sổ ~1400px
là co còn ~53%, chữ 13px hiển thị như ~7px.

Đã tạo `docs/tdq/brief/2026-08-12-layout-a4-doc.md` (mục `## Nguyên văn`), nêu 4 chỗ
chưa rõ phải interview: số px của bề ngang A4 · mỗi chương có = đúng 1 trang A4 không ·
xử lý 3 khối bản chất nằm ngang (Ch.7 sequence 6 lane, Ch.4 state machine 8 node,
Ch.2 lưới 2 cột) · có tăng fontSize không.

Đề xuất lane full, đang chờ user chốt lane. Chưa init state, chưa đụng canvas.
Không có test nào phải chạy ở bước này.

## 13:04 — Analyze request 2026-08-12-layout-a4-doc

Init state lane full (ghi đè request cũ đã idle), set phase=analyze.

Đo bounding box thật trên bản export để khỏi đoán. Phát hiện đổi hẳn phạm vi: **4/5 khối
cũ (Ch.2, 5, 9, 10) vốn chỉ rộng 760–940px**, tức đã gần vừa khổ A4 — chúng không phải
vấn đề. Chỗ rộng 2540–2550px là 8 chương TÔI tự xếp 2–3 cột, cộng đúng **hai** khối rộng
do bản chất: Ch.4 (8 node state machine xếp ngang + bảng schema 3 cột) và Ch.7 (sequence
diagram 6 lane, 2640px).

Tính ra số để chọn khổ: zoom vừa bề ngang 2640px vào cửa sổ ~1400px là tỉ lệ 0,53 nên chữ
13px hiện như 7px; ở bề ngang 1240px tỉ lệ thành 1,13 nên chữ 16px hiện như 18px. Đòn bẩy
chính là bề rộng khung, không phải fontSize.

Bỏ bước research ngoài: A4 là hằng số ISO 216, phần còn lại đo từ file trong repo.

File đổi: `docs/tdq/brief/2026-08-12-layout-a4-doc.md` (thêm mục Hiểu & kiến thức, bảng
kiểm kê năng lực B0, bảng số đo, bảng quy đổi DPI). Chưa đụng canvas, chưa có test để chạy.
Đang chờ user trả lời 4 câu interview.

## 13:1x — Spec khổ A4 (2026-08-12-layout-a4-doc)

Viết `docs/tdq/spec/2026-08-12-layout-a4-doc.md` từ brief: bề ngang 1240px (A4 @150dpi),
chiều cao thả tự do, Ch.4 + Ch.7 vẽ lại theo chiều dọc, chữ thân 16 / tối thiểu 14 /
tiêu đề 30. Phạm vi: vẽ lại 10 chương (0,1,3,4,6,7,8,11,12,13), dời 4 khối đã hẹp
(2,5,9,10), thêm 2 cờ kiểm `--width` và `--fontsize` kèm unit test. DoD 12 hạng mục.
`doc_lint.py` exit 0. Đã set `spec_file`. Chờ user duyệt spec.

## 15:29 — Duyệt spec + viết plan khổ A4

User duyệt spec lúc 15:28. Viết `docs/tdq/plan/2026-08-12-layout-a4-doc.md`: 5 phase + Px,
23 task, tổng độ phức tạp n89. Mode đề xuất `main` (mọi task ghi cùng một canvas sống).
Hai khối hợp đồng skill: `excalidraw-skill` (mcp) ở T3.1, `mem0-memory` (mcp) ở T5.4.
`doc_lint.py --pair` exit 0. Đã set `plan_file`. Chờ user duyệt plan kèm mode.

## 15:35–15:55 — Implement + QC khổ A4 (2026-08-12-layout-a4-doc)
- `check_canvas_layout.py`: thêm cờ `--width`, `--fontsize` (+6 test, tổng 17).
- `canvas_draw.py`: W=1240, cỡ chữ 16/20/30, thêm `stack()`; `canvas_move_block.py`: CHAPTER_W=1240.
  `tests/test_canvas_draw.py` mới — 14 test.
- `scripts/canvas_a4_rebuild.py` + `scripts/canvas_a4_ch4_ch7.py` mới: dựng offline ra
  `_a4-draft.excalidraw`, kiểm máy rồi mới xoá-tạo lên canvas một lượt (393 phần tử, cao 19.549px).
- Sửa ngắt dòng: `unwrap()` nối lại dòng bị ngắt do ô cũ; `squeeze_tree_line()` bóp khoảng đệm cho
  cây thư mục Ch.8 thay vì ngắt theo từ.
- Export lại `docs/diagrams/tdq-workflow-product-doc.{excalidraw,png}` (PNG 1260×19448).
- QC 12/12 PASS → `docs/tdq/qc/2026-08-12-layout-a4-doc.md`; report →
  `docs/tdq/reports/2026-08-12-layout-a4-doc.md`. Full-suite 477 test xanh. Chưa commit.

## 17:31 — Phân tích lỗ hổng tick checkbox ở lane quick
- Bối cảnh: user nghi ngờ task lane quick không được tick ngay khi xong mà gom vào cuối.
- Kết quả rà soát (chỉ đọc, chưa sửa code): xác nhận nghi ngờ đúng, ba lớp lệch nhau —
  `PHASE_TABLE["quick"]` (scripts/tdq_state.py:583-614) và
  `skills/tdq-intake/references/quick-lane.md` không hề dạy dấu `[~]`;
  `hooks/scripts/stop_gate.py:148-165` chỉ so `plan_sha` đầu/cuối turn nên bulk-tick trong
  một turn duy nhất của lane quick luôn lọt; `hooks/scripts/edit_gate.py:87-99` chỉ NHẮC
  `TDQ:TICK` và bị đặt cuối chuỗi `remind()`.
- User chốt phương án C (sửa cả tài liệu lẫn nâng hook thành chặn).
- File đổi: thêm `docs/tdq/brief/2026-08-12-siet-tick-lane-quick.md`. Chưa init state,
  đang chờ user chọn lane. Chưa chạy test (turn này không đổi mã nguồn).

## 17:35 — Mở request quick `2026-08-12-siet-tick-lane-quick`
- Init state lane quick (ghi đè request `2026-08-12-layout-a4-doc` đã đóng).
- Interview 1 câu: user chọn phương án B — chặn TDQ:TICK nhưng miễn trừ `tests/**`.
- Viết mini-spec/plan gộp `docs/tdq/plan/2026-08-12-siet-tick-lane-quick.md` (5 task, 5 DoD).
- Chưa sửa mã nguồn, chưa chạy test. Đang chờ user duyệt quick.
- 17:37 — User hỏi phạm vi ảnh hưởng của hàng rào chặn ở project không dùng TDQ. Rà
  `edit_gate.py:55-58` + `tdq_state.load/state_path`: không có `docs/tdq/state.json` →
  `load()` trả None → hook return sớm, không chặn. Bổ sung vào T2 yêu cầu thông điệp deny
  phải kèm đường thoát cho ca state treo `phase=implement`.
- 17:38 — User duyệt quick (QC BẬT). Bắt đầu implement 5 task theo plan.
- 17:38-17:55 — Implement xong 5 task + 2 vòng fix QC.
  - `hooks/scripts/_common.py`: thêm `block()` (PreToolUse deny, KHÔNG dedupe vì điều kiện
    chặn tự tan khi tick; ghi sổ turn kind `block`).
  - `hooks/scripts/edit_gate.py`: `TDQ:TICK` chuyển remind → block, miễn trừ `tests/**`,
    thông điệp có lệnh thoát `set phase=idle`; cập nhật docstring.
  - `scripts/tdq_state.py`: PHASE_TABLE["quick"] dạy `[~]` + `forbidden` cấm gom tick;
    regen `skills/tdq-conventions/references/phases.md`.
  - `skills/tdq-intake/references/quick-lane.md` (+ SKILL.md Phần C bước 7): thêm mục
    "Luật tick" và hàng rào chặn.
  - `tests/test_edit_gate.py`: `TickRemindTest` → `TickBlockTest`, thêm ca không-dedupe và
    ca `tests/**` không bị chặn.
  - QC vòng 1: `test_compliance_protocol.py` T2.11 cấm `"deny"` tuyệt đối → thu hẹp bất biến,
    chỉ `_common.py` được phép. QC vòng 2: `trim()` cắt mất lệnh thoát → rút gọn thông điệp.
  - Full-suite 479 test xanh, doc_lint sạch, QC 7/7 PASS. Chưa commit.

## 18:03 — Mở request commit + bump version (kiêm bài thử hàng rào tick)
- Ghi brief `docs/tdq/brief/2026-08-12-commit-bump-thu-hang-rao.md`.
- Chưa init state, đang chờ user chốt lane và 3 câu hỏi (mức bump, push hay không,
  gộp/tách commit).
- 18:06 — Interview chốt: bump 0.11.3 (patch), chỉ commit local, tách 2 commit. Viết
  mini-plan `docs/tdq/plan/2026-08-12-commit-bump-thu-hang-rao.md` (6 task, 7 DoD),
  chờ duyệt.
- 18:07 — User duyệt quick (QC BẬT). Bắt đầu T1–T6.
