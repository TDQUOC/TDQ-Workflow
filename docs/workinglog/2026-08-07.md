# Working log 2026-08-07

## 16:21 — Mở request siết QC + vòng fix cho lane quick

**Ngữ cảnh:** trước đó 2 turn read-only đã xác minh lane quick chỉ nói "chạy validate"
(không định nghĩa) và không có luật nào cho tình huống gặp bug — `qc.md` và luật
`## QC vòng N — fix` đều chỉ được `tdq-build` (lane full) nạp. User yêu cầu mở request TDQ.

**File đổi:**
- `docs/tdq/requests/2026-08-07-siet-qc-lane-quick.md` (mới) — nguyên văn 3 turn yêu cầu,
  cách hiểu đầu tiên, hiện trạng đã xác minh kèm số dòng, 6 chỗ chưa rõ cần interview.
- `docs/tdq/state.json` — turn trước đã `set phase=idle` để đóng
  `2026-08-05-clone-setting-codex`; turn này chưa `init` (đang chờ user chốt lane).

**Lý do:** ghi lại yêu cầu trước khi hỏi lane, theo Phần A bước 1 của `tdq-intake`.

**Test đã chạy:** không có — turn này chỉ tạo tài liệu, chưa đụng code. Hiện trạng được
xác minh bằng grep/read trực tiếp các file `skills/tdq-intake/**`, `skills/tdq-build/**`.

**Trạng thái:** DỪNG chờ user chốt lane (đề xuất quick). Chưa có file plan nên chưa có
task nào để tick.

## 16:24 — Lane full, phase analyze: kiểm kê + đọc code + interview vòng 1

**Ngữ cảnh:** user chốt lane **full** (B) cho request siết QC/vòng fix lane quick.

**File đổi:**
- `docs/tdq/state.json` — `init 2026-08-07-siet-qc-lane-quick full` (ghi đè request cũ
  `2026-08-05-clone-setting-codex` phase idle, đã cảnh báo), rồi `set phase=analyze`.
- `docs/tdq/questions/2026-08-07-siet-qc-lane-quick.md` (mới) — interview vòng 1, 7 câu.

**Việc đã làm:**
- B0 kiểm kê năng lực: `scripts/skill_inventory.py` → 248 skill, 32 nguồn (bảng phán quyết
  sẽ ghi vào knowledge sau khi interview xong).
- B2 đọc code: xác định **4 nguồn sự thật** phải sửa đồng bộ —
  `skills/tdq-intake/references/quick-lane.md:14`, `skills/tdq-intake/SKILL.md` Phần C b7,
  `scripts/tdq_state.py` `PHASE_TABLE["quick"]` (dòng 529-551), và bản mirror
  `portable/workflow/references/quick-lane.md` + `portable/workflow/phases.md`.
  Ràng buộc test: `tests/test_portable_sync.py` (parity skills↔portable từng bước),
  `tests/test_phase_table.py` (PHASE_TABLE là nguồn sự thật, doc phải khớp),
  `scripts/doc_lint.py` R6 trần dòng `tdq-intake` = 120 (hiện 84 → còn 36 dòng dư).
- B3 research: **BỎ** — việc thuần nội bộ, không có ẩn số thư viện/API/phiên bản bên ngoài.

**Test đã chạy:** chưa — phase analyze chưa sửa code.

**Trạng thái:** DỪNG chờ user trả lời interview vòng 1.

## 16:42 — Interview vòng 1 có đáp, mở vòng 2 vì đáp 7 xung đột đáp 1+2

**Ngữ cảnh:** user đáp 1A-6A và 7.B ("ở quick thì sẽ hỏi người dùng cần QC ko thay vì tự
có QC"). Đáp 7 ngược với đáp 1+2 (QC 3 hạng mục bắt buộc, bằng chứng ghi vào plan) → không
được tự suy diễn, phải hỏi vòng 2.

**File đổi:**
- `docs/tdq/questions/2026-08-07-siet-qc-lane-quick.md` — ghi đáp vòng 1 (bảng 7 dòng,
  kèm nguyên văn), nêu xung đột, thêm vòng 2 gồm 5 câu (8-12): hỏi ở đâu trong luồng,
  mặc định khi user im lặng, vòng fix có còn bắt buộc khi bỏ QC, mục `## QC` xử lý sao.

**Test đã chạy:** chưa — vẫn ở phase analyze.

**Trạng thái:** DỪNG chờ user trả lời interview vòng 2.

## 16:52 — Chốt knowledge + viết spec v1.0, chờ duyệt

**Ngữ cảnh:** user đáp vòng 2 toàn A (8A/9A/10A/11A/12A) → hết chỗ đoán, xung đột vòng 1 đã
giải: QC mặc định BẬT, opt-out qua gate duyệt; vòng fix KHÔNG opt-out được.

**File đổi:**
- `docs/tdq/questions/2026-08-07-siet-qc-lane-quick.md` — ghi đáp vòng 2 (bảng 5 dòng) + kết luận.
- `docs/tdq/knowledge/2026-08-07-siet-qc-lane-quick.md` (mới) — bảng kiểm kê 248 skill
  (3 DÙNG: test-driven-development, verification-before-completion, skill-creator; 2 NỀN),
  9 quyết định đã chốt, 4 nguồn sự thật N1-N4, 4 phương án đã loại, Lộ trình 12 dòng, kiểm cổng 3/3.
- `docs/tdq/spec/2026-08-07-siet-qc-lane-quick.md` (mới) — spec v1.0, 7 đầu ra, 8 hạng mục QC.
- `docs/tdq/state.json` — `set phase=spec`, `set spec_file=...`.

**Test đã chạy (lấy mốc trước khi sửa):**
- `cd tests && python3 -m unittest discover . -q` → **603 test, 1 failure**:
  `test_claude_md_core.test_d_ban_repo_trung_ban_da_cai` — ĐỎ TỪ TRƯỚC, do
  `portable/claude-md/CLAUDE.md` lệch `~/.claude/CLAUDE.md` (mục plugin đổi 2026-08-06).
  Đã ghi NGOÀI phạm vi ở spec §1, DoD tính mốc này.
- `python3 scripts/doc_lint.py skills portable` → exit 0.
- `python3 scripts/doc_lint.py docs/tdq/spec/2026-08-07-siet-qc-lane-quick.md` → exit 0 (R8 pass).

**Trạng thái:** DỪNG chờ user duyệt spec.

## 17:34 — Viết plan, gọi tdq-reviewer, áp 17/17 finding, spec lên bản 1.1

**Ngữ cảnh:** user duyệt spec 1.0 lúc 16:56 → viết plan ngay trong turn. Lộ trình spec §1b có
dòng "Review sâu (`tdq-reviewer`) | CÓ" nên gọi agent review spec+plan trước build. Agent trả
**"Không sound — 17 finding"** (3 P0, 9 P1, 5 P2). Đã tự xác minh từng finding bằng đọc code /
chạy lệnh trước khi áp — không nhận kết quả agent nguyên xi.

**Xác minh (kết quả thật, không suy đoán):**
- `render_phases_md()` (`tdq_state.py:565`) đổ cả `action`/`done_when`/`forbidden`/`cmd` VÀ mục
  `## quick` checklist ra doc → `skills/tdq-conventions/references/phases.md` là **nguồn sự thật
  thứ 5**, phải sinh lại bằng `phases-doc`, không sửa tay. Bản plan cũ thiếu hẳn nguồn này.
- `_cli_approve` in `✅` bằng `print()` ra **stdout KHÔNG timestamp** (`tdq_state.py:956`);
  timestamp chỉ có ở `_info`/`_warn` → **stderr**. Luật log của `--no-qc` phải đi qua `_info`.
- `get spec_approved` in `true` (JSON chữ thường), không phải `True` → sửa mọi điều kiện PASS.
- `quick_approved_by` đã tồn tại (`tdq_state.py:63`) → bỏ field trùng `quick_qc_skipped_by`,
  còn đúng 1 field mới `quick_qc_skipped`.
- Mốc suite đo lại: **603 test / 1 failure** (không phải 602/603 như spec 1.0 ghi lẫn).
- `prompt_context.QUESTION` neo `\bkhông\b\s*$` ở CUỐI câu → `"duyệt quick không QC"` không bị
  lọc thành câu hỏi; chỉ phải sửa `APPROVE_HINTS["quick"]` ở `hooks/scripts/_common.py:28`.
- `next` của quick hiện in **14 dòng**, trần `test_next.py:36` là 20 → thêm 3 mục checklist vẫn dư.
- `-k portable` không khớp tên test nào thì unittest in "Ran 0 tests" và **exit 0** = xanh giả
  → đặt tên test tiền tố `test_portable_` và lọc bằng `-k test_portable`.
- 3 lệnh `approve` trong spec §6 thiếu `TDQ_PROJECT_DIR` → sẽ **ghi đè `docs/tdq/state.json`
  thật của chính request này**. Đây là P0 nặng nhất.

**File đổi:**
- `docs/tdq/plan/2026-08-07-siet-qc-lane-quick.md` (mới) — 6 phase, 18 task (thêm T2.5 sinh lại
  2 bản `phases.md`, T3.4 sửa `_common.py`), mode ĐỀ XUẤT `main`, 3 khối hợp đồng 6 trường.
- `docs/tdq/spec/2026-08-07-siet-qc-lane-quick.md` → **bản 1.1**: 4 nguồn → 5 nguồn (thêm N5),
  bỏ field trùng, `--by` bắt buộc khi có `--no-qc`, luật sandbox `TDQ_PROJECT_DIR` cho cả §6,
  `true` thay `True`, `grep -l … | wc -l` = 3 thay `grep -l` trần, thêm `cd tests &&`,
  ≥ 12 test mới (thay ≥ 6), Q3 ≥ 615, thêm 4 dòng rủi ro (quên sinh N5 · thiếu sandbox ·
  R5 câu > 40 từ · trần 20 dòng của `next`).
- `docs/tdq/state.json` — `set plan_file=…` (chỉ qua CLI).

**Test đã chạy:**
- `python3 scripts/doc_lint.py docs/tdq/spec/…` → exit 0.
- `python3 scripts/doc_lint.py --pair <spec> <plan>` → exit 0 (ánh xạ đầu ra + hợp đồng skill).
- Đo mốc suite: `cd tests && python3 -m unittest discover . -q` → 603 test, 1 failure có sẵn.

**Trạng thái:** DỪNG. Spec đã sửa sau khi duyệt nên sha256 lệch → cần user **duyệt lại spec 1.1**
rồi **duyệt plan kèm mode**. Chưa sửa một dòng code sản phẩm nào.

## 17:33

Implement xong 18/18 task P1-P5 lane quick QC: field quick_qc_skipped + cờ approve quick --no-qc (chỉ quick, bắt buộc --by) + log _info có timestamp ra stderr; PHASE_TABLE[quick] action/cmd/checklist/done_when/forbidden; sinh lại 2 bản phases.md bằng phases-doc; luật QC 3 hạng mục + vòng fix trần 3 vòng viết vào 5 nguồn sự thật; hooks/_common.py APPROVE_HINTS[quick] thêm biến thể bỏ QC; bump 0.8.0 -> 0.9.0. Test: test_quick_qc 15/15 OK, full suite 618 test 1 failure có sẵn ngoài phạm vi (test_claude_md_core), doc_lint skills portable exit 0, next quick 16 dòng <= 20.

## 17:45

QC + report: Q1-Q8 PASS (test_quick_qc 15/15, suite 618 test — 1 failure có sẵn ngoài phạm vi, doc_lint 0, grep 5 nguồn khớp, 2 phases.md IDENTICAL với phases-doc, next quick 16 dòng). Agent tdq-qc-tester kiểm độc lập PASS, sửa 1 số liệu Q7 của tôi (--no-qc lặp là idempotent exit 0) — đã đo lại bằng argv thật. 0 vòng fix. Viết report, plan header thành HOÀN THÀNH, bump 0.9.0. CHƯA commit.
