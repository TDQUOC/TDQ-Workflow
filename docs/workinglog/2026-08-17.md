# Working log 2026-08-17

## 09:20

Mở brief 2026-08-17-0919-eta-task-agent: xác định đúng vấn đề là eNm trong plan-template (bỏ điểm nN + thang neo mốc, ETA chỉ tính theo eNm = thời gian agent thực thi)

## 09:21

Viết mini-plan quick 2026-08-17-0919-eta-task-agent: bỏ điểm nN + thang neo mốc, ETA task chỉ tính theo eNm (thời gian agent thực thi)

## 09:23

Bỏ điểm độ phức tạp (nN) + thang neo mốc khỏi plan-template.md, tdq-plan/SKILL.md, tdq-build/SKILL.md; ETA task giờ chỉ tính theo (eNm) = ước tính phút Claude tự thực thi task (thời gian agent, không phải người chờ). QC 3/3 PASS.

## 09:38

Mở brief 2026-08-17-0938-portable-codex: phân tích khả thi portable codex (mcp/hook/skill/rule) ra folder portable_codex; đang hỏi lane

## 09:39

Bổ sung brief 2026-08-17-0938-portable-codex: chốt 'codex' = chính TDQWorkflow, mục tiêu đóng gói portable_codex/ tự chứa để move sang project/máy khác

## 10:11

Brief portable-codex: xong khảo sát 3 nhánh (cơ chế đóng gói hiện có, phụ thuộc đường dẫn, cơ chế project-level Claude Code); user chọn phương án C + skill tdq-checkportable tự setup; đang hỏi làm rõ target harness/quyền setup/manifest + đề xuất chuyển lane deep

## 10:16

Chuyển lane full (user chốt 1A 2C 3B 4A); hoàn thiện brief phase analyze; viết spec 2026-08-17-0938-portable-codex: 2 bản portable tự sinh (portable_claude + portable_codex), skill tdq-checkportable tự kiểm & tự setup quyền tối đa, manifest đầy đủ, 10 hạng mục QC. doc_lint exit 0. Chờ duyệt spec.

## 10:21

Viết plan 2026-08-17-0938-portable-codex: 8 phase, 28 task, ~304 phút, mode đề xuất main; doc_lint --pair exit 0. Chờ duyệt plan.

## 10:42

Portable tự sinh: build_portable.py sinh portable_claude/ (.claude/skills+agents+settings hook, .mcp.json không secret, gốc .claude/tdq giữ hooks cạnh scripts) và portable_codex/ (AGENTS.md + workflow/NN-*.md); tdq_checkportable.py check/setup theo manifest sha256 có backup; xoá portable/ viết tay, 4 test khoá bản chép tay chuyển sang trỏ bản sinh; 733 test xanh

## 10:56

QC vòng 1 FAIL 5 lỗi → fix hết: checkportable tự tìm gốc bundle (đi ngược tìm manifest.json), setup sinh lại settings.json/.mcp.json có backup và exit!=0 khi còn hỏng, manifest rỗng bị coi là hỏng, README cho bản codex, bỏ lời hứa tự-cài-gói khỏi tài liệu. 738 test xanh

## 11:06

QC vòng 2: 5 lỗi vòng 1 PASS hết; fix tiếp 5 điểm nhỏ — to_ten_khoa có caller thật (check in trạng thái biến MCP, chỉ tên), setup bắt OSError khi bundle chỉ đọc, docstring bỏ lời hứa quá năng lực, settings mất trắng báo rõ khối env, setup chỉ vá file có trong manifest. 743 test xanh

## 11:09

QC vòng 3 PASS toàn bộ, không khuyết tật mới; viết report, đóng sổ plan (28 task + 10 task fix đều [x]). 743 test xanh. Chưa commit.

## 11:43

Đóng sổ request portable-codex (phase=idle, chưa commit). Mở brief mới 2026-08-17-1139-codex-native-layers: nâng portable_codex lên lớp native của Codex CLI (.codex/config.toml cho MCP, .agents/skills/ cho skill tự nạp, hooks.json), sửa giả định sai trong build_portable.py. Đang chờ user chọn pipeline.

## 11:47

Analyze 2026-08-17-1139-codex-native-layers: init state lane full, B0 kiểm kê năng lực, đọc code (build_portable.sinh_ban_codex, hooks/hooks.json 5 hook, _common.py ../../scripts, ràng buộc test_setup_khong_them_file_la, kien-truc.md còn nhắc portable/ đã xoá). Giao subagent research xác minh tài liệu Codex CLI, đang chạy.

## 11:55

Analyze: 2 vòng research Codex CLI (vòng 2 đọc thẳng source codex-rs/hooks). Chốt: Codex CÓ hook đủ 10 event, KHÔNG có feature flag, giao thức stdin/stdout TRÙNG Claude Code, áp mọi tool -> cả 5 hook TDQ ánh xạ được. MCP + skill .agents/skills xác nhận. Ràng buộc nặng: project untrusted bị bỏ qua toàn bộ lớp .codex/. Bỏ vòng scope (có lý do), trình 5 câu hỏi chi tiết.

## 12:01

Ghi nhận trả lời interview 1a/2a/3b/4a vào brief, thêm Lộ trình + kiểm cổng. Viết spec 2026-08-17-1139-codex-native-layers (9 đầu ra, 6 rủi ro, 10 hạng mục QC gồm Q9 chạy codex thật), doc_lint exit 0, đăng ký spec_file. Chờ user duyệt.

## 12:22

User duyệt spec. Viết plan 2026-08-17-1139-codex-native-layers: 5 phase, 18 task, ETA 262 phút, mode đề xuất main. P1 trinh sát bằng chạy codex thật (giải R2 cwd+matcher) trước khi viết cấu hình. doc_lint --pair exit 0. Chờ user duyệt plan.

## 12:46

Implement P1: T1.1 xác minh CODEX_HOME (pass), T1.2 viết tests/probe_codex_hook.py (pass). T1.3 đang chạy: phát hiện Codex gác hook bằng TRUST HASH (hooks.state.trusted_hash, duyệt trong TUI) — hook im lặng không chạy cho tới khi được trust; tìm ra cờ --dangerously-bypass-hook-trust để kiểm. Đo được: cwd tiến trình hook = gốc project (đường dẫn tương đối dùng được), tool_name shell = Bash, tool_input.command giống Claude Code. Đang đo tên tool sửa file.

## 14:05

codex-native-layers P1-P5: trinh sát chốt R2 (cwd hook = gốc project → hooks.json dùng đường dẫn tương đối; tool thật = Bash + apply_patch). Sinh 3 lớp native cho portable_codex (.agents/skills 8 skill, .codex/config.toml 2 MCP dùng env_vars, .codex/hooks.json + cây hooks ở gốc bundle) + adapter codex_edit_gate.py chỉ có trong bundle (edit_gate.py của repo không sửa). Thêm setup --trust ghi [projects] trust_level vào config.toml của Codex, có backup .tdq-bak, không cờ thì không chạm ~. Viết lại README/AGENTS.md thành 4 việc thủ công (thêm duyệt hook trong TUI), sửa docs/kien-truc.md. Q9a PASS bằng codex thật: 8 skill được nạp, 4 event hook nổ, mcp list thấy 2 server, sổ turn ghi đúng path từ apply_patch. Test 764 passed.

## 14:13

Đóng sổ request codex-native-layers: QC độc lập PASS 11/11 hạng mục 0 defect (764 test pass), viết report, plan HOÀN THÀNH 18/18 task. Chưa commit.

## 14:30

Viết hướng dẫn cài đặt + trust vào README của cả hai bundle (sửa build_portable.py rồi sinh lại); thêm 3 test khoá đường dẫn lệnh trong README. 767 test pass.

## 15:09

Bump 0.24.0 (plugin.json + CHANGELOG), sinh lại hai bundle, commit a107c55 gộp hai request portable-codex + codex-native-layers. Chưa push.
