# Working log — 2026-08-02

## 11:31 — Mở request tdq-default-cleanup
- Ngữ cảnh: user muốn TDQ workflow là default bắt buộc (siết CLAUDE.md §10 + skill tdq-intake), bỏ §5 superpower khỏi CLAUDE.md user-level.
- File đổi: tạo `docs/tdq/requests/2026-08-02-tdq-default-cleanup.md`.
- Lý do: intake TDQ — ghi request trước khi hỏi lane.
- Test: chưa chạy (mới ghi request, chờ user chốt lane + 2 câu hỏi phạm vi).

## 11:36 — Analyze xong tdq-default-cleanup (lane full)
- Ngữ cảnh: user chọn lane full; interview 3 câu → chốt: mọi prompt qua intake, bỏ trọn §5, thêm hook enforce.
- File đổi: init state (tdq_state.py), research/questions/knowledge `2026-08-02-tdq-default-cleanup.md`.
- Lý do: phase analyze của tdq-intake; research tavily 2 truy vấn (hook deterministic vs instruction, description trigger).
- Test: chưa chạy (chưa sửa code); phase → spec, spec sẽ lập ở turn mới.

## 11:47 — Spec v1.1 tdq-default-cleanup
- Ngữ cảnh: phase spec; viết spec 3 tầng enforce + xóa §5, tdq-reviewer trả 8 findings → áp dụng hết (định nghĩa "request mở", Q5 tách 3 kỳ vọng, wording [TDQ:INTAKE] ≤160 ký tự, mapping số mục 1b, backup CLAUDE.md, chốt tests/test_prompt_context.py).
- File đổi: docs/tdq/spec/2026-08-02-tdq-default-cleanup.md (v1.1), state spec_file.
- Test: doc_lint spec exit 0. Chờ user duyệt spec.

## 11:52 — Plan tdq-default-cleanup trình duyệt
- Ngữ cảnh: phase plan; user chọn mode main qua AskUserQuestion; reviewer trả 7 findings → áp 6 (backup .bak + sha256, T1.3 đủ 4 case, red-condition nới case d, assert MAX_CHARS, grep scope đóng cho T2.4).
- File đổi: docs/tdq/plan/2026-08-02-tdq-default-cleanup.md, state plan_file.
- Test: doc_lint --pair spec/plan exit 0. Chờ user duyệt plan.

## 12:01 — Build + QC + report tdq-default-cleanup (HOÀN THÀNH)
- Ngữ cảnh: user duyệt plan mode main; build end-to-end 1 turn (T1.1→T4.3 + QC vòng 1).
- File đổi: tests/test_prompt_context.py (mới), hooks/scripts/prompt_context.py ([TDQ:INTAKE], dùng phase_key), ~/.claude/CLAUDE.md (xóa §5, đánh số 1..10, luật MỌI prompt mới), backup .bak, skills/tdq-intake/SKILL.md (description + định nghĩa request mở), tests/test_context_hooks.py (2 test cập nhật hợp đồng), knowledge/spec sửa tham chiếu cũ, qc + report của request.
- Lý do: enforce TDQ default 3 tầng theo spec 1.1; QC vòng 1 bắt bug hook so phase thô làm lane quick mất APPROVE → fix phase_key.
- Test: suite 371 OK; doc_lint SKILL.md + --pair exit 0; hook chạy tay 4 case đúng Q5; graphify extract xong (4 file re-extracted).

## 13:05 — Mở request fix-approve-hint-mode
- Ngữ cảnh: user báo plan chốt external nhưng dòng duyệt vẫn gợi ý "duyệt plan mode main".
- File đổi: tạo `docs/tdq/requests/2026-08-02-fix-approve-hint-mode.md`.
- Lý do: intake TDQ; detect sơ bộ — chuỗi hint hardcode "main" ở _common.py:24, tdq-plan SKILL.md:48, portable/03-plan.md:43.
- Test: chưa chạy (đang chờ user chốt lane).

## 13:22 — Quick approved: fix-approve-hint-mode (mini-plan)
- User duyệt "duyêttj quick main" (typo, nghĩa rõ: duyệt quick, tự làm).
- Mini-plan: (1) `_common.py` approve_hint(target, mode); (2) `prompt_context.py` đọc `Mode thực thi:` từ plan_file, hint động + cảnh báo mode lệch; (3) sửa dòng duyệt trong tdq-plan SKILL.md + portable/03-plan.md; (4) test mới test_context_hooks; (5) toàn suite + doc_lint + graphify.
- Năng lực: doc_lint (script), unittest suite.

## 13:30 — Fix-approve-hint-mode HOÀN THÀNH (quick)
- File đổi: `hooks/scripts/_common.py` (plan_mode() + approve_hint(target, mode), hint template động), `hooks/scripts/prompt_context.py` (hint theo mode plan, cảnh báo ⚠️ mode lệch không tự approve, dòng APPROVE đứng trước NEXT chống _truncate cắt cụt hint — bug UX có sẵn), `skills/tdq-plan/SKILL.md` + `portable/workflow/03-plan.md` (bỏ hardcode "mode main"), `tests/test_context_hooks.py` (+4 test red→green).
- Test: suite 375 OK; doc_lint tdq-plan SKILL.md exit 0; chạy tay hook: hint in "duyệt plan mode external", mismatch in cảnh báo ⚠️; grep hardcode = 0; graphify extract xong.

## 13:32 — Mở request auto-pick-recommended
- Ngữ cảnh: user muốn khi gặp chặn kỹ thuật giữa build, Claude tự chọn phương án đề xuất thay vì dừng hỏi.
- File đổi: tạo `docs/tdq/requests/2026-08-02-auto-pick-recommended.md`.
- Test: chưa chạy (chờ user chốt lane + 1 câu hỏi ranh giới commit).

## 13:42 — Quick approved: auto-pick-recommended (mini-plan)
- User chốt: lane quick, ranh giới (b) — giữa build được tự commit gỡ chặn (không push).
- Mini-plan: (1) tdq-build SKILL.md thêm luật tự chọn đề xuất khi chặn kỹ thuật + được commit gỡ chặn; (2) portable build đồng bộ; (3) ~/.claude/CLAUDE.md §2 thêm ngoại lệ commit gỡ chặn; (4) doc_lint + grep + toàn suite + graphify.
- Năng lực: doc_lint (script), unittest suite.

## 13:48 — Auto-pick-recommended HOÀN THÀNH (quick)
- File đổi: `skills/tdq-build/SKILL.md` + `portable/workflow/04-build.md` (Luật cứng mới "Chặn kỹ thuật → tự chọn đề xuất, không hỏi" + được tự commit gỡ chặn không push, bước 10 thêm ngoại lệ tương ứng), `~/.claude/CLAUDE.md` §2 (ngoại lệ commit gỡ chặn khi build TDQ).
- Test: doc_lint 2 file exit 0; grep luật mới có ở cả 3 file; toàn suite 375 OK; graphify extract xong (0 file đổi code).

## 13:50 — Mở request check-version-sync
- Ngữ cảnh: user hỏi đã bump version plugin + đồng bộ repo ↔ registry user-level chưa.
- File đổi: tạo `docs/tdq/requests/2026-08-02-check-version-sync.md`.
- Test: chưa chạy (chờ user chốt lane).

## 13:55 — Quick approved: check-version-sync (mini-plan)
- User duyệt "duyệt quick". Mini-plan: bump plugin.json 0.6.2 → commit 3 cụm (core / docs tdq / graphify-out) → `claude plugin update tdq-workflow@tdq-local` → verify registry + git status sạch.
- Năng lực: không có.
