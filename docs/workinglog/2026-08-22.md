# Working log 2026-08-22

## 03:02

T9.2 xong: full suite 37 lỗi = đúng mốc test_skill_router. Sửa lưới mục lục thành nhận biết khối ``` (khuôn trong fence không tính là tiêu đề file), thêm mục lục thật cho plan-template + spec-template; nới trần description 1080→1450 ký tự (đo token thật: 628→304) và trần dòng reference 200→215; SO_KHOI quick-lane 1→2; needle song ngữ cho test_user_facing_block; làm mới số dòng luat-hien-co.md (29 dòng). Sinh lại portable, checkportable CLEAN cả hai.

## 03:08

QC + report: 17/18 mục PASS (Q6 pass sau fix QC1.1 dịch nốt chú thích doc_lint.py), Q11b FAIL vì chưa chạy lại evals/tuan-thu (cần user duyệt ~70 USD). Full suite 37 failed/1198 passed = đúng mốc. checkportable CLEAN 84+129. Report ghi ở docs/tdq/reports/.

## 09:00

Pump version 0.26.0 → 0.27.0: plugin.json + mục CHANGELOG 0.27.0 (gộp 6 commit chưa phát hành + request quốc tế hoá). Sinh lại 2 bản portable, checkportable CLEAN 84+129, full suite 37 failed/1198 passed = đúng mốc. Commit toàn bộ theo yêu cầu user.

## 11:13

Implement xong 14/14 task request quan-ly-worktree: thêm scripts/tdq_worktree_registry.py (sổ worktree 7 trường + render docs/tdq/worktrees.md + bảng lý do chặn đóng), tdq_team.py ghi sổ khi mo / dọn worktree+nhánh sau hop khi đủ 3 điều kiện (sạch · đã merge · không khoá) / lệnh mới soat và soat --don quét mọi request, tdq_state.py chặn set phase=qc khi sổ còn dòng mở, prompt_context.py in [TDQ:WORKTREE] mỗi turn khi còn worktree bẩn, team-mode.md thêm mục sổ + luật chép khối gợi ý xuống cuối turn, dựng lại 2 cây portable (85/130 file, CLEAN). QUYẾT ĐỊNH LỆCH SPEC §4: docs/tdq/worktrees.md render bằng TIẾNG ANH thay vì theo doc_lang, theo tiền lệ sổ sinh tự động docs/tdq/STATE.md — sổ sống xuyên request nên không có doc_lang riêng, và làm theo doc_lang thì phải dựng bảng dịch, đúng thứ quyết định 0.27.0 đã loại. Full suite: 37 đỏ (đúng mốc nền, toàn bộ trong tests/test_skill_router.py), 1246 xanh.

## 11:52

QC request quan-ly-worktree: vòng fix 1 (F1-F10, gồm gitignore sổ worktree + luật dịch khối NOT CLEANED UP YET sang doc_lang) rồi agent QC vòng 2 trả PASS cho Q1-Q20 kèm 5 khiếm khuyết mới; vòng fix 2 (F11-F14): worktree bị lock không còn làm chết soat/don, rác ignored có lý do chặn riêng bo-qua với phương án gỡ được thật, dòng lạ kiểm đủ điều kiện cấp thư mục, unittest.main() xuống cuối file test. Test: team_mode 142 passed, full suite 37 đỏ đúng mốc nền / 1257 passed, portable CLEAN 85+130, i18n 0 dòng.

## 12:04

Vòng fix 3 (cuối): lời từ chối của git có lý do riêng git-tu-choi, nhãn khoa chỉ dùng khi worktree thật sự bị lock (F15) + 2 test hồi quy. QC độc lập vòng 2 trả PASS cho cả bảng DoD lẫn vòng fix 2. Viết report, plan sang HOÀN THÀNH, 29/29 task tick. Test: team_mode 144 passed (pytest = chạy trực tiếp), full suite 37 đỏ đúng mốc nền / 1259 passed, portable CLEAN 85+130.
