# Working log 2026-07-29

## ~00:05 — User duyệt spec 0.3.0 → viết plan
- `tdq_state.py approve spec --by "duyệt spec"` → `spec_approved=true`; `set phase=plan`.
- User chốt: mode thực thi **main** (tôi tự làm tuần tự, không spawn subagent), độ mịn **mỗi task 1 test red→green**.
- File mới: `docs/tdq/plan/2026-07-28-instruction-hardening-7b.md` — 34 task / 7 phase + QC, theo thứ tự phụ thuộc: P1 CLI (state S1–S8, STATE.md mirror, PHASE_TABLE, next/--brief, get <key>, log service) → P2 hook (sổ turn .tdq-turn.jsonl, 5 mã nhắc + dedupe, observe ở edit/bash gate, stop_gate đối chiếu bằng hiệu ứng, xoá dấu vết transcript, resilience) → P3 skills 9→5 + references → P4 portable → P5 doc_lint R1–R7 + test ngân sách token → P6 dọn dẹp T2–T4/C1–C5/D1–D2 → P7 đóng gói 0.3.0 + CLAUDE.md §10 → P8 QC Q1–Q15 + report.
- Đăng ký `plan_file` vào state. Chưa đụng code; plan chờ duyệt.

## ~01:00–01:40 — Implement plan 0.3.0 end-to-end (P3 → P8)
- **P3 skills 9→5**: hoàn tất `skills/tdq-{intake,spec,plan,build,status,conventions}/` + `references/`; `git rm` 6 skill cũ; sửa `agents/tdq-implementer.md` (trỏ `tdq-build`) và viết lại `README.md` theo 0.3.0 → `test_skill_shape` 10/10 xanh. Tick T3.1–T3.7.
- **P4 portable**: thêm `portable/AGENTS.md`, `portable/workflow/{01-intake,02-spec,03-plan,04-build}.md`, `portable/workflow/references/*` (copy từ skills), `portable/README.md`; test mới `tests/test_portable_sync.py` (4 test) so danh sách bước skill ↔ portable sau chuẩn hoá. Sửa `skills/tdq-intake` bước C4 dùng lệnh đầy đủ thay vì `...`. Tick T4.1–T4.4.
- **P5 lint + ngân sách**: thêm `scripts/doc_lint.py` (R1–R7, hỗ trợ `<!-- doc-lint: allow Rn -->`), `tests/test_doc_lint.py` (10 test, mỗi rule 1 fixture bẩn + 1 sạch), `tests/test_token_budget.py` (8 mục §2.7, đo trên cả 6 phase). Sửa `render_phases_md` bọc lệnh trong inline-code để qua R2; `test_skill_shape` dùng chung hằng `SKILL_LINE_LIMITS` của doc_lint. Tick T5.1–T5.8.
- **P6 dọn dẹp**: viết lại `docs/notes/user-level-install.md` (§3 block instruction 0.3.0, mục 4 portable, lưu ý an toàn); `.gitignore` bỏ `docs/tdq/state.json`, thêm `docs/tdq/.tdq-turn.jsonl`; thêm `CHANGELOG.md`; chuyển `idea.md` + `docs/{spec,plan,qc,reports}` v0.1 → `docs/archive/v0.1/` + README; xoá `docs/.DS_Store`; thêm `tests/test_docs_consistency.py` (5 test). Tick T6.1–T6.5.
- **P7 đóng gói**: `plugin.json` → 0.3.0 + description mới; `marketplace.json` bỏ "gate duyệt cứng"; `claude plugin validate . --strict` PASS; gỡ/cài lại plugin user-level để cache lấy đúng 0.3.0. Tick T7.1, T7.3. **T7.2 (`~/.claude/CLAUDE.md` §10) chưa làm — chờ user đồng ý nội dung.**
- **P8 QC**: chạy Q1–Q15; phát hiện **QC1.1** (`next` in `phase idle` cho lane quick trong khi thân bài dùng row `quick`) → thêm task fix vào plan, viết test red rồi sửa `next_headline` dùng `phase_key` → green. Kết quả: `python3 -m unittest discover tests` → **Ran 162 tests, OK**; `doc_lint.py skills portable` exit 0; smoke bản cài (a–d) đúng kỳ vọng.
- Doc mới: `docs/tdq/qc/2026-07-28-instruction-hardening-7b.md`, `docs/tdq/reports/2026-07-28-instruction-hardening-7b.md` (50 dòng). Plan chuyển trạng thái HOÀN THÀNH (còn T7.2).
- `graphify extract . --code-only` đã chạy lại. Chưa commit — chờ user.
- Phát hiện lúc kết turn: Stop hook báo "chưa append working log" dù log ĐÃ được append — vì entry viết bằng `cat >>` trong Bash, mà `log_written` chỉ được ghi vào sổ turn từ `edit_gate` (PreToolUse của Edit/Write). Đã append lại bằng Edit để sổ turn ghi nhận. Đây là điểm mù thật của thiết kế verify-by-effect, cần task fix riêng.

## ~02:10 — Phân tích + viết spec fix điểm mù verify-by-effect
- File mới: `docs/tdq/requests/2026-07-29-turn-effect-blindspot.md`, `docs/tdq/spec/2026-07-29-turn-effect-blindspot.md`.
- Chốt nguyên nhân gốc: sổ turn chỉ nhận `observe` từ tên tool (Edit/Write, Bash-state-cli) chứ không nhìn hiệu ứng trên đĩa → sai **hai chiều**: chặn oan khi log ghi qua shell, và bỏ lọt khi repo bị sửa hoàn toàn bằng shell.
- Phương án chọn: **B — chụp trạng thái đầu turn (`prompt_context`) rồi so lúc Stop** (`sha256` log hôm nay + digest `git status --porcelain`), thay vì bắt regex lệnh bash (PA A vừa không đủ vừa dễ cấp `log_written` giả).
- Chưa đụng code, chưa `init` state cho request mới (sẽ xoá state của `2026-07-28-instruction-hardening-7b`) — chờ user duyệt spec.

## ~02:30 — User duyệt spec → viết plan
- User nhắn "duyệt spec". File mới: `docs/tdq/plan/2026-07-29-turn-effect-blindspot.md` — 22 task / 5 phase (P1 helper `repo_status_digest`/`turn_snapshot` → P2 `prompt_context` ghi `turn_start` → P3 `stop_gate` đối chiếu 2 chiều + resilience → P4 doc/đóng gói 0.3.1 → P5 QC + report).
- Chưa ghi state (`spec_approved`/`plan_file`) vì state vẫn trỏ request cũ; `init` cho request mới sẽ xoá state đó nên đang chờ user quyết. Chưa đụng code.

## ~02:45–03:30 — Implement plan 0.3.1 end-to-end (mode main)
- **P1** `scripts/tdq_state.py`: thêm `today_log_rel`, `repo_status_digest`, `repo_status_paths`, `turn_snapshot`; test mới `tests/test_turn_snapshot.py` (11 test). Vân tay repo gồm `status --porcelain -uall` + `diff HEAD` + `size:mtime` file untracked.
- **P2** `hooks/scripts/prompt_context.py` ghi 1 dòng `turn_start` vào sổ turn ngay sau `turn_log_clear` (không in ra context → không tốn token); 3 test mới trong `test_turn_ledger.py`; sửa 2 test cũ vốn assert sổ turn rỗng.
- **P3** `hooks/scripts/stop_gate.py`: `logged` = sổ turn **hoặc** log đã đổi trên đĩa; `edited` = sổ turn **hoặc** vân tay repo đổi; loại trừ `docs/tdq/`; nêu tên file ưu tiên file mới xuất hiện; fallback khi thiếu `turn_start`/không phải git; 11 test mới. `edit_gate` dùng chung `today_log_rel`.
- **P4** doc: `reminder-codes.md` (skills + portable đồng bộ), README, `CHANGELOG.md` mục 0.3.1, `plugin.json` → 0.3.1, `plugin validate --strict` PASS, cài lại plugin user-level.
- **P5** QC: **187 test OK**, `doc_lint skills portable` exit 0. Smoke phát hiện **QC1.1** — sửa nội dung file *untracked* không đổi `porcelain` nên vẫn bỏ lọt → thêm task fix vào plan, băm thêm `size:mtime_ns`, red → green. Smoke lại (a) log bằng `cat >>` không chặn / (b) `sed -i` file tracked bị chặn / (c) sửa file untracked bị chặn. `repo_status_digest` mất 32 ms trên repo này.
- Doc mới: `docs/tdq/qc/2026-07-29-turn-effect-blindspot.md`, `docs/tdq/reports/2026-07-29-turn-effect-blindspot.md`. Plan tick đủ 22/22 task, trạng thái HOÀN THÀNH.
- `graphify extract . --code-only` đã chạy lại. Chưa commit — chờ user. **Cần restart Claude Code** để hook 0.3.1 có hiệu lực.

## ~04:00 — Audit toàn bộ tdq-workflow 0.3.1 (theo yêu cầu user)
- Đối chiếu request → spec → code cuối + dựng sandbox kiểm chứng. Kết luận: mục tiêu gốc đạt, nhưng 0.3.1 **đẻ ra chặn oan mới nặng hơn lỗi nó vá**.
- **A (nặng)**: `_repo_changed()` so vân tay TOÀN repo, còn vùng loại trừ `docs/tdq` chỉ áp cho `_shell_changed_path()` (đặt tên). Chính hook append `.tdq-turn.jsonl` SAU khi chụp baseline → vân tay luôn đổi → mọi file bẩn cũ bị lôi ra làm vật tế thần. Bằng chứng: turn **read-only** (chỉ chạy `next`) và turn **chỉ ghi state** đều bị `block` với `(a.py)`.
- **B**: `touch` file untracked (nội dung y nguyên) → chặn oan, do QC1.1 băm `size:mtime_ns`.
- **C**: `BOOKKEEPING` dùng `os.path.join` → `docs\tdq` trên Windows, git luôn in `/` ⇒ loại trừ im lặng chết. **D**: hook không có một dòng log nào (trái §6). **E**: không có `SubagentStop`. **F**: `UNTRACKED_STAT_CAP` cắt theo dòng status chứ không theo số file; `repo_paths` cap 100 < 110 path bẩn của repo. **G**: `_snapshot` lấy dòng `turn_start` đầu tiên. **H**: `stop_hook_active` ⇒ gate là nhắc-một-lần.
- Đo hiệu năng: `turn_snapshot` 42 ms (`status -uall` 10.5 + `diff HEAD` 34), dòng `turn_start` 4 KB, không tốn token model.
- Chưa sửa gì trong turn audit; trình 5 đề xuất fix cho user.

## ~04:15 — User duyệt fix 0.3.2 → plan
- User nhắn "duyệt hãy sửa theo đề xuất giúp tôi". File mới: `docs/tdq/plan/2026-07-29-false-block-0.3.2.md` — 15 task / 5 phase: P1 helper (loại trừ bằng **git pathspec** `:(top,exclude)`, băm nội dung file untracked ≤256 KB, tiền tố `/` cứng, cap đếm file, stat theo repo root) → P2 `stop_gate` (`turn_start` cuối cùng, không chặn turn read-only/state-only) → P3 log service cho hook → P4 doc + 0.3.2 → P5 QC.
- **E (`SubagentStop`) cố ý ngoài phạm vi**: subagent không phải nơi ghi working log, thêm gate ở đó là tạo chặn oan mới; Stop của phiên cha đã bắt được file subagent ghi.
- Chưa ghi state (vẫn trỏ request cũ `2026-07-28-instruction-hardening-7b`) — `init` sẽ xoá state đó nên chờ user quyết.

## ~04:20–05:00 — Implement 0.3.2 end-to-end (mode main)
- **P1** `scripts/tdq_state.py`: thêm `BOOKKEEPING_PATHS`/`_EXCLUDE` (pathspec `:(top,exclude)`, áp cho cả `status` lẫn `diff HEAD` trong `repo_status_digest` + `repo_status_paths`), `repo_root()` (cache trong process), `_untracked_mark()` (băm nội dung ≤256 KB, ngân sách 4 MB, vượt trần mới dùng `size:mtime`); cap đếm số file untracked; `repo_status_paths` limit 100 → 400. 11 test mới trong `test_turn_snapshot.py` (red 8 fail + 2 error → green 22/22).
- **P2** `hooks/scripts/stop_gate.py`: `_snapshot` lấy dòng `turn_start` **cuối cùng**; `BOOKKEEPING` lấy từ `tdq_state` và khớp theo `/`. 6 test mới (`TestStopGateNoFalseBlock`), đổi `test_first_turn_start_row_wins` → `test_last_turn_start_row_wins` (đảo kỳ vọng: baseline cũ phải bị bỏ qua nên turn này vẫn bị chặn).
- **P3** log service: `_git` cảnh báo khi timeout / không chạy được (rc≠0 im vì đó là "không phải repo"); `stop_gate` `_info` mỗi quyết định chặn (nguồn + path) và `_warn` khi cuối turn mất vân tay. `tests/helper.py::run_hook` nhận `env=`. Sửa `test_malformed_turn_start_row` từ `err == ""` sang `not in Traceback`.
- **P4** doc: `reminder-codes.md` (skills + portable đồng bộ, `doc_lint` exit 0), `CHANGELOG.md` mục 0.3.2, `plugin.json` → 0.3.2, `user-level-install.md` khuyến nghị `.gitignore` sổ turn; `plugin validate --strict` PASS; gỡ + cài lại plugin user-level (cache 0.3.2).
- **P5 QC 10/10**: `unittest discover tests` → **204 test OK**. Smoke trên bản cài: A1 (read-only) / A2 (chỉ ghi state) / B (touch untracked) → **hết chặn oan**; (a) `cat >>` không chặn, (b) `sed -i` tracked chặn, (c) sửa untracked chặn → giữ nguyên. Shim `git` treo 5 s → 2 dòng ⚠️ có timestamp, rc=0, không crash. Vân tay repo 32 → 53 ms.
- Doc mới: `docs/tdq/qc/2026-07-29-false-block-0.3.2.md`, `docs/tdq/reports/2026-07-29-false-block-0.3.2.md` (≤50 dòng). Plan tick đủ 15/15.
- **Cố ý không làm**: hook `SubagentStop` — subagent không phải nơi ghi working log, gắn gate ở đó là tạo thêm chặn oan; Stop của phiên cha đã bắt được file subagent ghi.
- `graphify extract . --code-only` đã chạy. Chưa commit — chờ user. **Cần restart Claude Code** để hook 0.3.2 có hiệu lực.

## ~05:30 — Audit: workflow có tận dụng skill phụ trợ không?
- Kết luận: **không kiểm kê**, nhưng cũng **không chặn** (`hooks.json` chỉ match `Edit|Write|MultiEdit|NotebookEdit` + `Bash`, tool `Skill` không bị gate). Năng lực ngoài duy nhất được hard-code: tavily (`tdq-conventions:82`), graphify (`tdq-build:38,67`), 3 agent nội bộ.
- Đo thật: prototype quét đĩa ra **7 skill**, context có **18** → skill built-in không nằm trên đĩa (`find / -maxdepth 8 -name artifact-design -type d` → rỗng). `find ~/.claude -name SKILL.md` ra **152** file vì cache giữ mọi version. `installed_plugins.json` có entry `scope: project` của project khác (`superpowers`) → phải lọc `projectPath`.
- Chỉ đọc/đo, không đổi repo trong turn phân tích.

## ~05:50 — User chọn lane full → viết spec 0.3.3
- File mới: `docs/tdq/requests/2026-07-29-skill-inventory.md`, `docs/tdq/spec/2026-07-29-skill-inventory.md` (138 dòng, 12 đầu ra, Q1–Q12).
- Thiết kế chốt: **hai nguồn** kiểm kê (script lo đĩa + model chép built-in từ context); quy tắc 1% mã hoá thành *xét 100% bắt buộc · loại chỉ bằng 4 lý do đóng · phân vân thì DÙNG*; cưỡng chế bằng `doc_lint` **R8** thay vì thêm mã hook thứ 6 (giữ trần token).
- Phát hiện lúc tự review: `doc_lint` hiện chỉ chạy trên `skills/`+`portable/`; R8 phải soi `docs/tdq/spec/` → lệnh QC đổi thành `doc_lint.py skills portable docs/tdq/spec`, và 4 spec cũ được miễn trừ bằng cơ chế `<!-- doc-lint: allow R8 -->` sẵn có (thêm đầu ra 8b).
- Spec tự áp khuôn mới: có sẵn mục §3b, trong đó `security-review` được đánh **DÙNG** cho QC Q9.
- **Chưa** chạy `set spec_file=...`: state vẫn trỏ request cũ `2026-07-28-instruction-hardening-7b`, `init` sẽ xoá state đó nên chờ user quyết. Chưa viết plan (spec và plan không cùng turn). Chưa đụng code.

## ~06:05 — Góp ý của user → spec bản 1.1
- User: "nếu xác định sẽ dùng skill đó thì quy rõ trong plan implement luôn là dùng skill đó thế nào, không chỉ ghi sơ sơ rồi implement mù".
- Sửa `docs/tdq/spec/2026-07-29-skill-inventory.md` → **Bản 1.1** (138 → 177 dòng): `DÙNG` trở thành **hợp đồng 6 trường** ở mức task (`Dùng / Nạp / Để / Ra / Kiểm / Không dùng cho`) kèm bảng "Năng lực → task"; thêm §3 điểm 4 với khối mẫu copy-paste.
- Đầu ra 12 → 13 (tách 6 = khuôn hợp đồng, 6b = `doc_lint.py --pair <spec> <plan>` đối chiếu hai file). QC Q1–Q12 → Q1–Q14 (Q13 khớp hợp đồng, Q14 chạy thật trường **Kiểm** của từng khối). Thêm 2 dòng rủi ro: plan phình, và điền hợp đồng cho có.
- Luật chống "ghi cho có": trường **Ra** phải là artifact tồn tại được; không có artifact thì buộc sửa §3b thành `KHÔNG — <lý do>`, cấm để lửng.
- Vẫn chưa viết plan, chưa đụng code, chưa ghi state.

## ~06:15 — User duyệt spec 1.1 → viết plan 0.3.3
- User nhắn "duyệt spec". File mới: `docs/tdq/plan/2026-07-29-skill-inventory.md` — 19 task / 9 phase: P1 script (7 task: 3 tầng settings, lọc `scope: project`, chỉ đọc `installPath`, 2 dòng nhắc built-in, log service) → P2 intake B0 + reference → P3 khuôn §3b + hợp đồng 6 trường → P4 lint R8 + `--pair` + miễn trừ 4 spec cũ → P5 build thi hành hợp đồng → P6 PHASE_TABLE → P7 portable → P8 đóng gói 0.3.3 → P9 QC Q1–Q14.
- Plan dogfood luật mới: có bảng `## Năng lực → task` và khối hợp đồng 6 trường cho `security-review` tại T9.2; Q13 chạy `--pair` trên chính cặp spec/plan này.
- Đề xuất mode **main** (file móc nhau dạng chuỗi, chia worktree phí công merge) — chờ user chốt mode khi duyệt.
- Không ghi được approve/spec_file vào state (state vẫn trỏ request cũ, `init` sẽ xoá — user chưa quyết). Chưa đụng code.

## ~06:30–07:15 — Implement 0.3.3 end-to-end (mode main, user duyệt "duyệt plan mode main")
- **P1** `scripts/skill_inventory.py` (mới) + `tests/test_skill_inventory.py` (11 test, HOME giả): 3 tầng settings, lọc `scope: project` khác `projectPath`, chỉ đọc `installPath`, 2 dòng nhắc built-in nguyên văn, log service `TDQ_LOG`. Red 8 fail → green; repo thật ra đúng 7 skill.
- **P2** intake: bước B0 mới (Phần B đánh số lại 1–6), lane quick thêm dòng `Năng lực:`; reference mới `skills/tdq-intake/references/skill-inventory.md` (khuôn bảng, 3 phán quyết `DÙNG/KHÔNG/NỀN`, 4 lý do đóng, phân vân → DÙNG).
- **P3** `spec-template.md` thêm §3b; `plan-template.md` thêm bảng `Năng lực → task` + khối hợp đồng 6 trường; `tdq-plan` bước 3 thêm luật ánh xạ DÙNG → khối (fix 1 câu 48 từ vì R5).
- **P4** `doc_lint.py`: rule **R8** (chỉ soi thư mục `spec/`; miễn trừ mức file bằng allow-comment) + **`--pair <spec> <plan>`** (thiếu trường nào nêu tên trường đó); 12 test mới (tách `LintBase` để R8Test/PairTest không chạy trùng test cũ); 4 spec cũ thêm `<!-- doc-lint: allow R8 -->`; file trong `spec/` không chịu R1–R7 hồi tố. Dòng tdq-* trong spec 3b đổi DÙNG → NỀN.
- **P5** `tdq-build` vòng lặp task thêm bước nạp skill theo trường `Nạp` trước bước đỏ; `qc.md` mục 6 chạy trường `Kiểm`.
- **P6** PHASE_TABLE: checklist `analyze` +1 mục B0, `quick` sửa mục mini-plan kèm `Năng lực:`; `phases-doc` sinh lại 2 bản. `next` phase analyze 11/20 dòng.
- **P7** portable: 01-intake (B0 + agent ngoài xét công cụ tương đương), 02-spec (§3b + `tương đương:`), 03-plan (luật `--pair`), references đồng bộ (spec/plan-template, qc, skill-inventory bản portable); `test_portable_sync` chuẩn hoá tổng quát `${CLAUDE_PLUGIN_ROOT}/scripts/`; README portable copy thêm 2 script.
- **P8** CHANGELOG 0.3.3, `plugin.json` 0.3.3, validate PASS, gỡ + cài lại plugin (cache chỉ còn 0.3.3).
- **P9 QC 14/14**: 227 test OK · lint + `--pair` dogfood exit 0 · **Q9**: skill `security-review` fail nạp (preamble `git diff origin/HEAD...`, repo không có remote) → rà thủ công 5 hạng mục theo đúng hợp đồng T9.2; phát hiện **QC1.1** ANSI escape từ SKILL.md xấu lọt ra terminal → `_clean()` lọc ký tự <0x20, test red→green, tái diễn 2 kịch bản tấn công đều sạch · **Q10**: khuôn bảng chứa ví dụ thật → đổi placeholder.
- Doc mới: `docs/tdq/{qc,reports}/2026-07-29-skill-inventory.md` (report đúng 50 dòng). Plan tick 20/20, HOÀN THÀNH. `graphify extract . --code-only` đã chạy. Chưa commit — chờ user.
