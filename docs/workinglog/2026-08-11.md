
## 2026-08-11-xoa-nen-video-webm (quick)
- Yêu cầu: xóa nền video hiệu ứng (`okay_vậy_hãy_tạo_video_với_hiệ.mp4`) bằng ffmpeg, xuất
  webm VP8 alpha (yuva420p) cho Unity, qua trung gian chuỗi PNG.
- Test colorkey `0x000000:0.12:0.05` trên frame mẫu → key sạch, giữ glow neon.
- Đang implement: temp_chromakey/frames → encode webm.

- Kết quả: `okay_vậy_hãy_tạo_video_với_hiệ_alpha.webm` (VP8 alpha, 1280x720, 24fps,
  9.96s, ~7.5MB) trên Desktop. QC pass — verify thật bằng Chrome (không dùng ffmpeg
  tự decode vì ffmpeg/mpv có giới hạn đã biết không đọc lại alpha webm). Đúng chuẩn
  Unity VideoPlayer (WebM+VP8 alpha, không transcode, hoạt động mọi Editor platform).
  Đã dọn temp, tắt local server test.

- Follow-up (tầng nhỏ): move video gốc + output vào
  `Desktop/temp_chromakey/`, viết `README.md` chi tiết từng bước ffmpeg + cách QC
  (kèm giải thích quirk ffmpeg/mpv không đọc lại alpha webm, cách verify đúng qua
  Chrome, checklist QC, hướng dẫn dùng trong Unity).

## 09:15 — Mở request fix lỗi import webm Unity 6.3 (Mac)
- Ngữ cảnh: user báo Unity 6.3 (Mac) báo lỗi generic "VideoClip import error" khi
  import `okay_vậy_hãy_tạo_video_với_hiệ_alpha.webm` (output request trước
  [[2026-08-11-xoa-nen-video-webm]]). Đã lấy đúng nguyên văn lỗi từ Console qua
  screenshot user gửi.
- Research (tavily-primary, 4 query): Unity docs xác nhận WebM+VP8 đúng combo, nhưng
  ví dụ ffmpeg chính thức của Unity dùng `-vcodec vp8` +
  `-metadata:s:v:0 alpha_mode="1"` và có audio Vorbis — khác bản gốc của mình (không
  audio). Tìm thấy 1 thread Unity Discussions user khác gặp đúng lỗi này với file
  libvpx+alpha không audio → nghi thiếu audio track là nguyên nhân chính.
  Kiểm tra không có Unity MCP tool trong phiên để tự test import trực tiếp.
- File đổi: tạo brief `docs/tdq/brief/2026-08-11-fix-loi-import-webm-unity.md`, mini-plan
  `docs/tdq/plan/2026-08-11-fix-loi-import-webm-unity.md` (lane quick, `init` xong).
- Đã trình tóm tắt fix (thêm audio Vorbis câm + đúng recipe Unity docs + test tên
  ASCII), đang chờ user duyệt quick.

## 09:20 — Implement fix webm Unity: encode lại có audio, chờ user test import
- Việc: encode 2 bản fix 1-pass thẳng từ mp4 gốc (bỏ qua PNG sequence trung gian đã
  bị dọn), thêm audio (Vorbis câm / Opus từ track gốc) + `-metadata:s:v:0
  alpha_mode="1"` tường minh, theo đúng recipe ffmpeg Unity docs công bố.
- File đổi: `Desktop/temp_chromakey/okay_vậy_hãy_tạo_video_với_hiệ_alpha_fix.webm`,
  `..._fix_opus.webm`, `README.md` (thêm mục "Bản fix cho Unity"),
  `docs/tdq/plan/2026-08-11-fix-loi-import-webm-unity.md` (task + QC).
- Test đã chạy: ffprobe cả 2 file PASS (vp8 + audio hợp lệ + alpha_mode=1).
- Test KHÔNG chạy được: Chrome verify — công cụ `claude-in-chrome` lỗi tầng
  browser/extension trong phiên này (tab crash cả với trang không liên quan), dừng
  theo nguyên tắc tránh rabbit hole, ghi rõ trong README/plan để làm lại sau.
- Housekeeping: xoá thư mục `docs/tdq/` thừa lẫn trong `Desktop/temp_chromakey/`
  (sinh ra do lần init state chạy nhầm cwd trước đó).
- Blocked: chờ user tự import 2 file vào Unity 6.3 (Mac) và báo kết quả — đây là
  phần DoD quan trọng nhất, không có cách tự verify trong phiên.

## 09:30 — QC vòng 2: user báo cả 2 bản fix vẫn lỗi, tìm nguyên nhân mới + encode lại
- Ngữ cảnh: user gửi screenshot Console Unity — cả `_fix.webm` (Vorbis) và
  `_fix_opus.webm` (Opus) đều lỗi. Opus bị Unity từ chối rõ ràng ("Unsupported
  codec 'OPUS'", đúng docs — chỉ hỗ trợ Vorbis). Vorbis vẫn lỗi generic
  `VideoClip import error ... VideoClipImporter.cpp Line: 548`.
- Kết luận: audio không phải nguyên nhân gốc (loại bỏ giả thuyết vòng 1).
- Research thêm (tavily-primary, extract trang Unity Discussions chính thức về
  recipe ffmpeg) → xác nhận recipe tối giản Unity thật ra KHÔNG bắt buộc audio.
  Test lại recipe tối giản → ffmpeg tự báo lỗi thiếu `-auto-alt-ref 0`
  ("Transparency encoding with auto_alt_ref does not work") → xác nhận flag này
  bắt buộc, không phải nguyên nhân.
- Giả thuyết mới: ffmpeg/libvpx có thể bỏ ghi `BlockAdditional` (alpha) ở P-frame
  để tối ưu — Chrome tự suy ra opaque khi thiếu, Unity parser riêng có thể không
  có fallback này. Encode lại ép mọi frame là keyframe (`-g 1 -lag-in-frames 0`):
  `muxing overhead` giảm 101%→92%, cấu trúc đều hơn — củng cố giả thuyết.
- File đổi: `Desktop/temp_chromakey/okay_vậy_hãy_tạo_video_với_hiệ_alpha_v3_allintra_noaudio.webm`
  (ưu tiên test), `..._v4_allintra_audio.webm` (đối chứng), xoá `_fix_opus.webm`
  (biết chắc sai), cập nhật `README.md` (2 mục vòng 1/vòng 2 chi tiết) + plan
  (`## QC vòng 2`).
- Test đã chạy: ffprobe 2 file mới PASS. Chrome verify vẫn không chạy được (tool
  lỗi môi trường từ trước, chưa hồi phục).
- Blocked: chờ user import 2 file vòng 2 vào Unity, báo kết quả. Còn 1 vòng fix
  nữa trước khi phải đề xuất chuyển lane full hoặc phương án dự phòng khác.

## 09:35 — Báo cáo vòng 2 cho user, chờ test
- Ngữ cảnh: sau khi encode 2 file vòng 2 (`_v3_allintra_noaudio.webm`,
  `_v4_allintra_audio.webm`) và cập nhật README/plan, gửi tóm tắt trong chat cho user:
  xác nhận Opus bị từ chối là đúng theo docs, audio không phải nguyên nhân gốc, giả
  thuyết mới (ffmpeg bỏ ghi `BlockAdditional` alpha ở P-frame, Unity parser thiếu
  fallback), 2 file candidate mới, và yêu cầu user import vào Unity 6.3 + báo kết quả
  (nếu vẫn lỗi thì cần `Editor.log` đầy đủ).
- File đổi turn này: không đổi thêm file ngoài đã liệt kê ở mục 09:30 (README.md,
  plan, 2 file webm mới) — turn này chỉ là báo cáo chat + ghi log.
- Trạng thái: vẫn Blocked, chờ user test 2 file vòng 2 trong Unity. Còn 1 vòng fix
  trong trần 3 vòng nếu vòng 2 cũng fail.

## 10:00 — Vòng 2 PASS (import Unity OK), phát sinh viền đen → vòng 3 fix erode alpha
- Ngữ cảnh: user báo import Unity 6.3 đã thành công (hết "VideoClip import error")
  — xác nhận giả thuyết all-intra vòng 2 đúng. Nhưng chiếu trong Unity thấy viền đen
  mờ quanh mép chi tiết mảnh (vòng tròn neon) — artifact chromakey mới, không phải
  lỗi import.
- Đưa 3 phương án fix (ngưỡng cứng alpha / erode alpha / despill+rebuild) qua
  AskUserQuestion, user chọn **erode alpha 2px**.
- Encode: `okay_vậy_hãy_tạo_video_với_hiệ_alpha_v5_erode_noaudio.webm` (ưu tiên,
  filter `colorkey→alphaextract→erosion,erosion→alphamerge`, giữ nguyên all-intra),
  `..._v6_erode_audio.webm` (đối chứng có audio Vorbis). ffprobe cả 2 PASS.
- Thử lại Chrome QC: kết nối browser thành công lần này (lỗi cũ hoá ra do có 2
  Chrome cùng kết nối tài khoản, chưa chọn máy nào — đã chọn Browser 1 macOS qua
  AskUserQuestion, `select_browser` OK). Nhưng chụp screenshot bị timeout liên tục
  trên trang có `<video autoplay loop>` (~5 lần thử, mỗi lần đợi thêm) — dừng theo
  nguyên tắc tránh rabbit hole, dọn tab + tắt local http server (`pkill http.server
  8935`), xoá `verify_erode.html` tạm.
- File đổi: `README.md` (mục "Vòng 3 — viền đen"), plan (`## QC vòng 3`, DoD, task
  list), 2 file webm mới trong `Desktop/temp_chromakey/`.
- Blocked: chờ user import `_v5_erode_noaudio.webm` vào Unity, báo còn viền đen
  không. Đây là bug mới phát sinh sau khi lỗi import đã đóng (PASS), không tính vào
  trần 3 vòng fix của lỗi import.

## 20:19 — Mở request mới: project-level TDQ workflow cho Claude Code + Codex
- Ngữ cảnh: user hỏi phân tích khả năng cài `tdq-workflow` ở project-level và tạo
  instruction hướng cả Claude Code lẫn Codex theo workflow này. Phân tích xong: Claude
  Code đã có đường sẵn (`--plugin-dir` / marketplace scope project + block trong
  `docs/notes/user-level-install.md` mục 3), nhưng Codex thì tài liệu mục 4 trỏ tới
  thư mục `portable/` đã bị **xoá ở bản 0.10.0** (gỡ mode `external`) — lỗi thời, chưa
  có đường thật. User chọn hướng B (làm cho cả hai harness).
- Quyết định: xác nhận xoá state của request đang mở (`fix-loi-import-webm-unity`,
  vẫn Blocked chờ user test Unity vòng 3 — file/plan/log của nó giữ nguyên, không mất)
  để mở request mới.
- File đổi: tạo `docs/tdq/brief/2026-08-11-cai-tdq-project-level.md` (mục Nguyên văn),
  đề xuất lane full, đang chờ user chọn lane trong chat.
- Test: không có (thuần phân tích/ghi brief).

## 20:23 — Chờ user chọn lane (full/quick) cho request project-level TDQ
- Ngữ cảnh: đã trình tóm tắt việc + đề xuất lane full trong chat, in dòng hỏi lane,
  DỪNG chờ user trả lời — chưa `init` state (chưa có lane chốt).
- File đổi turn này: không thêm/sửa file ngoài `docs/workinglog/2026-08-11.md` (mục
  này) — brief đã tạo ở entry 20:19 giữ nguyên.
- Test: không có (turn thuần hỏi/chờ duyệt).

## 20:26 — [Tầng nhỏ] Set viewMode focus cho Claude Code CLI
- Ngữ cảnh: user hỏi cách thu gọn/ẩn tool call trong Claude Code CLI. Research qua
  claude-code-guide agent + tavily-primary (GitHub issue #14511, #40428, doc chính
  thức code.claude.com/docs/settings): xác nhận CHƯA có setting `compactToolOutput`
  để mặc định collapse từng tool call (đề xuất đã bị Anthropic đóng "not planned").
  `Ctrl+O` chỉ toggle verbose (mở rộng thêm), không phải nút ẩn bớt.
- Quyết định: đề xuất `"viewMode": "focus"` — gần nhất với ý user, không đổi hành vi
  sản phẩm, 1 dòng hiển nhiên → xử lý tầng nhỏ (không mở request/state), tách biệt
  hoàn toàn với request `cai-tdq-project-level` đang mở song song.
- File đổi: `~/.claude/settings.json` (thêm `"viewMode": "focus"`, giữ nguyên mọi
  key khác — đã đọc & merge, không ghi đè).
- Test: không có test tự động (setting UI khởi động lại mới thấy hiệu lực); user tự
  xác nhận sau khi mở phiên mới.

## 20:33 — [Tầng nhỏ] Bật Focus view cho Claude Code VS Code extension
- Ngữ cảnh: research xác nhận (code.claude.com/docs/vs-code + tavily-primary) VS Code
  extension có sẵn `focusView` (mặc định false) ẩn tool call/result/thinking, gọn 1
  dòng/turn; Claude Desktop app chưa có tương đương. User yêu cầu bật giúp.
- File đổi: `~/Library/Application Support/Code/User/settings.json` (thêm
  `"claudeCode.focusView": true`, giữ nguyên các key khác — đã đọc & merge).
- Test: không có test tự động; áp dụng ngay cho mọi session Claude Code đang mở
  trong VS Code (theo doc chính thức), không cần khởi động lại.

## 20:29 — Spec + plan duyệt, chờ duyệt plan (mode)
- Ngữ cảnh: sau interview 4 câu (chốt A cả 4: dựng `portable/` chung, gộp gọn theo
  skills/ hiện tại, chỉ tài liệu không script, QC bằng đối chiếu), viết brief đủ 3 mục
  → viết spec 13 đầu ra + 6 hạng mục QC → user duyệt spec ("duyệt spec") → viết plan
  5 phase/20 task, đề xuất mode `main`.
- File đổi: `docs/tdq/brief/2026-08-11-cai-tdq-project-level.md` (đủ 3 mục + Lộ trình),
  `docs/tdq/spec/2026-08-11-cai-tdq-project-level.md` (mới), `docs/tdq/plan/2026-08-11-cai-tdq-project-level.md`
  (mới), state (`spec_file`, `spec_approved`, `plan_file`).
- Test đã chạy: `doc_lint.py` trên spec → exit 0; `doc_lint.py --pair spec plan` → exit 0.
- Trạng thái: đã trình tóm tắt plan, DỪNG chờ user duyệt (chưa implement, chưa tạo file
  `portable/` nào).

## 21:10 — Implement + QC + report xong (mode main, 20/20 task)
- Ngữ cảnh: user "duyệt plan mode main" → build end-to-end: dựng 11 file `portable/`
  (dịch từ `skills/tdq-{conventions,intake,spec,plan,build}/` hiện hành, không dùng bản
  cũ đã xoá ở 0.10.0); gộp khuôn spec-template/report-template/interview thẳng vào file
  phase liên quan (thay vì tách file riêng) để giữ đúng 11 file theo spec §2; sửa
  `docs/notes/user-level-install.md` mục 1+4; kiểm `README.md` — không cần sửa.
- File đổi (mới): `portable/AGENTS.md`, `portable/README.md`,
  `portable/workflow/{01-intake,02-spec,03-plan,04-build,phases}.md`,
  `portable/workflow/references/{approval,plan-template,qc,quick-lane}.md`.
- File đổi (sửa): `docs/notes/user-level-install.md`,
  `docs/tdq/plan/2026-08-11-cai-tdq-project-level.md` (tick 20/20 task, mục `## QC`,
  header HOÀN THÀNH), `docs/tdq/reports/2026-08-11-cai-tdq-project-level.md` (mới).
- Test đã chạy: `find portable -type f | wc -l` → 11; `diff` phases.md portable vs
  skills (chỉ khác tiền tố path, nội dung khớp); `grep -rn "external\|deep.search"
  portable/` → 1 kết quả là ghi chú lịch sử đã gắn nhãn; `grep -n "portable" README.md`
  → rỗng; `ls` 5 đường dẫn trong user-level-install.md → đều OK;
  `python3 scripts/doc_lint.py docs/tdq/spec/2026-08-11-cai-tdq-project-level.md` → exit 0.
  Tất cả 7 hạng mục QC PASS, không có FAIL, không cần vòng fix.
- Trạng thái: report đã ghi, đã trình chat, đang hỏi user có commit không.
