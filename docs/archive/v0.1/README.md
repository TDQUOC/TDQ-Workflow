# Lưu trữ v0.1

Doc của thời plugin còn dùng gate duyệt cứng (`/tdq-workflow:tdq-approve`) và bộ 10 skill.
Giữ lại để truy vết lịch sử; **không** phản ánh hành vi hiện tại — xem `README.md` ở root
và `docs/tdq/` cho bản đang chạy.
