{
  "permissions": {
    "ask": [
      "*"
    ],
    "deny": [
      "write_file(*state.json)",
      "write_file(*STATE.md)",
      "command(git checkout -b claude*)",
      "command(git branch claude*)",
      "command(git switch -c claude*)",
      "command(git worktree add*claude*)",
      "command(git checkout -b antigravity*)",
      "command(git branch antigravity*)",
      "command(git switch -c antigravity*)",
      "command(git worktree add*antigravity*)",
      "command(git checkout -b gemini*)",
      "command(git branch gemini*)",
      "command(git switch -c gemini*)",
      "command(git worktree add*gemini*)",
      "command(git checkout -b codex*)",
      "command(git branch codex*)",
      "command(git switch -c codex*)",
      "command(git worktree add*codex*)"
    ]
  }
}
